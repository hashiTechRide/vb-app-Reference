方針の確認から入ります。「非連結にする」と前の会話で決めたはずですが、今回は「データ連結させる場合」と書かれています。連結（バインド）に戻す前提でよいか、非連結でこのエラー列方式をやりたいのか、どちらかで実装が変わります。ひとまず、質問の核心である**ユニーク制約検証の性能問題**は、連結・非連結どちらでも共通なので、そこを軸に答えます。エラー列の持ち方だけ両対応で触れます。

## 質問の核心：全体ユニーク検証がデータ増加で遅くなる問題

「基準テーブルは編集中データ以外も含めてテーブル全体でユニーク性を確認する必要がある。データが増えると検証時間が増えるのでは」——これは正しい懸念です。ただ、**やり方次第でデータ量にほぼ依存しない形にできます。** 素朴にやると遅くなり、工夫すると速いままです。順に整理します。

## 遅くなる素朴なやり方（避けるべき）

編集中の各行について、全件と突き合わせるパターン。

```
編集行1件ごとに「SELECT ... WHERE key = :editRowKey」を投げる
   → 編集行がN件なら N回クエリ
   → 各クエリはインデックスがあれば速いが、N回の往復が積もる
あるいは全件をアプリに取得して突き合わせ
   → データ増加で取得量・メモリ・比較時間が線形に増える（これが懸念通り遅くなる）
```

特に「全件をアプリに持ってきて比較」が、データ増加で確実に遅くなる形です。これは避けます。

## 速いやり方1：DBのインデックスと集合演算に任せる

ユニーク検証はアプリで全件比較せず、**DBに「重複があるか」だけを問う**のが基本です。DBはインデックスで効率的に重複を探せるので、全件スキャンになりません。

編集中のキー群を渡し、それらがDB上（自分以外）に既に存在するかを一度に問い合わせます。

```sql
-- 編集中のキー群のうち、自分(編集対象Seq)以外で既に存在するものを返す
SELECT KEY_COL
FROM   BASE_TABLE
WHERE  KEY_COL IN (:key1, :key2, :key3, ...)   -- 編集中のキー群
AND    SEQ <> :editingSeq                       -- 自分自身は除外
```

`KEY_COL`にユニークインデックス（or 一意制約）があれば、この検索は件数が増えてもインデックス経由で速いままです。**データが100万件でも、インデックスがあれば該当キーの有無確認はlog時間程度**で、全件スキャンしません。返ってきたキーが「重複しているキー」なので、その行のエラー列にメッセージを入れます。

ポイントは、**アプリに全件を持ってこないこと**と、**KEY_COLにインデックスがあること**。この2つで、データ量への依存がほぼ消えます。

## 速いやり方2：編集行同士の重複はアプリ内、DB重複はDB問い合わせ

ユニーク検証には2種類あります。分けて考えると効率的です。

- **編集行同士の重複** … 今画面で編集している行の中での重複。これはアプリ内で完結（編集行はせいぜい数十〜数百件なのでメモリ内比較で一瞬）。
- **編集行とDB既存データの重複** … 上の「速いやり方1」でDBに一括問い合わせ。

```
1. 編集行のキーをアプリ内でグループ化 → 編集行同士の重複を検出（速い・件数少ない）
2. 重複していない編集行のキー群だけをDBに一括問い合わせ → DB既存との重複を検出
```

編集行同士はアプリ、DB既存はDB、と役割分担すると、どちらも全件比較を避けられます。

## 速いやり方3：最終防衛はDBの一意制約に任せる

そもそも、アプリで完璧に事前チェックしても、チェットと保存の間に他者がINSERTすれば漏れます（競合)。だから**DB側に一意制約を張り、保存時の制約違反例外を最終防衛にする**のが堅い設計です。

```
事前チェック(速いやり方1,2) … ユーザーに親切なエラー表示のため（どの行が重複か教える）
DB一意制約                  … 最終防衛。事前チェックをすり抜けた競合を確実に弾く
```

事前チェックは「UXのため」、一意制約は「整合性の最終保証」。二段構えにすると、事前チェックを軽く保ちつつ、正しさはDBが担保します。保存時に一意制約違反(ORA-00001)が出たら、その行のエラー列に反映してロールバックします。

## エラー列の持ち方（連結・非連結別）

### 連結（DataTable）の場合

SQLでエラー列を追加、というより、**DataTableにDB由来でない検証用の列を足す**のが素直です。SELECTで空のエラー列を作ってもよいですが、アプリ側で追加するほうが柔軟です。

```vb
' 取得後、検証用の列を足す（DBには無い列）
If Not _table.Columns.Contains("ERROR_MSG") Then
    _table.Columns.Add("ERROR_MSG", GetType(String))
End If
```

検証結果を各行のこの列に格納。DataTableなら`SetColumnError`と併用するとグリッドのエラー表示とも連動します。保存時はこのエラー列をDBに送らない(SELECT/INSERT対象から除外)ようにします。

### 非連結（前回の方針）の場合

行モデル`RowItem`にエラーを持たせます。列にする必要すらなく、モデルのプロパティで十分です。

```vb
Public Class RowItem
    ' ...
    Public Property ErrorMessage As String   ' 行の検証エラー
    Public ReadOnly Property HasError As Boolean
        Get
            Return Not String.IsNullOrEmpty(ErrorMessage)
        End Get
    End Property
End Class
```

エラー表示はOwnerDrawでエラー行を赤くする等、グリッド側で制御できます。非連結ならこちらのほうが素直で、SQLにエラー列を足す必要もありません。

## 検証の流れ全体

確定（保存）前の検証を、性能を意識した順で組みます。

```
1. 桁数・形式チェック（アプリ内・行単位）
   → 各行を単独で検証。DB不要。エラー列/モデルに格納。

2. 編集行同士のユニーク重複（アプリ内）
   → 編集行のキーをグループ化して重複検出。DB不要。

3. 編集行 vs DB既存のユニーク重複（DB一括問い合わせ）
   → 編集行のキー群を IN で1回問い合わせ、自分以外の既存重複を取得。
   → インデックス前提でデータ量に依存しない。

4. エラーが1件でもあれば確定中止、エラー列/モデルを表示。

5. エラーが無ければ保存実行。
   → DB一意制約が最終防衛。制約違反例外が出たらその行にエラー反映しロールバック。
```

桁数チェック（1）はDB不要でアプリ内完結。ユニーク検証だけ2段階（アプリ内＋DB一括）にするのが、性能と正確性の両立です。

## 「確定前に現状のDB値を取得して確認」について

質問にあった「確定用SQLの前に現状のDB値を取得して制約違反を確認」は、**全件取得ではなく、上の「速いやり方1」の一括問い合わせにする**のが答えです。全件を持ってくると懸念通り遅くなりますが、「編集中のキーがDBに存在するか」だけを問えば、取得量は編集行数分の結果だけで、データ量に依存しません。

```
❌ SELECT * FROM BASE_TABLE                    … 全件取得。データ増加で線形に遅く
✅ SELECT KEY FROM BASE_TABLE
   WHERE KEY IN (編集キー群) AND SEQ <> 自分   … 該当分だけ。インデックスで高速
```

## インデックスの確認（最重要）

この方式が速いかどうかは、**検証対象のキー列にインデックス（できれば一意制約）があるか**に懸かっています。無いと`IN`検索が全件スキャンになり、データ増加で遅くなります。基準テーブルのユニークキーには必ずインデックス/一意制約を張ってください。複合キーなら複合インデックス。ここが性能の生命線です。

## まとめ

- 懸念は正しいが、**やり方次第でデータ量にほぼ依存しない検証にできる**。素朴な全件取得・全件比較を避けるのが鍵。
- ユニーク検証は2種に分ける。**編集行同士の重複はアプリ内**（件数少・一瞬）、**編集行とDB既存の重複はDBに一括問い合わせ**（`WHERE KEY IN (編集キー群) AND SEQ <> 自分`）。
- DB問い合わせは**キー列のインデックス/一意制約が前提**。あれば100万件でもインデックス検索でデータ量にほぼ非依存。無いと全件スキャンで遅くなる——ここが生命線。
- 「確定前に現状のDB値を取得」は、**全件取得でなく該当キーの有無だけを問う**形にする。取得量が編集行数分で済み、データ増加の影響を受けない。
- **DB一意制約を最終防衛**に張る。事前チェックはUX（どの行が重複か教える）、制約は整合性の最終保証、の二段構え。事前チェックをすり抜けた競合も確実に弾ける。
- エラー列は、連結ならDataTableに検証用列を足す（保存時は除外）、非連結なら`RowItem.ErrorMessage`に持つ。非連結ならSQLにエラー列を足す必要はない。
- 桁数・形式チェックはアプリ内・行単位で完結。DB不要。ユニーク検証だけ2段階にする。

データ増加で遅くなるかどうかは、ほぼ「キー列にインデックスがあるか」と「全件を取ってこないか」の2点で決まります。この2つさえ守れば、検証時間はデータ量ではなく編集行数（数十〜数百）に比例するだけになり、テーブルが大きくなっても事実上一定に保てます。まずは基準テーブルのユニークキーにインデックス／一意制約があるかを確認するところから始めてください。
