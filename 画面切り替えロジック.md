二つに分けて答えます。貼り付けロジックと、Oracle接続インスタンスの置き場所です。

## タブ切り替えの貼り付けロジック

UserControlをタブごとに保持する方針なので、**生成は1回、切り替えは表示の差し替えだけ**にします。`Panel.Controls.Add` を毎回呼ぶのではなく、`Visible` の切り替えで見せ替えるのが軽くてチラつきません。

```vb
Public Class MainForm

    Private ReadOnly _controls As New Dictionary(Of TabKind, UserControl)
    Private _activeControl As UserControl

    Private Sub MainForm_Load(sender As Object, e As EventArgs) Handles MyBase.Load
        ' 全タブのUserControlを一度だけ生成し、パネルに積んでおく（最初は非表示）
        _controls(TabKind.Standard) = New StandardEditorControl()
        _controls(TabKind.Large)    = New ListEditorControl()
        _controls(TabKind.Medium)   = New ListEditorControl()
        _controls(TabKind.Small)    = New ListEditorControl()

        For Each kv In _controls
            Dim uc = kv.Value
            uc.Dock = DockStyle.Fill
            uc.Visible = False
            contentPanel.Controls.Add(uc)   ' 全部このパネルに重ねて入れておく
        Next

        ActivateTab(TabKind.Standard)        ' 初期は基準項目
    End Sub
```

切り替えは、今出ているものを隠して目的のものを出すだけ。

```vb
    Private Sub ActivateTab(kind As TabKind)
        Dim target = _controls(kind)
        If ReferenceEquals(target, _activeControl) Then Return  ' 同じタブの連打を無視

        If _activeControl IsNot Nothing Then
            _activeControl.Visible = False
        End If

        target.Visible = True
        target.BringToFront()
        _activeControl = target

        UpdateTabButtonStyles(kind)   ' 選択中ボタンの見た目を変える
    End Sub
```

タブボタンのクリックは、各ボタンに対応する `TabKind` を `Tag` に持たせて共通ハンドラで受けると、ボタンが増えても1メソッドで済みます。

```vb
    ' デザイナ or Load で：standardTabButton.Tag = TabKind.Standard … と設定しておく
    Private Sub TabButton_Click(sender As Object, e As EventArgs) _
            Handles standardTabButton.Click, largeTabButton.Click,
                    mediumTabButton.Click, smallTabButton.Click
        Dim kind = DirectCast(DirectCast(sender, Button).Tag, TabKind)
        ActivateTab(kind)
    End Sub
```

選択中タブの見た目（色を変える等）も1箇所で。

```vb
    Private Sub UpdateTabButtonStyles(active As TabKind)
        For Each btn In tabButtonPanel.Controls.OfType(Of Button)()
            Dim kind = DirectCast(btn.Tag, TabKind)
            If kind = active Then
                btn.BackColor = Color.FromArgb(0, 120, 215)   ' 選択中
                btn.ForeColor = Color.White
            Else
                btn.BackColor = SystemColors.Control            ' 非選択
                btn.ForeColor = SystemColors.ControlText
            End If
        Next
    End Sub
End Class
```

### Visible切り替え vs Add/Remove

**Visible方式（推奨）** は全UserControlを最初からパネルに積んでおき、表示だけ切り替える。状態もハンドルも保たれ、再表示が速い。タブ4枚なら常駐コストは問題になりません。

**Add/Remove方式** は切り替え時に `contentPanel.Controls.Clear()` してから `Add` する。常駐は減りますが、`Clear` はコントロールを破棄しないので保持方針とは噛み合い、メリットが薄い。保持するなら Visible 方式が素直です。

チラつきが気になるなら、`contentPanel` に `DoubleBuffered` を効かせる派生パネルを使うと滑らかになります。

## Oracle接続インスタンスの置き場所

**`DbLockState` に持たせるのは反対です。** 役割が違うものを1クラスに混ぜると、前半でずっと整理してきた責任分離が崩れます。

`DbLockState` は「今ロックされているか・誰がか」という**状態を通知する**のが役割。Oracleの接続（`OracleConnection`）は**インフラ資源の管理**で、関心事が別です。混ぜると、UIが購読する状態クラスがDB接続を抱える歪な形になります。

接続は専用のクラスに分けます。

```vb
' DB接続をForm共通で1つ保持する責務だけを持つ
Public Class OracleDbContext
    Implements IDisposable

    Private ReadOnly _connection As OracleConnection

    Public Sub New(connectionString As String)
        _connection = New OracleConnection(connectionString)
    End Sub

    Public ReadOnly Property Connection As OracleConnection
        Get
            If _connection.State <> ConnectionState.Open Then
                _connection.Open()
            End If
            Return _connection
        End Get
    End Property

    Public Sub Dispose() Implements IDisposable.Dispose
        _connection?.Dispose()
    End Sub
End Class
```

そして両方をFormが共有資源として1つずつ持ち、各ViewModelに渡します。

```vb
Public Class MainForm
    Private ReadOnly _db As OracleDbContext
    Private ReadOnly _lockState As New DbLockState()
    Private ReadOnly _modeState As New EditorModeState()

    Public Sub New()
        InitializeComponent()
        _db = New OracleDbContext(GetConnectionString())
        ' ViewModel生成時に _db, _lockState, _modeState を注入
    End Sub

    Protected Overrides Sub Dispose(disposing As Boolean)
        If disposing Then
            _db?.Dispose()           ' 接続はForm破棄時に確実に閉じる
            components?.Dispose()
        End If
        MyBase.Dispose(disposing)
    End Sub
End Class
```

### 接続の保ち方についての注意

「Form共通で1インスタンス」を**1本の `OracleConnection` を開きっぱなしで共有する**意味で言っているなら、一点気をつけてください。長時間1本を握り続けると、ネットワーク断やタイムアウトで切れたときに全機能が巻き込まれます。Oracleでは通常、**接続プーリング**（接続文字列で有効）を使い、必要なときに開いて使ったら閉じる（プールに返す）のが定石です。その場合「共有したいもの」は接続オブジェクトそのものではなく、**接続文字列とプール設定を持つ `OracleDbContext`** になります。

```
本当に共有したいもの = 接続の作り方（接続文字列・プール）
共有したくないもの   = 1本の物理接続を握り続けること
```

どちらの方式でも、置き場所は `OracleDbContext` のような専用クラスが適切で、`DbLockState` ではありません。

## 全体の共有資源の構図

ここまでの共有オブジェクトが3つに整理されます。

```
MainForm が1つずつ保持し、各ViewModelへ注入
   ├─ OracleDbContext   … DB接続（インフラ資源）。Form破棄時にDispose
   ├─ DbLockState       … ロック状態の通知（UIが購読）
   └─ EditorModeState   … 編集/削除/復元モードの通知（UIが購読）
```

役割が「資源管理」「状態通知」「状態通知」とそれぞれ独立しているので、別クラスのままにします。`DbLockState` が `OracleDbContext` を使ってロックを取得・確認することはあり得ますが、それは `DbLockState` が `OracleDbContext` を**参照する**関係であって、接続を**所有する**わけではありません。

```vb
' DbLockState はDB接続を「使う」が「持たない」
Public Class DbLockState
    Private ReadOnly _db As OracleDbContext
    Public Sub New(db As OracleDbContext)
        _db = db
    End Sub
    ' _db.Connection を使ってロック取得・解放・確認
End Class
```

## まとめ

- 貼り付けは**全UserControlを起動時にパネルへ積み、Visibleで見せ替え**る。切り替えは隠す→出す→`BringToFront` だけ
- タブボタンは `Tag` に `TabKind` を持たせ共通ハンドラで受ける。選択中スタイルも1メソッド
- Oracle接続は **`DbLockState` に入れない**。`OracleDbContext` のような専用クラスに分け、Formが1つ持ってForm破棄時に `Dispose`
- 「共有したい」のは実質**接続文字列＋プール設定**。1本の物理接続を握り続ける方式はリスクがあるので、プーリング前提を勧める
- `DbLockState` は接続を**使うが所有しない**。資源管理と状態通知は別クラスのままにする

接続を `DbLockState` に同居させたくなるのは、どちらも「DB」という言葉で括れるからですが、クラスを分ける基準は名詞の一致ではなく**責任（資源管理か状態通知か）**です。ここはこれまでの議論と一貫しています。


二つとも、これまで作った共有状態と通知の仕組みに乗ります。順に整理します。

## 1. 親が更新されたら子グリッドも更新する

タブは基準項目→大→中→小と階層で、各行は一つ左の項目に紐づいていました。「親の情報が更新されたら子を更新」は、この親子連鎖の再読み込みです。

### 何を「更新」と呼ぶか整理

二通りあり、対処が違います。

- **親の選択が変わった**（別の親レコードを選んだ）→ 子はその親に紐づくデータを読み直す。
- **親の内容が保存された**（同じ親のデータが変わった）→ 子の表示を最新化する、または親キーが変わったなら子を再取得。

どちらも「親の変化を子が知って読み直す」構造は同じです。前回の共有状態と同じく、**親の状態変化を通知し、子が購読して再読み込みする**形にします。子から親を直接触らせない（向きを保つ）。

### 選択中の親キーを共有状態にする

現在どの親が選択されているかを共有オブジェクトで持ち、変化を通知します。

```vb
Public Class SelectionState
    Implements INotifyPropertyChanged

    Private _standardSeq As Long   ' 基準項目の選択Seq
    Public Property StandardSeq As Long
        Get
            Return _standardSeq
        End Get
        Set(value As Long)
            If _standardSeq <> value Then
                _standardSeq = value
                RaiseEvent PropertyChanged(Me, New PropertyChangedEventArgs(NameOf(StandardSeq)))
            End If
        End Set
    End Property

    ' 大の選択が変われば中の親キーになる…と階層ごとに持つ
    Private _largeSeq As Long
    Public Property LargeSeq As Long
        ' 同様に通知
    End Property
    ' 中→小も同様
End Class
```

### 子ViewModelが親の変化を購読して読み直す

大ViewModelは基準項目の選択を購読し、変わったら自分のデータを読み直す。同時に、大の選択が変わったことを次の階層（中）に伝えるため、自分の選択も更新します。

```vb
Public Class LargeEditorViewModel
    Public Sub New(repo As IListRepository, selection As SelectionState, ...)
        _selection = selection
        ' 親（基準項目）の選択変化を購読
        AddHandler _selection.PropertyChanged,
            Sub(s, e)
                If e.PropertyName = NameOf(SelectionState.StandardSeq) Then
                    Reload()
                End If
            End Sub
    End Sub

    Private Sub Reload()
        Items.Clear()
        For Each m In _repository.GetByParent(_selection.StandardSeq)
            Items.Add(m)
        Next
        ' 大の選択をリセット（子=中に伝わる）
        _selection.LargeSeq = Items.FirstOrDefault()?.Seq
    End Sub
End Class
```

中は`LargeSeq`を購読、小は中の選択を購読、と連鎖します。**親が変われば通知が下流へ伝播し、各階層が自分のデータを読み直す。**一方向で、子から親を触らない。

```
基準項目を保存/選択変更
   → SelectionState.StandardSeq 更新
      → 大が購読して Reload → LargeSeq 更新
         → 中が購読して Reload → MediumSeq 更新
            → 小が購読して Reload
```

### 未保存の編集がある子の扱い（重要）

親が変わったとき、子に未保存の編集があると、読み直しで消えます。前回の`IsDirty`ガードをここでも効かせます。親を変える前に子がDirtyなら確認する。

```vb
Private Sub Reload()
    If IsDirty Then
        ' 破棄確認（前回の三択と同じ）。破棄しないなら親変更を差し戻すか、保存を促す
    End If
    ' 読み直し
End Sub
```

親子連鎖では「親を変えると複数の子が一気に読み直される」ので、どこか一つでも未保存があれば止める、という設計にしておくと事故が減ります。

## 2. 再ロック失敗後、編集モードに固定する

前回は「再ロック失敗＝読み取り専用（IsLocked=True）」にしました。今回の要件は少し違って、「編集モードに固定したい」。整理すると、**モードの遷移を禁止して編集モードに留める**ということですね。読み取り専用と組み合わせると、こうなります。

- 再ロック失敗 → 読み取り専用（編集はできない）＋ モードは編集モードに固定（削除・復元へ行かせない）。

削除・復元モードは「これから何か変更する」モードなので、編集権を失った状態で入れてはいけない。だから編集モードに固定するのは筋が通っています。

### モード固定の状態を持つ

`EditorModeState`に「固定されているか」を足し、固定中は遷移を拒否します。

```vb
Public Class EditorModeState
    Private _locked As Boolean   ' モード遷移そのものを禁止

    Public Sub FixToEdit()
        Mode = EditorMode.Edit    ' 編集モードへ戻す
        _locked = True            ' 以降の遷移を禁止
    End Sub

    Public Function CanTransit(toMode As EditorMode) As Boolean
        If _locked Then Return False   ' 固定中はどこへも遷移不可
        Return Allowed(_mode).Contains(toMode)
    End Function
End Class
```

再ロック失敗時に`FixToEdit`を呼ぶ。

```vb
' 再ロック失敗時（前回のReacquireLockの失敗分岐）
If Not result.Acquired Then
    _modeState.FixToEdit()        ' 編集モードに固定
    _lockState.Apply(result)      ' 読み取り専用（IsLocked=True）
    RaiseEvent LockLost(...)
End If
```

## 3. 遷移ボタンは非表示にすべきか、無効化すべきか

これは設計判断です。**無効化（グレーアウト）を勧めます。非表示は避けたほうがよい**、というのが一般的な指針です。

理由を整理します。

- **非表示**は「その操作が存在すること自体」を隠します。ユーザーは「削除ボタンがあったはずなのに消えた」と混乱し、なぜ消えたのか分からない。UIの構造が状況で変わると、操作の一貫性が失われます。
- **無効化**は「操作は存在するが、今はできない」を示せます。ツールチップやメッセージで理由（排他が取れないため読み取り専用）を添えれば、ユーザーは状況を理解できます。

今回はまさに「本来できるが、再ロック失敗という一時的な理由でできない」状態です。これは無効化が表す状態そのものです。恒久的に存在しない機能なら非表示もありですが、状況依存で一時的にできないだけなら無効化が適切。

```vb
Private Sub RefreshButtons()
    Dim canEdit = Not _lockState.Info.IsLocked
    ' モード遷移ボタンは「遷移可能か」で活性を決める（固定中は自動でFalse）
    deleteModeButton.Enabled = _modeState.CanTransit(EditorMode.Delete) AndAlso canEdit
    restoreModeButton.Enabled = _modeState.CanTransit(EditorMode.Restore) AndAlso canEdit
    saveButton.Enabled = canEdit
End Sub
```

`CanTransit`が固定中はFalseを返すので、モード遷移ボタンは自動で無効になります。ボタンの活性判定が`CanTransit`一本に集約され、「固定されたら押せない」が自然に効きます。

### 理由を伝える

無効化しただけだと「なぜ押せない?」となるので、読み取り専用に落ちたことのメッセージ（前回の`LockLost`）に加え、ボタンのツールチップで理由を出すと親切です。

```vb
Dim tip As New ToolTip()
If _lockState.Info.IsLocked Then
    tip.SetToolTip(deleteModeButton, "排他ロックを取得できないため、この画面は読み取り専用です")
End If
```

## まとめ

**親→子の更新**

- 「選択中の親キー」を共有状態（`SelectionState`）で持ち、変化を通知。子が購読して自分のデータを読み直し、自分の選択を更新して次の階層へ伝播させる。基準→大→中→小と一方向に連鎖。
- 子から親を触らず、親の変化を子が購読する向きを保つ（これまでと同じ原則）。
- 読み直しで未保存編集が消えるので、`IsDirty`ガードを親変更時にも効かせる。

**再ロック失敗時の編集モード固定**

- `EditorModeState`に固定フラグを足し、`FixToEdit`で編集モードへ戻して以降の遷移を禁止（`CanTransit`がFalseを返す）。読み取り専用（`IsLocked=True`）と併用。
- 削除・復元は「これから変更する」モードなので、編集権を失った状態では入らせない。編集モード固定は妥当。

**遷移ボタンは非表示か無効化か**

- **無効化を勧める。** 今回は「本来できるが一時的な理由でできない」状態で、これは無効化が表す状態。非表示は操作の存在を隠してユーザーを混乱させ、UIの一貫性を損なう。
- `CanTransit`が固定中にFalseを返すようにすれば、ボタン活性は`CanTransit`一本に集約され、固定が自動で反映される。
- 無効化の理由はメッセージやツールチップで伝えると親切。

非表示にしたくなるのは「押せないボタンを見せたくない」からだと思いますが、業務アプリでは「なぜできないか分かる」ことのほうが重要です。存在は見せて、できない理由を添える。恒久的に無い機能でない限り、無効化で揃えるのが一貫性の面でも優れています。
