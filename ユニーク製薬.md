```markdown
# VB.NET DataTable ユニーク制約の重複検出まとめ

## 背景・課題
- 単一列ユニークと複合列ユニークの両方が存在する。
- `DataTable` の `UniqueConstraint` は違反時に `ConstraintException` を投げるが、**どの列・どの値で違反したかを機械的に取得しづらい**。
- そのため、ユーザー提示や事前バリデーションには LINQ による事前チェックを使う。
- `UniqueConstraint` は最終防衛線として別途張り、事前チェックと**二段構え**にするのが実務的。

## 方針
- 単一列を「1列だけの複合キー」として扱い、単一・複合を同一ロジックで処理する。
- ユニークキー定義を「配列の配列（`List(Of String())`）」で渡す。
- 各キー定義ごとに、違反したキー値・件数・該当行を返す。

## 結果を受け取る型

```vb
Public Class UniqueViolation
    Public Property Columns As String()          ' 違反したキーを構成する列名
    Public Property Duplicates As List(Of DuplicateEntry)
End Class

Public Class DuplicateEntry
    Public Property KeyValues As String()         ' 重複したキー値（列順）
    Public Property Count As Integer              ' 重複件数
    Public Property Rows As List(Of DataRow)      ' 該当行
End Class
```

## 重複検出関数

```vb
''' <summary>
''' 複数のユニークキー定義（単一列・複合列混在可）について重複を検出する
''' </summary>
''' <param name="table">対象テーブル</param>
''' <param name="uniqueKeys">
''' ユニークキー定義のリスト。各要素が1つのユニーク制約で、
''' その中の String() が制約を構成する列名（1列なら単一、複数なら複合）
''' </param>
''' <returns>違反したキー定義ごとの、重複キー値リスト</returns>
Public Function FindUniqueViolations( _
        table As DataTable, _
        uniqueKeys As List(Of String())) As List(Of UniqueViolation)

    Dim results As New List(Of UniqueViolation)

    For Each keyCols In uniqueKeys
        ' キー列すべてが非NULLの行のみ対象（NULLは制約対象外とする場合）
        Dim dupGroups = table.AsEnumerable() _
            .Where(Function(r) keyCols.All(Function(c) Not r.IsNull(c))) _
            .GroupBy(Function(r) BuildKey(r, keyCols)) _
            .Where(Function(g) g.Count() > 1) _
            .ToList()

        If dupGroups.Any() Then
            Dim v As New UniqueViolation With {
                .Columns = keyCols,
                .Duplicates = New List(Of DuplicateEntry)
            }
            For Each g In dupGroups
                v.Duplicates.Add(New DuplicateEntry With {
                    .KeyValues = g.Key.Split(ChrW(1)),   ' 区切り復元
                    .Count = g.Count(),
                    .Rows = g.ToList()
                })
            Next
            results.Add(v)
        End If
    Next

    Return results
End Function

' 複数列を1つのキー文字列に結合（区切りに制御文字を使い衝突回避）
Private Function BuildKey(r As DataRow, cols As String()) As String
    Return String.Join(ChrW(1), cols.Select(Function(c) Convert.ToString(r(c))))
End Function
```

## 使い方

```vb
Dim keys As New List(Of String()) From {
    New String() {"Code"},              ' 単一列ユニーク
    New String() {"Email"},             ' 単一列ユニーク
    New String() {"ColA", "ColB"}       ' 複合ユニーク
}

Dim violations = FindUniqueViolations(table, keys)

For Each v In violations
    Console.WriteLine($"■ ユニーク違反: 列 [{String.Join(", ", v.Columns)}]")
    For Each d In v.Duplicates
        Console.WriteLine($"  値 ({String.Join(", ", d.KeyValues)}) が {d.Count} 件重複")
    Next
Next
```

出力イメージ：

```
■ ユニーク違反: 列 [Code]
  値 (A001) が 2 件重複
■ ユニーク違反: 列 [ColA, ColB]
  値 (X, 100) が 3 件重複
```

## 補足・注意点

- **単一列も1要素配列で渡す**（`New String() {"Code"}`）ことで、複合と同じコードパスで処理でき、分岐不要で保守が楽。
- **区切り文字に `ChrW(1)`（制御文字SOH）を使用**しているのは、`"A"+"B"` と `"AB"` のような結合衝突を避けるため。通常データに含まれない文字を選定。値にこの文字が混入し得る場合は別の一意な区切りに変更する。
- **NULL の扱いは要件次第**。上記は「キー列にNULLを含む行は制約対象外」（SQLの一般的な UNIQUE 制約と同じ挙動）。NULLも一意判定に含めたい場合は `Where` の絞り込みを外し、`BuildKey` で NULL を専用トークンに変換する。
- **`UniqueConstraint` は最終防衛線として別途張る**。この関数はユーザー提示・事前バリデーション用に使い、二段構えにする。

## 参考：UniqueConstraint の張り方（最終防衛線）

```vb
' 単一列
Dim uc1 As New UniqueConstraint("UQ_Code", table.Columns("Code"))
table.Constraints.Add(uc1)

' 複合列
Dim uc2 As New UniqueConstraint("UQ_Multi", _
    New DataColumn() {table.Columns("ColA"), table.Columns("ColB")})
table.Constraints.Add(uc2)
' ※すでに重複があると Constraints.Add 時点で ConstraintException
```
```

ファイルとして書き出しが必要であれば `.md` で渡します。
