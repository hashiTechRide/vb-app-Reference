# 要件定義書と機能仕様書の運用ルール

## なぜ2種類の書類を作るのか

要件定義書だけで運用してきたが、以下の問題が出ている。

- ユーザー要望がコロコロ変わるたびに要件定義書全体を更新する必要があり、メンテが大変
- 「何を作るか」は書かれているが「なぜそうしたか」が残らない
- 結果、引継ぎ時にユーザーへ再確認するハメになる

そこで、目的の違う2つの書類に分ける。

| 書類 | 何のために書くか | 誰のために書くか |
|------|------------------|------------------|
| **要件定義書** | 顧客との合意の証跡 | 顧客 |
| **機能仕様書** | 実装の答え合わせ | チーム内・未来の自分 |

---

## それぞれの書類の役割

### 要件定義書(減量版)

- **位置づけ**: 顧客と握る契約レベルの文書。原則変更しない
- **書く内容**: 顧客と合意しないと後で揉めることだけ
- **更新タイミング**: 要件定義フェーズ完了後はほぼ更新なし
- **ID管理**: なし

**書く5項目**:
1. プロジェクト概要(目的・ゴール・対象業務範囲・**対象外業務**)
2. 機能の輪郭(機能名と1〜2行の説明だけ)
3. 業務ルール(法令・社内規程由来の変えられないルール)
4. リカバリー要件(バックアップ・障害対応・データ復旧・業務継続・保管廃棄)
5. 制約・前提条件

### 機能仕様書

- **位置づけ**: チーム内文書。実装の答え合わせ用の生きた文書
- **書く内容**: 実装に必要な情報すべて
- **更新タイミング**: ユーザー意見・実装中の気づきで随時更新OK
- **ID管理**: なし(シート名=機能名で識別)
- **単位**: 1機能=1シート

**書く7項目**:
1. 要求内容(「〜したい」+ 出典)
2. 機能(「〜できる」+ 優先度 Must/Should/Could)
3. 画面項目仕様(項目名・型・桁・必須・初期値・バリデーション)
4. UIイメージ(モックアップ・MVPレベルでOK)
5. **UX仕様(状態遷移・ボタン制御 + ★決定理由)**
6. **変更履歴(版・日付・内容 + ★変更理由・更新者)**
7. 関連情報

---

## 運用ルール

### 基本原則

#### 1. 要件定義書は触らない、機能仕様書を更新する

ユーザー要望が変わったら、まず機能仕様書側で対応する。要件定義書は基本的に書き換えない。

#### 2. ID管理はしない

REQ-XX や F-XX のようなIDは使わない。要件定義書と機能仕様書のつながりは「機能名(シート名)」で表現する。

#### 3. ★決定理由を必ず書く

機能仕様書のUX仕様欄で「保存ボタンはエラー時にグレーアウト」と書いたら、その理由(誰の要望か、なぜそうしたか)も書く。1行でいい。

例:
> 検索ボタンは検索実行中は押下不可
> → **理由**: 二重実行による負荷を防ぐため。山田課長より「無駄な待ち時間を避けたい」との要望(2026/04/20)

#### 4. ★変更履歴は必ず残す

ユーザー意見で仕様を変えたら、変更履歴に記録する。「何を」「なぜ」「いつ」「誰が」の4点セット。

例:
> 1.1 / 2026/05/10 / BOM反映状況列を追加 / 山田課長より「BOMの反映漏れを画面上で気づきたい」との要望 / ○○

#### 5. 見た目の細部は書かない

hover色やfocusのoutline等は書かない。書きすぎると守られなくなる。書くのは「業務的に意味のある状態変化」だけ。

---

### 要件定義書と機能仕様書、どっちに書くべきか

迷ったらこの基準で判断する。

| 質問 | Yes なら |
|------|----------|
| 顧客と握らないと揉める? | 要件定義書 |
| 変更時に見積見直しが必要? | 要件定義書 |
| 画面を見ながら議論して決めるもの? | 機能仕様書 |
| 実装者が変わっても困らない情報? | 機能仕様書 |

具体例:
- 「変更点を一覧・詳細で管理できること」(機能の存在) → 要件定義書
- 「Rev/Ver履歴を10年保管」(業務ルール) → 要件定義書
- 「保存ボタンは入力エラー時にグレーアウト」(UX) → 機能仕様書
- 「BOM列が`—`の時は連携対象外」(表示ルール) → 機能仕様書

---

### 要望変更が来た時のフロー

```
ユーザー要望が出た
    ↓
要件定義書の範囲内?
    ├─ Yes → 機能仕様書だけ更新 + 変更履歴に記録
    │        (チームDRI判断でOK)
    │
    └─ No  → 要件定義書側に「追加合意メモ」を追加
             (PM・顧客と合意必要、見積見直しの可能性あり)
```

判断基準:
- スコープ範囲内の細部変更 → チームDRI判断(週次定例で報告)
- 新機能追加・既存機能の大幅変更 → PM判断
- 予算・納期に影響 → 顧客合意必須

---

## 書類作成時の注意点

### 要件定義書を書く時

- **対象外業務を必ず書く**: 「何をしないか」を書かないと後で要求が膨らむ
- **機能の輪郭は1〜2行で**: 詳細は機能仕様書に逃がす
- **業務ルールは出典を明記**: ISO9001・社内規程・法令等の根拠を必ず書く
- **リカバリー要件はエンジニア側で起案**: ユーザーに白紙で聞かない。テンプレを埋めて「これで進めます」と提示
- **制約は具体的に書く**: 「速く」ではなく「3秒以内」のように数値で

### 機能仕様書を書く時

- **要求は出典付きで書く**: 「○○さんが△月△日に発言」を必ず付ける
- **画面項目は実装担当が迷わない粒度で**: 曖昧な言葉を避ける
- **UIイメージはMVPでOK**: 手書きスキャン・Excel図形・PowerPointなんでもOK
- **★UX仕様には決定理由を必ず書く**: 書かないと未来の実装者が勝手に変える
- **★変更履歴は記入忘れない**: ユーザー意見で変えた事実を残す

---

## チームでの使い方

1. テンプレファイル(`要件定義書テンプレート_減量版.xlsx` / `機能仕様書テンプレート.xlsx`)を共有フォルダから取得
2. 機能仕様書は1機能=1シートで作成。「99_機能仕様書(空テンプレ)」をコピーしてシート名を機能名にリネーム
3. 書き方に迷ったら「01_変更点管理画面(記入例)」シートを参照
4. 週次定例で機能仕様書の更新状況をレビュー(変更履歴・決定理由の記入漏れチェック)
5. ユーザー意見で仕様変更があった時、その場で機能仕様書を開いて変更履歴に記入する習慣を付ける

---

## よくある質問

**Q1: ID管理がないと、要求と機能の対応が追えなくなりませんか?**
A: 機能仕様書の中で要求と機能を同じシート内に書くので、シート単位で完結します。シートを開けば対応が見えます。

**Q2: 機能仕様書の「決定理由」って、毎回書くのは大変では?**
A: 1行でOK。「山田課長要望(2026/04/20)」だけでも十分。書く習慣を付けることが重要。

**Q3: 要件定義書が薄すぎてユーザーが不安がるのでは?**
A: 「機能の詳細は機能仕様書で管理しています」と最初に説明する。役割分担を理解してもらえば問題ない。

**Q4: 非機能要件って、本当にユーザーに聞かなくていいの?**
A: 性能・稼働率等の数値はユーザーに聞いても答えが出ないのでエンジニア側で決める。ただしリカバリー要件(バックアップ・障害対応等)は顧客と合意する。

**Q5: 過去の案件もこの運用に変えるの?**
A: いいえ。まず変更点管理システムで試行する。効果が確認できたら他案件にも展開する想定。

---

## 困った時の連絡先

運用ルールで迷ったら、まず気軽にDRIに相談してください。記入例シートと相談で大体解決します。
