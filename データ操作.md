DataTableをデータソースにする前提だと、これまでの「強い型のRecordModel + RowState」とは別のやり方になります。DataTableには変更追跡（RowState）が標準で組み込まれているので、それを使うのが筋です。ViewModelごとにカラムが違う問題も、DataTableなら型を分けずスキーマ違いとして扱えます。順に整理します。

## 前提の整理：DataTableは変更追跡を内蔵している

自前で `RowEditState` を持たせていたのは、強い型のモデルに変更履歴が無いからでした。`DataTable` を使うなら不要です。各 `DataRow` が `RowState`（`Added`/`Modified`/`Deleted`/`Unchanged`）を自動で持ち、`GetChanges()` で変更行だけ抜き出せます。**自前のRowState管理はDataTableに任せる**のが、二重管理を避ける正解です。

```
DataRow.RowState … System.Data が自動で追跡（編集すれば Modified、追加すれば Added…）
table.GetChanges() … 変更があった行だけの DataTable を返す
table.AcceptChanges() … 保存後に全行を Unchanged に確定
```

## カラム構成がViewModelごとに違う問題

共通の強い型モデルは使えない、という判断はDataTable前提なら自然に解決します。**型は共通（DataTable）のまま、スキーマ（カラム定義）だけViewModelごとに変える**からです。強い型クラスを4つ作る必要はありません。

ただし「型は共通だが中身の構造が違う」ので、ViewModelの抽象は**カラムに依存しない操作だけ**を共通IFに置き、カラム固有の知識は各ViewModelに閉じ込めます。

```vb
' 共通IF：カラムに依存しない操作だけ
Public Interface ITableEditorViewModel
    Inherits IEditorViewModel        ' LockInfo / Actions など既存の共通契約
    ReadOnly Property Table As DataTable
    Sub Load(parentSeq As Long)
    Function Validate() As IReadOnlyList(Of ValidationError)
    Sub Save()
    ReadOnly Property IsDirty As Boolean
End Interface
```

`Table` は `DataTable` 型で共通。中のカラムが何かは各ViewModelの実装が決めます。Form・UserControlは `ITableEditorViewModel` までしか知らず、カラム差異に触れません。

## ViewModelごとのスキーマ定義

各ViewModelが自分のテーブル構造を作ります。ここがカラム差異の置き場所です。

```vb
Public Class LargeEditorViewModel
    Implements ITableEditorViewModel

    Private ReadOnly _repository As ILargeRepository
    Private _table As DataTable

    Public ReadOnly Property Table As DataTable Implements ITableEditorViewModel.Table
        Get
            Return _table
        End Get
    End Property

    Public Sub Load(parentSeq As Long) Implements ITableEditorViewModel.Load
        _table = _repository.GetByParent(parentSeq)   ' Repositoryがスキーマごと返す
        ' 主キーを設定しておくと更新・削除のSQL生成や検索が楽
        _table.PrimaryKey = New DataColumn() {_table.Columns("SEQ")}
    End Sub
End Class
```

スキーマはRepository側で作って返すのが一貫します（DBのカラムとDataTableのカラムを対応させる責任はデータ層にある）。ViewModelはそれを受け取るだけ。

## Repository：DataTableで取得し、スキーマを持つ

`OracleDataAdapter` を使うと、SELECT結果から `DataTable` を自動構築でき、`Update` で変更行を一括反映できます。カラムが違っても同じ仕組みで扱えます。

```vb
Public Class OracleLargeRepository
    Implements ILargeRepository

    Private ReadOnly _db As OracleDbContext

    Public Function GetByParent(parentSeq As Long) As DataTable _
            Implements ILargeRepository.GetByParent
        Dim table As New DataTable("T_LARGE")
        Using conn = _db.OpenConnection()
            Using da As New OracleDataAdapter(
                    "SELECT SEQ, PARENT_SEQ, NAME, VALUE, NOTE, FLAG " &   ' 大固有のカラム
                    "FROM T_LARGE WHERE PARENT_SEQ = :p ORDER BY SEQ", conn)
                da.SelectCommand.Parameters.Add("p", OracleDbType.Int64).Value = parentSeq
                da.Fill(table)
            End Using
        End Using
        Return table
    End Function
End Class
```

中・小はカラムもテーブルも違いますが、同じ構造で別SQLを書くだけです。共通化したければSQLとテーブル名を注入する1実装でも回せます。

## 保存：GetChangesとAdapterで一括

自前でAdded/Modified/Deletedを振り分ける必要はありません。DataTableの変更追跡とAdapterに任せます。

```vb
Public Sub Save(table As DataTable) Implements ILargeRepository.Save
    Dim changes = table.GetChanges()      ' 変更行だけ（無ければ Nothing）
    If changes Is Nothing Then Return

    Using conn = _db.OpenConnection()
        Using da As New OracleDataAdapter()
            da.SelectCommand = New OracleCommand(
                "SELECT SEQ, PARENT_SEQ, NAME, VALUE, NOTE, FLAG FROM T_LARGE", conn)

            ' INSERT/UPDATE/DELETE コマンドを定義（バインド変数で）
            da.InsertCommand = BuildInsertCommand(conn)
            da.UpdateCommand = BuildUpdateCommand(conn)
            da.DeleteCommand = BuildDeleteCommand(conn)

            Using tx = conn.BeginTransaction()
                da.InsertCommand.Transaction = tx
                da.UpdateCommand.Transaction = tx
                da.DeleteCommand.Transaction = tx
                Try
                    da.Update(changes)        ' RowStateに応じて自動で振り分け実行
                    tx.Commit()
                    table.AcceptChanges()     ' 成功後に全行を確定（Unchangedへ）
                Catch
                    tx.Rollback()
                    table.RejectChanges()     ' 失敗時は編集を巻き戻す（任意）
                    Throw
                End Try
            End Using
        End Using
    End Using
End Sub
```

`OracleCommandBuilder` で自動生成もできますが、業務テーブルでは**明示的にInsert/Update/Deleteコマンドを書く**ほうが安全で、楽観排他条件（更新日時チェック）も仕込めます。カラムごとの `SourceColumn` 対応を一度書けば、Adapterが各DataRowの値を流し込みます。

```vb
Private Function BuildUpdateCommand(conn As OracleConnection) As OracleCommand
    Dim cmd As New OracleCommand(
        "UPDATE T_LARGE SET NAME=:NAME, VALUE=:VALUE, NOTE=:NOTE, FLAG=:FLAG " &
        "WHERE SEQ=:SEQ", conn)
    AddSrcParam(cmd, "NAME", OracleDbType.Varchar2, "NAME")
    AddSrcParam(cmd, "VALUE", OracleDbType.Varchar2, "VALUE")
    AddSrcParam(cmd, "NOTE", OracleDbType.Varchar2, "NOTE")
    AddSrcParam(cmd, "FLAG", OracleDbType.Int32, "FLAG")
    AddSrcParam(cmd, "SEQ", OracleDbType.Int64, "SEQ")
    Return cmd
End Function

' SourceColumn を指定すると、各DataRowの該当カラム値が自動でバインドされる
Private Sub AddSrcParam(cmd As OracleCommand, name As String,
                        type As OracleDbType, sourceColumn As String)
    Dim p = cmd.Parameters.Add(name, type)
    p.SourceColumn = sourceColumn
End Sub
```

## C1FlexGridへのバインドと編集確定

`DataTable` を `C1FlexGrid.DataSource` に設定するだけでカラムが表示されます。グリッド上の編集は `DataRow` に反映され、`RowState` も自動で更新されるので、**前回のような `UpdateCell` でViewModelに値を写す配線は不要**になります。グリッド↔DataTableが直結するためです。

```vb
' UserControl
Private Sub SetupGrid()
    grid.DataSource = _viewModel.Table        ' これだけでカラム表示＋編集追跡
    grid.AllowEditing = True
End Sub
```

ただし編集確定のタイミングだけ注意。グリッドのセル編集が `DataTable` に反映されるのは編集確定後です。保存前に編集中のセルを確定させるため、保存処理の冒頭でフォーカスを外すかコミットを促します。

```vb
Private Sub SaveButton_Click(sender As Object, e As EventArgs) Handles saveButton.Click
    grid.FinishEditing()        ' 編集中セルを確定（未確定値の取りこぼし防止）
    _viewModel.Save()
End Sub
```

## 検証はDataRowに対して行う

カラムが違っても、検証は「そのViewModelが自分のカラムを見る」形なので各ViewModelに閉じます。`DataRow.RowError` や `SetColumnError` を使うとC1FlexGridでエラー表示と連動させやすい。

```vb
Public Function Validate() As IReadOnlyList(Of ValidationError) _
        Implements ITableEditorViewModel.Validate
    Dim errors As New List(Of ValidationError)
    For Each row As DataRow In _table.Rows
        If row.RowState = DataRowState.Deleted Then Continue For   ' 削除行は対象外
        If String.IsNullOrWhiteSpace(row.Field(Of String)("NAME")) Then
            row.SetColumnError("NAME", "名称は必須です")
            errors.Add(New ValidationError(row.Field(Of Long)("SEQ"), "NAME", "名称は必須です"))
        Else
            row.SetColumnError("NAME", "")
        End If
    Next
    Return errors
End Function
```

削除行を `Field(Of T)` で読むと例外になるので、`RowState = Deleted` を先に弾くのを忘れないこと。

## IsDirtyと画面遷移ガード

`DataTable.GetChanges()` が `Nothing` でなければ変更ありです。前回の遷移ガードはそのまま使えます。

```vb
Public ReadOnly Property IsDirty As Boolean Implements ITableEditorViewModel.IsDirty
    Get
        Return _table IsNot Nothing AndAlso _table.GetChanges() IsNot Nothing
    End Get
End Property

Public Sub DiscardChanges()
    _table.RejectChanges()      ' 編集を破棄して読込時の状態へ戻す
End Sub
```

`RejectChanges` で破棄が一発でできるのもDataTableの利点です（再取得すら不要）。

## 強い型モデル方式との違い（まとめの前に）

| 観点 | 強い型 RecordModel | DataTable（今回） |
|---|---|---|
| カラム差異 | 型をViewModelごとに作る | スキーマ違いで吸収・型は共通 |
| 変更追跡 | 自前 RowState | DataRow.RowState（自動） |
| グリッド連携 | UpdateCellで写す配線が必要 | DataSource直結で編集が自動反映 |
| 保存 | 自前で振り分け | DataAdapter.Update が振り分け |
| 破棄 | 再取得 | RejectChanges 一発 |
| 型安全 | 高い（コンパイル時） | 低い（文字列カラム名・実行時） |

DataTableは変更追跡・グリッド連携・保存の配線が大幅に減ります。代償は型安全の低下（カラム名を文字列で扱う、値の型変換が実行時）です。カラムが頻繁に変わる前提なら、この交換は妥当です。

## まとめ

- **自前のRowStateは捨て、DataTableの変更追跡（DataRow.RowState / GetChanges / AcceptChanges）を使う**。二重管理しない。
- カラム差異は**型ではなくスキーマの違い**として扱う。共通IF `ITableEditorViewModel` は `DataTable` 型と、カラムに依存しない操作（Load/Validate/Save/IsDirty）だけ持たせる。
- スキーマ定義はRepositoryが担い、`OracleDataAdapter` で取得・保存する。Insert/Update/Deleteコマンドは明示的に書き、`SourceColumn` でDataRow値を流し込む。
- 保存は `GetChanges → DataAdapter.Update → AcceptChanges` をトランザクションで囲む。振り分けは自動。
- C1FlexGridは `DataSource = Table` で直結。編集は自動でDataRowに反映されるので `UpdateCell` 配線は不要。保存前に `FinishEditing` でセル確定。
- 検証は各ViewModelが自分のカラムを `DataRow` に対して行い、`SetColumnError` でグリッド表示と連動。削除行は先に除外。
- IsDirty/破棄は `GetChanges`/`RejectChanges` で簡潔に。

DataTable前提なら、これまで自前で組んでいた変更追跡と保存の振り分けが標準機能で置き換わります。設計の骨格（ViewModelが判断・Repositoryがデータ・Viewが表示、共有状態は購読）は変わりませんが、**RowState管理をDataTableに委ねる**点だけ、強い型方式から明確に切り替えてください。混在させると二重管理になって事故ります。

行ごとに、しかもタブごとに異なる基準でステータスを見て編集可否を決める、という要件ですね。これは前回作った「判定はViewModel、反映はグリッド」の仕組みに、行単位・タブ単位の判定を足す形で収まります。

## 設計の位置づけ

編集可否の判定材料は「その行のステータス列の値」で、判定基準は「タブごとに違う」。判定は業務判断なのでViewModel、反映（編集させない・見た目)はグリッド、という切り分けは変わりません。前回の`ICellStateProvider`がまさにこの窓口なので、それを行単位で答えられるようにします。

```
グリッド: このセル編集していい? → ICellStateProvider に問い合わせ
ViewModel: その行のステータスを見て、自タブの基準で可否を返す（タブごとに基準が違う）
グリッド: 不可なら編集開始させない＋見た目を変える
```

## 1. 判定をViewModelに持たせる

各ViewModelが「自分のタブの基準」で行の編集可否を判断します。基準がタブごとに違うので、これは各ViewModelの実装に閉じ込めます。共通IFは「行が編集可能か」という問いだけを定義します。

```vb
' 共通の窓口：行単位の編集可否を答える
Public Interface IRowEditPolicy
    Function IsRowEditable(rowIndex As Integer) As Boolean
End Interface
```

`ITableEditorViewModel` にこれを含める（または継承させる）と、全タブが自分の基準で実装します。

```vb
' 大タブ：ステータスが「確定」なら編集不可
Public Class LargeEditorViewModel
    Implements ITableEditorViewModel
    Implements IRowEditPolicy

    Public Function IsRowEditable(rowIndex As Integer) As Boolean _
            Implements IRowEditPolicy.IsRowEditable
        Dim status = Convert.ToString(_table.Rows(rowIndex)("STATUS"))
        Return status <> "確定"        ' 大タブの基準
    End Function
End Class

' 中タブ：ステータスが「承認済」or「却下」なら編集不可（基準が違う）
Public Class MediumEditorViewModel
    Implements ITableEditorViewModel
    Implements IRowEditPolicy

    Public Function IsRowEditable(rowIndex As Integer) As Boolean _
            Implements IRowEditPolicy.IsRowEditable
        Dim status = Convert.ToString(_table.Rows(rowIndex)("STATUS"))
        Return status <> "承認済" AndAlso status <> "却下"   ' 中タブの基準
    End Function
End Class
```

タブごとに基準が違っても、グリッド側は「`IsRowEditable`に聞く」だけなので、基準の違いを意識しません。基準はViewModelに散らばらず、各ViewModelの1メソッドに集約されます。

## 2. グリッドの編集可否に反映（BeforeEdit）

グリッドは編集開始直前の`BeforeEdit`で、その行が編集可能かを問い合わせ、不可ならキャンセルします。これがロックやモードとは別の、行単位の制御になります。

```vb
Private _rowPolicy As IRowEditPolicy

Private Sub grid_BeforeEdit(sender As Object, e As RowColEventArgs) Handles grid.BeforeEdit
    If _rowPolicy IsNot Nothing AndAlso Not _rowPolicy.IsRowEditable(e.Row) Then
        e.Cancel = True   ' この行は編集させない
    End If
End Sub
```

`e.Row`はグリッドの行インデックスです。DataTableをそのままバインドしていれば行の並びは対応しますが、ソートやフィルタをかけている場合は行インデックスとDataRowがずれるので、後述の注意を参照してください。

## 3. 見た目に反映（編集不可の行を灰色に）

編集できない行は見た目でも分かるようにします。前回のOwnerDraw（`ICellStateProvider`）と統合できます。行が編集不可なら背景色を変える。

```vb
Protected Overrides Sub OnOwnerDrawCell(e As OwnerDrawCellEventArgs)
    If _rowPolicy IsNot Nothing AndAlso e.Row >= FixedRows Then
        If Not _rowPolicy.IsRowEditable(e.Row) Then
            e.Style.BackColor = Color.FromArgb(240, 240, 240)   ' 編集不可の見た目
        End If
    End If
    MyBase.OnOwnerDrawCell(e)
End Sub
```

これで「編集不可の行は灰色＋編集開始できない」が揃います。

## 4. ロック・モードとの合成

編集可否には複数の要因が重なります。整理すると、あるセルが編集できるのは以下の全部を満たすときです。

```
編集可能 = 排他ロックを自分が保持(Not IsLocked)
        ∧ モードが編集を許す（Editモード等）
        ∧ 列が編集可能（ReadOnly列でない）
        ∧ 行が編集可能（今回のステータス判定）
```

`BeforeEdit`でこれらをまとめて判定すると、判定の一元化になります。散らばると「なぜか編集できる/できない」の原因が追いにくくなるので、1箇所に集めます。

```vb
Private Sub grid_BeforeEdit(sender As Object, e As RowColEventArgs) Handles grid.BeforeEdit
    If Not CanEditCell(e.Row, e.Col) Then e.Cancel = True
End Sub

Private Function CanEditCell(row As Integer, col As Integer) As Boolean
    If _lockState.Info.IsLocked Then Return False              ' 排他ロック
    If Not _modeState.Mode = EditorMode.Edit Then Return False ' モード（例：編集モード以外は不可）
    If Not grid.Cols(col).AllowEditing Then Return False       ' 列単位
    If _rowPolicy IsNot Nothing AndAlso _
       Not _rowPolicy.IsRowEditable(row) Then Return False     ' 行単位（今回）
    Return True
End Function
```

見た目側（OwnerDraw）も同じ`CanEditCell`を使えば、「編集できないのに白いまま」といった表示と挙動の食い違いを防げます。

## 5. 注意点

### ソート・フィルタ時の行インデックス

グリッドでソートやフィルタをかけていると、`e.Row`（表示上の行）とDataTableの行が一致しません。ステータスを引くときは、表示行から正しいDataRowを取る必要があります。C1FlexGridのバインド時は、行に紐づくDataRowView経由で取るのが安全です。

```vb
' 表示行から実際のDataRowを取得（バインド時）
Dim drv = TryCast(grid.Rows(rowIndex).DataSource, DataRowView)
Dim status = If(drv IsNot Nothing, Convert.ToString(drv("STATUS")), Nothing)
```

業務編集画面ではソートを切っていること（`AllowSorting = None`）も多く、その場合は行インデックス直読みで問題ありません。ソートを許すなら上記の変換を挟んでください。

### ステータス変更時の即時反映

同じ画面でステータス列自体を編集して、その結果で行の可否が変わる場合、`AfterEdit`で再描画を促すと見た目が即追従します。

```vb
Private Sub grid_AfterEdit(sender As Object, e As RowColEventArgs) Handles grid.AfterEdit
    If grid.Cols(e.Col).Name = "STATUS" Then grid.Invalidate()   ' 行の色を塗り直す
End Sub
```

### 編集不可行のセル移動

編集不可でも、セルの選択・閲覧・コピーは許すのが普通です。`BeforeEdit`のキャンセルは「編集開始」を止めるだけで、選択やスクロールは妨げません。全面的に触らせたくない行があるなら別途制御しますが、通常は編集開始の抑止で十分です。

## まとめ

- 行単位の編集可否は「判定はViewModel・反映はグリッド」の枠にそのまま乗る。窓口を`IRowEditPolicy.IsRowEditable(row)`として行単位に用意する。
- **タブごとに基準が違う点は、各ViewModelが自分の`IsRowEditable`を実装して吸収**する。グリッドは基準を知らず「聞くだけ」。基準は各ViewModelの1メソッドに集約される。
- グリッドは`BeforeEdit`で問い合わせて`e.Cancel`、`OnOwnerDrawCell`で見た目を変える。両方が同じ判定を使うよう`CanEditCell`に一元化する。
- 編集可否は排他ロック・モード・列単位・行単位の**AND**。`BeforeEdit`の1箇所に集めると原因が追いやすい。
- ソート/フィルタ時は`e.Row`とDataRowがずれるので、DataRowView経由でステータスを引く。ステータス自体を編集する画面なら`AfterEdit`で`Invalidate`して即反映。

タブごとの基準の違いは、`IsRowEditable`を各ViewModelに実装させることで自然に分かれます。グリッドと見た目のコードは共通のまま、判定だけがタブごとに差し替わる——これまでの「共通の器に、タブ固有の判断を注入する」構図と同じです。
