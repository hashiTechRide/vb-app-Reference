方針が変わったので、非連結前提で組み直す設計を出します。他の作業者に合わせる（実装様式の統一）、フィルタ時のスタイル・編集可否をシンプルにしたい、という2点は非連結の得意分野なので、方向として妥当です。作り直す時間があるなら、非連結のほうがこれらの悩みは素直に消えます。

前提が変わったので、これまでバインド前提だった部分（変更追跡・保存・IsDirty・破棄）を非連結用に再設計します。

## 非連結にすると何が変わるか（設計の起点）

バインドで無料だった「変更追跡」が消えるので、そこを自前で持ちます。逆に「グリッドが値を持つ」ので、表示・スタイル・編集可否はグリッド側で完結し、フィルタとの相性問題（行ずれ）が消えます。ここが今回の狙いに直結します。

```
バインド時: DataTableが真実 → グリッドは映すだけ、変更追跡は自動、但しフィルタで行ずれ
非連結時  : グリッドが値を持つ → 変更追跡は自前、但し表示・スタイル・編集可否はグリッドで完結
```

## 1. データの持ち方：行にモデルを紐づける

非連結でも、セルの値だけを`grid[row, col]`でバラバラに管理すると保存や検証が地獄になります。**各行に1つのモデルオブジェクトを紐づけて、モデルを真実にする**のがコツです。`grid.Rows(row).UserData`にモデルを持たせます。

```vb
Public Class RowItem
    Public Property Seq As Long
    Public Property RowState As RowEditState = RowEditState.Unchanged
    Public ReadOnly Values As New Dictionary(Of String, Object)  ' 列名→値

    Public Sub SetValue(col As String, v As Object)
        If Not Equals(Values.GetValueOrDefault(col), v) Then
            Values(col) = v
            If RowState = RowEditState.Unchanged Then RowState = RowEditState.Modified
        End If
    End Sub
End Class

Public Enum RowEditState
    Unchanged
    Added
    Modified
    Deleted
End Enum
```

行に紐づける。

```vb
Private Sub AddRow(item As RowItem)
    Dim r = grid.Rows.Add()
    grid.Rows(r).UserData = item          ' 行にモデルを持たせる
    For Each col In _columns
        grid(r, col.Name) = item.Values.GetValueOrDefault(col.Name)
    Next
End Sub
```

これで「セルの見た目はグリッド、行の実体はモデル」を両立できます。変更追跡はモデルの`RowState`で持つので、バインドの`GetChanges`相当を自前で持てます。

## 2. セル編集をモデルに反映（AfterEdit）

グリッドのセルが編集されたら、その行のモデルに書き戻して`RowState`を更新します。ここがバインドなら自動だった部分の代替です。

```vb
Private Sub grid_AfterEdit(sender As Object, e As RowColEventArgs) Handles grid.AfterEdit
    Dim item = TryCast(grid.Rows(e.Row).UserData, RowItem)
    If item Is Nothing Then Return
    Dim colName = grid.Cols(e.Col).Name
    item.SetValue(colName, grid(e.Row, e.Col))   ' モデルへ反映＋Modified印
End Sub
```

## 3. フィルタ時のスタイル・編集可否（今回の主目的）

ここが非連結の恩恵です。非連結だと、表示している行が「今見えている行」そのものなので、フィルタしてもモデルは行に紐づいたまま。行ずれの変換（前回`DataRowView`経由で引いていた面倒）が要りません。`OwnerDraw`と`BeforeEdit`が、見えている行のモデルを直接見ればよい。

```vb
' 見た目：行モデルのステータスで色を変える（フィルタしても行に紐づくモデルは正しい）
Protected Overrides Sub OnOwnerDrawCell(e As OwnerDrawCellEventArgs)
    Dim item = TryCast(Rows(e.Row).UserData, RowItem)
    If item IsNot Nothing AndAlso e.Row >= FixedRows Then
        If Not _rowPolicy.IsRowEditable(item) Then
            e.Style.BackColor = Color.FromArgb(240, 240, 240)
        End If
    End If
    MyBase.OnOwnerDrawCell(e)
End Sub

' 編集可否：同じくモデルを直接見る
Private Sub grid_BeforeEdit(sender As Object, e As RowColEventArgs) Handles grid.BeforeEdit
    Dim item = TryCast(grid.Rows(e.Row).UserData, RowItem)
    If item Is Nothing OrElse Not CanEditCell(item, e.Col) Then e.Cancel = True
End Sub
```

編集可否判定も、行インデックスでなく**モデルを引数に**します。フィルタで行番号が変わってもモデルは同じなので、判定が安定します。これが「フィルタ時の編集可否が面倒」を解消する核心です。

```vb
Private Function CanEditCell(item As RowItem, col As Integer) As Boolean
    If _lockState.Info.IsLocked Then Return False
    If _modeState.Mode <> EditorMode.Edit Then Return False
    If Not grid.Cols(col).AllowEditing Then Return False
    If Not _rowPolicy.IsRowEditable(item) Then Return False   ' モデルのステータスで判定
    Return True
End Function
```

行単位編集可否のIFも、行番号でなくモデルを受け取る形に変えます。

```vb
Public Interface IRowEditPolicy
    Function IsRowEditable(item As RowItem) As Boolean
End Interface
```

### フィルタ自体の実装

非連結のフィルタは、行を消すのでなく`grid.Rows(i).Visible = False`で隠すのが定石です。モデルは行に残るので、フィルタ解除で元に戻り、変更追跡も保たれます。

```vb
Private Sub ApplyFilter(predicate As Func(Of RowItem, Boolean))
    For i = grid.Rows.Fixed To grid.Rows.Count - 1
        Dim item = TryCast(grid.Rows(i).UserData, RowItem)
        grid.Rows(i).Visible = (item IsNot Nothing AndAlso predicate(item))
    Next
End Sub
```

隠すだけなので、フィルタ中に編集・保存しても隠れた行のモデルは無事です。バインド＋フィルタで悩んだ行ずれが、そもそも発生しません。

## 4. 表示と内部値の分離（DataMap相当を自前で）

非連結ではDataMapに頼らず、**セルにはコードを持ちつつ表示は名称**を自前で制御できます。やり方は2つ。

**方法A：セルには名称を表示し、コードはモデルに持つ。** セル表示は名称、実体（コード）は`RowItem.Values`に持つ。保存はモデルのコードを使う。表示とデータが完全に分離し、DataMapのキー不整合問題が起きません。

```vb
' セルには名称を出す
grid(r, "CLIENT") = clientName
' コードはモデルに
item.Values("CLIENT_CODE") = clientCode
```

**方法B：DataMapを非連結でも使う。** 非連結でも列の`DataMap`は使えるので、セルにコードを入れて名称表示、も可能。ただし前回のFormat競合を避ける運用は引き継ぐ。

非連結にするなら方法Aが素直です。表示は名称、コードはモデル、と役割が明確で、DataMap追記運用の煩雑さから解放されます。取引先選択も、選択結果の名称をセルに、コードをモデルに入れるだけ。

```vb
Private Sub grid_CellButtonClick(sender As Object, e As RowColEventArgs) Handles grid.CellButtonClick
    Dim item = TryCast(grid.Rows(e.Row).UserData, RowItem)
    Using dlg As New ClientSelectForm()
        If dlg.ShowDialog() = DialogResult.OK Then
            grid(e.Row, e.Col) = dlg.SelectedName        ' 表示は名称
            item.Values("CLIENT_CODE") = dlg.SelectedCode ' 実体はコード
            item.RowState = RowEditState.Modified
        End If
    End Using
End Sub
```

DataMap方針からの方針転換になりますが、非連結ではこちらが自然です。前回のDataMap追記・初期補完・Format競合の一連の面倒がまるごと消えます。

## 5. 保存：モデルを集めて振り分け

バインドの`GetChanges`が使えないので、行を走査してモデルの`RowState`で集めます。

```vb
Public Function CollectChanges() As IList(Of RowItem)
    Dim changes As New List(Of RowItem)
    For i = grid.Rows.Fixed To grid.Rows.Count - 1
        Dim item = TryCast(grid.Rows(i).UserData, RowItem)
        If item IsNot Nothing AndAlso item.RowState <> RowEditState.Unchanged Then
            changes.Add(item)
        End If
    Next
    changes.AddRange(_deletedItems)   ' 削除は別退避（下記）
    Return changes
End Function
```

削除は行を消すとモデルも消えるので、既存行（Seqあり）は退避します。

```vb
Private ReadOnly _deletedItems As New List(Of RowItem)

Private Sub DeleteRow(rowIndex As Integer)
    Dim item = TryCast(grid.Rows(rowIndex).UserData, RowItem)
    grid.Rows.Remove(rowIndex)
    If item IsNot Nothing AndAlso item.Seq <> 0 Then
        item.RowState = RowEditState.Deleted
        _deletedItems.Add(item)
    End If
End Sub
```

Repositoryは前と同じくトランザクションで振り分け。渡すのがDataTableでなくモデルのリストになるだけです。

```vb
Public Sub Save(items As IEnumerable(Of RowItem))
    Using conn = _db.OpenConnection()
        Using tx = conn.BeginTransaction()
            Try
                For Each it In items
                    Select Case it.RowState
                        Case RowEditState.Added    : ExecInsert(conn, tx, it)
                        Case RowEditState.Modified : ExecUpdate(conn, tx, it)
                        Case RowEditState.Deleted  : ExecDelete(conn, tx, it)
                    End Select
                Next
                tx.Commit()
            Catch
                tx.Rollback()
                Throw
            End Try
        End Using
    End Using
End Sub
```

保存成功後は`RowState`をUnchangedに戻し、`_deletedItems`をクリアします（バインドの`AcceptChanges`相当を自前で）。

## 6. IsDirty・破棄も自前

```vb
Public ReadOnly Property IsDirty As Boolean
    Get
        If _deletedItems.Count > 0 Then Return True
        For i = grid.Rows.Fixed To grid.Rows.Count - 1
            Dim item = TryCast(grid.Rows(i).UserData, RowItem)
            If item IsNot Nothing AndAlso item.RowState <> RowEditState.Unchanged Then Return True
        Next
        Return False
    End Get
End Property

Public Sub DiscardChanges()
    Reload()   ' 破棄はグリッドを作り直して読み直す（RejectChanges相当）
End Sub
```

破棄は`RejectChanges`一発が使えないので、読み直しで戻します。

## 7. これまでの資産で残るもの・変わるもの

作り直すにあたり、何がそのままで何が変わるか整理します。

**そのまま残る**
- 共有状態（`DbLockState` / `EditorModeState` / `SelectionState`）と購読の仕組み。
- モードの状態機械、遷移固定、再ロック失敗時の読み取り専用化。
- ロックサービス、Repository、Composition Root の構造。
- `ColumnDefinition`による列の種類ごとの振る舞い（`ApplyToColumn`は非連結用に調整）。
- 親子連鎖の再読込（`SelectionState`購読）。

**非連結用に変わる**
- 変更追跡：DataTable自動 → `RowItem.RowState`を自前管理。
- グリッドへのデータ投入：`DataSource` → `Rows.Add`＋`UserData`。
- 編集可否・スタイル判定：行インデックス＋DataRowView → 行の`UserData`モデル直参照（フィルタ耐性が上がる）。
- 表示と内部値の分離：DataMap → セル名称＋モデルにコード（方法A）。
- 保存収集：`GetChanges` → 行走査＋削除退避。
- IsDirty・破棄：`GetChanges`/`RejectChanges` → 自前走査／読み直し。

設計の骨格（判断はViewModel・共有状態は購読・保存はRepositoryでトランザクション）は変わりません。データの保持がDataTableからグリッド＋行モデルに移るだけです。

## 8. 他の作業者に合わせる観点

様式を統一するなら、行モデルの持たせ方（`UserData`に何を入れるか）、変更追跡の型（`RowState`の定義）、保存収集の仕方を、他の人の実装と揃えるのが重要です。ここがバラバラだと、非連結の一番の弱点（自前管理が各自流になる）が出ます。可能なら、`RowItem`と収集・保存のヘルパを共通部品にして全員がそれを使う形にすると、実装がそろい、レビューもしやすくなります。

## まとめ

- 非連結化は、**フィルタ時のスタイル・編集可否の面倒を解消し、他作業者と様式を揃える**という今回の目的に合致する。作り直す時間があるなら妥当な判断。
- 肝は**各行にモデル（`RowItem`）を`UserData`で紐づけ、モデルを真実にする**こと。変更追跡（`RowState`）を自前で持ち、バインドの`GetChanges`/`AcceptChanges`相当を手作りする。
- **編集可否・スタイル判定は行モデルを直接参照**する形にすると、フィルタで行番号が変わっても判定が安定する（前回の行ずれ・DataRowView変換が不要になる）。フィルタは`Rows.Visible`で隠す方式。
- 表示と内部値の分離は、DataMapをやめて**セルに名称・モデルにコード**（方法A）が非連結では素直。DataMap追記・初期補完・Format競合の面倒が消える。
- 保存はモデルを走査して`RowState`で振り分け、削除は退避。Repositoryのトランザクション振り分けは踏襲。IsDirtyは走査、破棄は読み直し。
- **共有状態・モード・ロック・Repository・列定義・親子連鎖の骨格はそのまま残る**。変わるのはデータ保持と変更追跡の部分だけ。
- 様式統一のため、`RowItem`と保存収集ヘルパを共通部品化して全員で使うと、非連結の弱点（自前管理の属人化）を抑えられる。

非連結は「表示・スタイル・編集可否をグリッドで完結させたい」という今回の動機にはよく合います。代償の変更追跡は`RowItem`＋`RowState`で肩代わりできるので、そこを共通部品として固めれば、作り直しの労力に見合う見通しの良さが得られます。まず`RowItem`と収集・保存ヘルパの共通化から着手して、他の人の実装と型を揃えるところを最初に決めるのがよいと思います。
