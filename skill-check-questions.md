# スキルチェック問題集

> **対応**: React / Next.js / TypeScript / Tailwind CSS 学習計画（拡張版）  
> **使い方**:  
> - 📝 **知識問題** … 概念を理解しているか確認（口頭で答えられればOK）  
> - 💻 **コード問題** … 何も見ずにコードを書けるか確認（エディタで実際に書く）  
> - 🏋️ **実装チャレンジ** … 応用力を試す小課題（時間に余裕があれば取り組む）  
> - ✅ 各問題のチェックボックスで正答/未正答を記録する  
>  
> 各日の学習が終わった後にその日の問題を解き、金曜夜に週末総合チェックを実施する。

---

## Week 1（2/16〜2/22）：TypeScript 基礎

### 2/16（月）環境構築 + TS入門

**📝 知識問題**

- [ ] Q1: `tsc` コマンドは何をするコマンドか？
- [ ] Q2: `tsconfig.json` の役割を説明せよ
- [ ] Q3: TypeScriptのコードはブラウザやNode.jsで直接実行できるか？理由も含めて答えよ

**💻 コード問題**

- [ ] Q4: 以下をTypeScriptで書け。「Hello, TypeScript!」とコンソールに出力するプログラム。ただし、メッセージを引数で受け取る関数として実装すること

**🏋️ 実装チャレンジ**

- [ ] Q5: `tsconfig.json` の `strict` オプションをtrue/falseで切り替えて、同じコードのコンパイル結果がどう変わるか確認し、違いを説明せよ

---

### 2/17（火）基本の型

**📝 知識問題**

- [ ] Q1: `any` と `unknown` の違いを説明せよ
- [ ] Q2: `null` と `undefined` の違いを説明せよ。VB.NETの `Nothing` はどちらに近いか？
- [ ] Q3: `tuple` とは何か？通常の配列との違いを説明せよ

**💻 コード問題**

- [ ] Q4: 以下の変数を適切な型注釈つきで宣言せよ
  - 名前（文字列）
  - 年齢（数値）
  - 既婚かどうか（真偽値）
  - 趣味の一覧（文字列の配列）
  - 座標（数値2つのタプル）

- [ ] Q5: `enum Direction { Up, Down, Left, Right }` を定義し、`Up` の値が何になるか答えよ

**🏋️ 実装チャレンジ**

- [ ] Q6: VB.NETの `Integer`, `String`, `Boolean`, `Double`, `Date` それぞれに対応するTypeScriptの型を書き出し、対応表を作成せよ

---

### 2/18（水）関数の型付け

**📝 知識問題**

- [ ] Q1: アロー関数と `function` キーワードの関数宣言の違いを1つ以上挙げよ
- [ ] Q2: オプション引数を示す記法は何か？
- [ ] Q3: 戻り値が無い関数の戻り値型は何と書くか？VB.NETで対応するものは何か？

**💻 コード問題**

- [ ] Q4: 2つの数値を受け取り、合計を返すアロー関数を型注釈つきで書け
- [ ] Q5: 名前（必須）と年齢（オプション）を受け取り、挨拶文を返す関数を書け。年齢がない場合は「年齢不明」と表示

**🏋️ 実装チャレンジ**

- [ ] Q6: 配列を受け取り、各要素に対して渡されたコールバック関数を適用する汎用的な関数 `mapArray` を書け。（`Array.prototype.map` を自力で再実装するイメージ）

---

### 2/19（木）interface と type

**📝 知識問題**

- [ ] Q1: `interface` と `type` の主な違いを2つ挙げよ
- [ ] Q2: `interface` の `extends` と `type` のインターセクション（`&`）はどちらもオブジェクト型の拡張に使えるが、どう使い分けるか？
- [ ] Q3: VB.NETの `Class` とTypeScriptの `interface` の最大の違いは何か？

**💻 コード問題**

- [ ] Q4: 以下の構造を `interface` で定義せよ
  ```
  ユーザー: { id: 数値, 名前: 文字列, メール: 文字列 }
  管理者ユーザー: ユーザーを拡張し、権限レベル: 数値 を追加
  ```
- [ ] Q5: 同じ構造を `type` とインターセクション（`&`）で書き直せ

**🏋️ 実装チャレンジ**

- [ ] Q6: 以下のネストされた型を定義せよ。会社（Company）は名前と従業員リストを持ち、各従業員（Employee）は名前・部署・スキル（文字列配列）を持つ

---

### 2/20（金）ユニオン型・リテラル型

**📝 知識問題**

- [ ] Q1: ユニオン型とは何か？具体例を挙げて説明せよ
- [ ] Q2: リテラル型とは何か？どんな場面で役に立つか？
- [ ] Q3: 型の絞り込み（type narrowing）とは何か？`typeof` を使う例を説明せよ

**💻 コード問題**

- [ ] Q4: APIのレスポンス状態を表す型を定義せよ。状態は `"loading"`, `"success"`, `"error"` の3つのリテラル型のユニオンとする
- [ ] Q5: `string | number` 型の引数を受け取り、文字列なら大文字に、数値なら2倍にして返す関数を書け（型ガード使用）

**🏋️ 実装チャレンジ**

- [ ] Q6: 以下のDiscriminated Union（判別可能なユニオン型）を定義し、`switch` 文で処理する関数を書け
  ```
  type Shape = Circle（radius） | Rectangle（width, height） | Triangle（base, height）
  → 面積を計算して返す
  ```

---

### 2/21（土）TS実践 - CLIツール

**🏋️ 実装チャレンジ（土日はチャレンジ中心）**

- [ ] Q1: CLIツールのエントリポイントで、コマンドライン引数を `process.argv` から取得し、引数の内容によって処理を分岐させるコードを書け
- [ ] Q2: JSONファイルの読み書き（`fs.readFileSync` / `fs.writeFileSync`）を型安全に行う関数を書け。読み込んだJSONをジェネリック型 `T` としてパースすること
- [ ] Q3: 作成したCLIツールのデータ構造について、「なぜこの型定義にしたか」を説明できるか？

---

### 2/22（日）CSS基礎の補強

**📝 知識問題**

- [ ] Q1: CSSのボックスモデルを構成する4つの要素を内側から順に挙げよ
- [ ] Q2: `display: flex` を親要素に指定したとき、子要素はデフォルトでどの方向に並ぶか？
- [ ] Q3: `position: absolute` と `position: relative` の違いを説明せよ

**💻 コード問題**

- [ ] Q4: Flexboxだけを使って、3つのボックスを横並びに中央揃えで配置するCSSを書け
- [ ] Q5: CSS Gridで2列3行のレイアウトを作り、最初の要素を2列分の幅にするCSSを書け

**🏋️ 実装チャレンジ**

- [ ] Q6: ヘッダー（上部固定）、サイドバー（左固定）、メインコンテンツ（残りの領域）の3ペインレイアウトをCSS Gridで作成せよ

---

### ⭐ Week 1 総合チェック（金曜夜 or 週末に実施）

> 何も見ずに解けたら ✅、調べながらなら 🔶、解けなかったら ❌ を記入

| # | 問題 | 結果 |
|---|------|------|
| 1 | `const`, `let`, `var` の違いを説明し、TypeScriptではどれを推奨するか答えよ | |
| 2 | 以下のコードのエラーを修正せよ: `const x: string = 123;` | |
| 3 | `interface User { name: string; age?: number }` の `?` の意味を説明せよ | |
| 4 | `type Status = "active" \| "inactive"` 型の変数に `"pending"` を代入するとどうなるか？ | |
| 5 | 何も見ずに、引数の配列から偶数だけをフィルタして返すアロー関数を型つきで書け | |
| 6 | `unknown` 型の値のプロパティにアクセスするにはどうすればよいか？ | |
| 7 | VB.NETの `Function Add(a As Integer, b As Integer) As Integer` をTypeScriptで書き直せ | |

---

## Week 2（2/23〜3/1）：TypeScript 応用

### 2/23（月）ジェネリクス

**📝 知識問題**

- [ ] Q1: ジェネリクスとは何か？なぜ必要か？`any` との違いを説明せよ
- [ ] Q2: `<T extends string>` の `extends` は何を意味するか？
- [ ] Q3: VB.NETの `Of T` とTypeScriptのジェネリクス `<T>` の共通点を挙げよ

**💻 コード問題**

- [ ] Q4: 任意の型の配列を受け取り、最初の要素を返すジェネリック関数 `getFirst<T>` を書け
- [ ] Q5: 以下のジェネリック型を定義せよ
  ```typescript
  ApiResponse<T> = { data: T; status: number; message: string }
  ```
  これを使って `ApiResponse<User[]>` 型の変数を宣言せよ

**🏋️ 実装チャレンジ**

- [ ] Q6: キーと値の型を両方ジェネリックにしたシンプルなKeyValueStore クラス（`set`, `get`, `has` メソッド持ち）を実装せよ

---

### 2/24（火）型ユーティリティ

**📝 知識問題**

- [ ] Q1: `Partial<T>` は何をする型か？具体的なユースケースを挙げよ
- [ ] Q2: `Pick<T, K>` と `Omit<T, K>` の違いを説明せよ
- [ ] Q3: `Readonly<T>` を適用した型のプロパティを変更するとどうなるか？

**💻 コード問題**

- [ ] Q4: `User` 型（id, name, email, password）から `Omit` で password を除いた `PublicUser` 型を作れ
- [ ] Q5: `User` 型から `Pick` で name と email だけの `UserSummary` 型を作れ
- [ ] Q6: `Record<string, number>` はどんな型か？例を書いて説明せよ

**🏋️ 実装チャレンジ**

- [ ] Q7: フォームの更新関数を想定して、`Partial<User>` を引数に取り、既存の `User` オブジェクトを更新して返す `updateUser` 関数を書け

---

### 2/25（水）モジュールとES構文

**📝 知識問題**

- [ ] Q1: 名前付きエクスポート（`export { ... }`）とデフォルトエクスポート（`export default`）の違いを説明せよ
- [ ] Q2: 分割代入（destructuring）とは何か？オブジェクトと配列それぞれの例を挙げよ
- [ ] Q3: スプレッド構文（`...`）の用途を3つ挙げよ

**💻 コード問題**

- [ ] Q4: 以下をスプレッド構文で書け: オブジェクト `user` の全プロパティをコピーしつつ、`name` だけ上書き
- [ ] Q5: Optional Chaining（`?.`）と Nullish Coalescing（`??`）を使って、`user?.address?.city ?? "不明"` の意味を説明し、同等の処理をif文で書け
- [ ] Q6: テンプレートリテラルを使って、`name` と `age` 変数を埋め込んだ自己紹介文を作れ

---

### 2/26（木）非同期処理

**📝 知識問題**

- [ ] Q1: `Promise` の3つの状態を挙げよ
- [ ] Q2: `async / await` は何の糖衣構文（シンタックスシュガー）か？
- [ ] Q3: `try / catch` で非同期エラーをハンドリングするとき、`await` を忘れるとどうなるか？

**💻 コード問題**

- [ ] Q4: `fetch` で `https://jsonplaceholder.typicode.com/users/1` からデータを取得し、名前を表示する `async` 関数を書け（型付きで）
- [ ] Q5: `Promise.all` を使って3つのAPIを並列に呼び出すコードを書け

**🏋️ 実装チャレンジ**

- [ ] Q6: 指定ミリ秒待つ `sleep` 関数（`Promise<void>` を返す）を実装し、使用例を書け

---

### 2/27（金）TS総合演習

**💻 コード問題**

- [ ] Q1: 何も見ずに、以下のTODOアプリのデータ型を定義せよ
  - Todo型: id（数値）、title（文字列）、completed（真偽値）、createdAt（Date）
  - Filter型: "all" | "active" | "completed"

- [ ] Q2: 何も見ずに、Todo配列から完了済みのものだけを抽出する関数を型つきで書け
- [ ] Q3: 何も見ずに、Todo配列に新しいTodoを追加する関数を書け（元の配列は変更しない=イミュータブル）

**🏋️ 実装チャレンジ**

- [ ] Q4: TODOアプリのロジック全体（追加、削除、完了切替、フィルタ）を何も見ずに書き直して、Week1に比べてどれだけスムーズに書けるようになったか振り返れ

---

### 2/28（土）Git + GitHub 実践

**📝 知識問題**

- [ ] Q1: `git merge` と `git rebase` の違いを説明せよ
- [ ] Q2: GitHubフロー（GitHub Flow）の手順を順番に説明せよ
- [ ] Q3: コンフリクト（競合）が発生する条件を説明せよ

**💻 コード問題**

- [ ] Q4: 新しいfeatureブランチを作成→ファイルを変更→コミット→mainにマージ、の一連のコマンドを書け
- [ ] Q5: 意図的にコンフリクトを起こし、手動で解決する手順を実演せよ

---

### 3/1（日）REST API 基礎

**📝 知識問題**

- [ ] Q1: HTTP メソッドの GET / POST / PUT / PATCH / DELETE はそれぞれ何の操作に対応するか？
- [ ] Q2: ステータスコード 200, 201, 400, 401, 404, 500 の意味を答えよ
- [ ] Q3: リクエストボディとクエリパラメータの違いを説明せよ

**💻 コード問題**

- [ ] Q4: `fetch` を使って POST リクエスト（JSON ボディ付き）を送信するコードを書け
- [ ] Q5: レスポンスのステータスコードをチェックし、200以外ならエラーをthrowする共通ラッパー関数を書け

---

### ⭐ Week 2 総合チェック

| # | 問題 | 結果 |
|---|------|------|
| 1 | `Array<string>` と `string[]` の違いはあるか？ | |
| 2 | `Partial<{ name: string; age: number }>` の結果の型を書け | |
| 3 | `const [a, ...rest] = [1, 2, 3, 4]` で `a` と `rest` の値は？ | |
| 4 | `async` 関数は必ず何を返すか？ | |
| 5 | ジェネリック関数 `identity<T>(arg: T): T` を書き、`identity<string>("hello")` の戻り値型を答えよ | |
| 6 | `Pick<User, "name" \| "email">` と `Omit<User, "id" \| "password">` が同じ結果になるUser型を定義せよ | |
| 7 | `Promise.all` と `Promise.race` の違いを説明せよ | |

---

## Week 3（3/2〜3/8）：React 基礎

### 3/2（月）React環境構築 + JSX

**📝 知識問題**

- [ ] Q1: JSXとは何か？なぜHTMLではないのか？
- [ ] Q2: JSX内でJavaScript式を埋め込むには何で囲むか？
- [ ] Q3: JSXで `class` 属性の代わりに何を使うか？その理由は？

**💻 コード問題**

- [ ] Q4: 名前と肩書きを表示するプロフィールカードコンポーネントをJSXで書け
- [ ] Q5: 変数 `isLoggedIn` がtrueなら「ようこそ」、falseなら「ログインしてください」と表示するJSXを書け（三項演算子と&&の2パターン）

**🏋️ 実装チャレンジ**

- [ ] Q6: 配列 `["React", "TypeScript", "Next.js"]` を `<ul><li>` でリスト表示するJSXを書け。`key` を適切に設定すること

---

### 3/3（火）コンポーネントとProps

**📝 知識問題**

- [ ] Q1: Reactコンポーネントの命名規則は何か？（先頭の文字）
- [ ] Q2: Propsとは何か？WinFormsの何に近いか？
- [ ] Q3: `children` propsとは何か？どんな場面で使うか？

**💻 コード問題**

- [ ] Q4: 以下のPropsを持つ `UserCard` コンポーネントを作れ
  ```typescript
  type UserCardProps = {
    name: string;
    age: number;
    avatarUrl?: string; // オプション
  }
  ```
- [ ] Q5: `children` を受け取って `<div className="card">` で囲む `Card` コンポーネントを書け

**🏋️ 実装チャレンジ**

- [ ] Q6: `Button` コンポーネントを作成せよ。Props: `label`（文字列）, `variant`（"primary" | "secondary" | "danger"）, `onClick`（関数）

---

### 3/4（水）useState

**📝 知識問題**

- [ ] Q1: `useState` の戻り値は何か？（2つの要素）
- [ ] Q2: Reactで「状態が変わるとUIが自動で更新される」仕組みを説明せよ。WinFormsとの違いは？
- [ ] Q3: stateの更新でオブジェクトや配列を直接変更（ミューテート）してはいけない理由は何か？

**💻 コード問題**

- [ ] Q4: カウンターコンポーネントを書け。+1、-1、リセットの3つのボタンを持つ
- [ ] Q5: 文字列の配列をstateで管理し、入力フォームから追加できるコンポーネントを書け（イミュータブルに更新）
- [ ] Q6: オブジェクトのstate（`{ name: string, age: number }`）で、`name` だけを更新するコードを書け（スプレッド構文使用）

---

### 3/5（木）イベント + リスト

**📝 知識問題**

- [ ] Q1: `onClick` ハンドラで引数を渡したいとき、`onClick={handleClick(id)}` ではなく何と書くべきか？その理由は？
- [ ] Q2: リスト表示で `key` が必要な理由を説明せよ
- [ ] Q3: `key` にインデックス（`index`）を使うべきでない場面はどんなときか？

**💻 コード問題**

- [ ] Q4: テキスト入力→追加ボタン→リスト表示の一連の処理を書け。追加後に入力欄をクリアすること
- [ ] Q5: リスト内の各アイテムに「削除」ボタンを付け、クリックでそのアイテムを配列から除去するコードを書け

**🏋️ 実装チャレンジ**

- [ ] Q6: リスト内のアイテムをクリックすると「選択中」のスタイルがトグルされるUIを実装せよ（複数選択可）

---

### 3/6（金）三目並べチュートリアル

**📝 知識問題**

- [ ] Q1: 「状態の持ち上げ（lifting state up）」とは何か？なぜ必要か？
- [ ] Q2: 三目並べで、各マスの状態を親コンポーネントで管理する理由を説明せよ
- [ ] Q3: イミュータブルな状態管理が「履歴機能」の実装に役立つ理由を説明せよ

**💻 コード問題**

- [ ] Q4: 9マスの配列をstateで持ち、クリックで交互に"X"/"O"を配置するロジックを書け（勝敗判定なしでOK）

**🏋️ 実装チャレンジ**

- [ ] Q5: 三目並べに「手番を戻す（Undo）」機能を追加するロジックを書け

---

### 3/7（土）ミニアプリ① じゃんけんアプリ

**🏋️ 実装チャレンジ**

- [ ] Q1: 何も見ずにじゃんけんアプリを30分以内に作れるか？（グー・チョキ・パーの選択→相手のランダム選択→勝敗判定→スコア表示）
- [ ] Q2: 勝敗のロジックを関数として切り出し、TypeScriptの型（リテラル型ユニオン `"rock" | "scissors" | "paper"`）で型安全にせよ
- [ ] Q3: 直近5回の対戦履歴を表示する機能を追加せよ

---

### 3/8（日）ミニアプリ② カラーパレットジェネレーター

**🏋️ 実装チャレンジ**

- [ ] Q1: ランダムな16進カラーコードを生成する関数を書け
- [ ] Q2: 5色のパレットをstateで管理し、「Generate」ボタンでランダムに再生成するコンポーネントを書け
- [ ] Q3: 各色をクリックするとカラーコードがクリップボードにコピーされる機能を実装せよ（`navigator.clipboard.writeText` 使用）

---

### ⭐ Week 3 総合チェック

| # | 問題 | 結果 |
|---|------|------|
| 1 | 「宣言的UI」と「命令的UI」の違いを、WinFormsとReactの具体例を挙げて説明せよ | |
| 2 | `useState(0)` の初期値を `"hello"` に変えると型はどうなるか？ | |
| 3 | 何も見ずに、入力フォーム付きの買い物リストコンポーネントを書け（追加・削除機能付き） | |
| 4 | `<li key={index}>` が問題になるケースを具体的に説明せよ | |
| 5 | 親コンポーネントから子コンポーネントに関数を渡して、子のボタンクリックで親のstateを更新するパターンを書け | |
| 6 | WinFormsでリストに項目を追加する場合（`ListBox.Items.Add()`）とReactでの方法の違いを説明せよ | |

---

## Week 4（3/9〜3/15）：React 応用

### 3/9（月）useEffect

**📝 知識問題**

- [ ] Q1: `useEffect` の依存配列が `[]`（空）のときと、省略したときの違いを説明せよ
- [ ] Q2: クリーンアップ関数はいつ実行されるか？どんな場面で必要か？
- [ ] Q3: 「useEffectは不要かも」に該当するケースを2つ挙げよ

**💻 コード問題**

- [ ] Q4: コンポーネントのマウント時にAPIを呼び出し、結果をstateにセットする `useEffect` を書け。ローディング状態とエラー状態も管理すること
- [ ] Q5: `setInterval` を `useEffect` 内で使い、毎秒カウントアップするタイマーを書け（クリーンアップ関数で `clearInterval` すること）

**🏋️ 実装チャレンジ**

- [ ] Q6: ウィンドウのリサイズイベントを監視して、現在のウィンドウサイズを表示するコンポーネントを書け（クリーンアップ必須）

---

### 3/10（火）フォーム処理

**📝 知識問題**

- [ ] Q1: 「制御コンポーネント（Controlled Component）」とは何か？VB.NETのTextBoxとの違いは？
- [ ] Q2: `onChange` イベントで `e.target.value` を取得するとき、`e` の型は何か？
- [ ] Q3: フォーム送信時にページ遷移を防ぐにはどうするか？

**💻 コード問題**

- [ ] Q4: 名前・メール・パスワードの3つのフィールドを持つフォームを書け。各入力を1つのオブジェクトstateで管理すること
- [ ] Q5: 上のフォームにバリデーションを追加せよ（名前: 必須、メール: @を含む、パスワード: 8文字以上）。エラーメッセージを各フィールドの下に表示

**🏋️ 実装チャレンジ**

- [ ] Q6: 動的にフィールドを追加・削除できるフォーム（例: スキルを複数入力）を実装せよ

---

### 3/11（水）useContext

**📝 知識問題**

- [ ] Q1: `useContext` が解決する問題（props drilling）とは何か？具体例で説明せよ
- [ ] Q2: Context の Provider と Consumer の関係を説明せよ
- [ ] Q3: useContextを使いすぎることのデメリットは何か？

**💻 コード問題**

- [ ] Q4: テーマ（"light" | "dark"）を管理するContextを作成し、Provider, useTheme カスタムフック、切替ボタンを実装せよ
- [ ] Q5: 3階層のコンポーネント（App → Parent → Child）で、ContextなしとContextありでデータを渡す場合のコードを両方書き、違いを確認せよ

---

### 3/12（木）カスタムフック

**📝 知識問題**

- [ ] Q1: カスタムフックの命名規則は何か？
- [ ] Q2: カスタムフック内で他のフックを使えるか？
- [ ] Q3: カスタムフックとただのユーティリティ関数の違いは何か？

**💻 コード問題**

- [ ] Q4: 以下の `useFetch` カスタムフックを書け
  ```typescript
  // 使い方: const { data, loading, error } = useFetch<User[]>("/api/users");
  ```
- [ ] Q5: LocalStorageの値を管理する `useLocalStorage<T>(key, initialValue)` カスタムフックを書け

**🏋️ 実装チャレンジ**

- [ ] Q6: デバウンス機能つきの `useDebounce<T>(value, delay)` フックを実装せよ

---

### 3/13（金）React総合演習

**💻 コード問題（すべて何も見ずに）**

- [ ] Q1: TODOアプリのコンポーネント構成を設計せよ（どのコンポーネントがどのstateを持つか）
- [ ] Q2: TODOの追加機能を実装せよ（フォーム送信→配列に追加）
- [ ] Q3: TODOの完了切替機能を実装せよ（チェックボックスクリック→completedトグル）
- [ ] Q4: フィルタ機能を実装せよ（All / Active / Completed の切替）

**🏋️ 実装チャレンジ**

- [ ] Q5: TODOアプリに「編集（インライン編集）」機能を追加せよ

---

### 3/14（土）React Hook Form + zod

**📝 知識問題**

- [ ] Q1: React Hook Formを使うメリットを2つ挙げよ（素のReactフォーム実装と比較して）
- [ ] Q2: zodとは何か？バリデーションにおける役割を説明せよ

**💻 コード問題**

- [ ] Q3: zodで以下のスキーマを定義せよ: 名前（必須・2文字以上）、メール（必須・メール形式）、年齢（任意・18以上）
- [ ] Q4: 上のzodスキーマを使って、React Hook Form でフォームコンポーネントを書け。エラーメッセージ表示も含む

**🏋️ 実装チャレンジ**

- [ ] Q5: パスワードと確認用パスワードの一致をzodの `refine` で検証するフォームを作れ

---

### 3/15（日）Zustand + TanStack Query

**📝 知識問題**

- [ ] Q1: Zustandが `useContext` + `useState` の組み合わせより優れている点を挙げよ
- [ ] Q2: TanStack Queryの `staleTime` と `gcTime` の違いを説明せよ

**💻 コード問題**

- [ ] Q3: Zustandでカウンターstore（count, increment, decrement, reset）を作成し、コンポーネントから使用するコードを書け
- [ ] Q4: TanStack Queryの `useQuery` でユーザー一覧を取得し、ローディング・エラー・データ表示を切り替えるコンポーネントを書け

**🏋️ 実装チャレンジ**

- [ ] Q5: Zustand + TanStack Queryを組み合わせて、「お気に入りユーザー」をグローバルstateで管理しつつ、ユーザー一覧はサーバーからフェッチするアプリを作れ

---

### ⭐ Week 4 総合チェック

| # | 問題 | 結果 |
|---|------|------|
| 1 | useEffectの依存配列に `[count]` を入れた場合の挙動を説明せよ | |
| 2 | 制御コンポーネントと非制御コンポーネントの違いを説明し、React Hook Formはどちらを使うか答えよ | |
| 3 | 何も見ずに、useFetchカスタムフックを書け | |
| 4 | props drilling を解決する方法を3つ挙げよ | |
| 5 | Zustandのstoreを定義するコードを何も見ずに書け | |
| 6 | TanStack QueryのuseQueryの第1引数（queryKey）の役割を説明せよ | |
| 7 | 何も見ずに、React Hook Form + zod でメールとパスワードのログインフォームを書け | |

---

## Week 5（3/16〜3/22）：Tailwind CSS

### 3/16（月）Tailwind入門 + Flexbox

**📝 知識問題**

- [ ] Q1: 「ユーティリティファースト」とは何か？従来のCSS設計（BEMなど）との違いは？
- [ ] Q2: Tailwindの `p-4` は何pxのパディングか？（Tailwindの基本単位を答えよ）
- [ ] Q3: `flex`, `justify-center`, `items-center` を組み合わせると何が起こるか？

**💻 コード問題**

- [ ] Q4: Tailwindで、青い背景・白い文字・角丸・パディング付きのボタンを作るクラスを書け
- [ ] Q5: 3つのカードを横並びにし、均等な間隔で配置するTailwindクラスを書け

---

### 3/17（火）Grid + レスポンシブ

**📝 知識問題**

- [ ] Q1: Tailwindのレスポンシブプレフィックス `sm:`, `md:`, `lg:` はそれぞれ何px以上を指すか？
- [ ] Q2: Tailwindの「モバイルファースト」とはどういう意味か？

**💻 コード問題**

- [ ] Q3: モバイルで1列、タブレットで2列、デスクトップで3列になるカードグリッドをTailwindで書け
- [ ] Q4: `col-span-2` は何を意味するか？3列グリッドで使った場合の見た目を説明せよ

**🏋️ 実装チャレンジ**

- [ ] Q5: 実在するWebサイトのトップページのレイアウトをTailwindだけで再現せよ（30分以内）

---

### 3/18（水）コンポーネントスタイリング

**📝 知識問題**

- [ ] Q1: Tailwindでホバー時にスタイルを変えるにはどう書くか？
- [ ] Q2: `clsx` や `cn` 関数は何のために使うか？
- [ ] Q3: Tailwindで `transition` と `duration-300` を組み合わせると何が起こるか？

**💻 コード問題**

- [ ] Q4: ホバー時に色が変わり、スムーズにトランジションするボタンをTailwindで書け
- [ ] Q5: `active` プロパティ（boolean）に基づいてスタイルを切り替えるボタンコンポーネントを `clsx` を使って書け

---

### 3/19（木）ダークモード + カスタマイズ

**📝 知識問題**

- [ ] Q1: Tailwindでダークモードを有効にする方法を説明せよ（`class` 戦略と `media` 戦略の違い）
- [ ] Q2: `tailwind.config.js` の `theme.extend` は何のために使うか？

**💻 コード問題**

- [ ] Q3: ライトモードで白背景・黒文字、ダークモードで黒背景・白文字のカードをTailwindで書け
- [ ] Q4: `tailwind.config.js` にカスタムカラー `brand: "#3B82F6"` を追加し、`bg-brand` として使うコードを書け

---

### 3/20（金）TODOアプリ リデザイン

**💻 コード問題**

- [ ] Q1: TODOアプリのフォーム入力欄にTailwindでスタイルを適用せよ（ボーダー、フォーカス時のリング、角丸、パディング）
- [ ] Q2: 完了したTODOに取り消し線と薄い文字色を適用するTailwindクラスを書け
- [ ] Q3: フィルタボタン群（All/Active/Completed）を選択状態に応じてスタイル変更するコードを書け

**🏋️ 実装チャレンジ**

- [ ] Q4: TODOアプリ全体をレスポンシブかつダークモード対応で仕上げよ（30分以内）

---

### 3/21（土）テスト入門

**📝 知識問題**

- [ ] Q1: 「ユニットテスト」と「インテグレーションテスト」の違いを説明せよ
- [ ] Q2: React Testing Libraryの思想（「実装の詳細をテストしない」）とは何か？
- [ ] Q3: `screen.getByRole`, `screen.getByText`, `screen.getByTestId` の使い分けを説明せよ

**💻 コード問題**

- [ ] Q4: カウンターコンポーネント（+1ボタンで数値が増える）のテストを書け（render → ボタンクリック → 値の確認）
- [ ] Q5: 非同期でデータを取得するコンポーネントのテストを書け（`waitFor` 使用）

---

### 3/22（日）ポートフォリオ構想

**📝 知識問題（設計力チェック）**

- [ ] Q1: 作ろうとしているアプリの画面を最低3つ挙げ、それぞれに必要なデータを列挙せよ
- [ ] Q2: データベースのテーブル構成を設計し、リレーションを説明せよ
- [ ] Q3: Server Components と Client Components をどの画面で使い分けるか計画せよ

---

### ⭐ Week 5 総合チェック

| # | 問題 | 結果 |
|---|------|------|
| 1 | `flex justify-between items-center p-4 bg-white shadow-md` が作るレイアウトを説明せよ | |
| 2 | モバイルで非表示、デスクトップで表示する要素のTailwindクラスを書け | |
| 3 | `dark:bg-gray-900 dark:text-white` が機能するための条件を説明せよ | |
| 4 | `hover:scale-105 transition-transform duration-200` の効果を説明せよ | |
| 5 | React Testing Libraryで `fireEvent.click` と `userEvent.click` の違いを説明せよ | |
| 6 | 何も見ずに、Tailwindでレスポンシブなナビゲーションバーを書け | |

---

## Week 6（3/23〜3/29）：Next.js 基礎

### 3/23（月）Next.js概要 + ルーティング

**📝 知識問題**

- [ ] Q1: Next.jsのApp Routerにおいて、`page.tsx`, `layout.tsx`, `loading.tsx`, `error.tsx` それぞれの役割を説明せよ
- [ ] Q2: `/dashboard/settings` のURLに対応するファイル構成を書け
- [ ] Q3: `layout.tsx` はページ遷移時に再レンダリングされるか？

**💻 コード問題**

- [ ] Q4: Home, About, Contact の3ページを持つNext.jsアプリのフォルダ構成を書け
- [ ] Q5: 共通のヘッダーとフッターを含むルートレイアウトを書け

---

### 3/24（火）レイアウト + ナビゲーション

**📝 知識問題**

- [ ] Q1: `<Link>` コンポーネントと `<a>` タグの違いを説明せよ
- [ ] Q2: `usePathname()` は何を返すか？どんな場面で使うか？
- [ ] Q3: ネストされたレイアウトとは何か？具体例を挙げよ

**💻 コード問題**

- [ ] Q4: 現在のパスに応じてナビゲーションのアクティブ項目のスタイルを変えるコンポーネントを書け（`usePathname` 使用）
- [ ] Q5: `/dashboard` 配下に独自のサイドバーレイアウトを持たせるフォルダ構成とコードを書け

---

### 3/25（水）Server vs Client Components

**📝 知識問題**

- [ ] Q1: Server Components のメリットを3つ挙げよ
- [ ] Q2: "use client" ディレクティブが必要になる具体的なケースを3つ挙げよ
- [ ] Q3: Server Component の中で Client Component を使えるか？逆はどうか？

**💻 コード問題**

- [ ] Q4: APIからデータを取得して表示する Server Component を書け
- [ ] Q5: ボタンクリックでカウントアップする Client Component を書け。"use client" を適切に配置すること

**🏋️ 実装チャレンジ**

- [ ] Q6: ユーザー一覧（Server Component）の中に「お気に入り」ボタン（Client Component）を含む構成を実装せよ

---

### 3/26（木）データ取得

**📝 知識問題**

- [ ] Q1: Next.js App RouterでServer Component内の `fetch` はデフォルトでどう動作するか？（キャッシュの観点から）
- [ ] Q2: `loading.tsx` はどのタイミングで表示されるか？Suspense との関係は？
- [ ] Q3: 動的レンダリングと静的レンダリングの違いを説明せよ

**💻 コード問題**

- [ ] Q4: Server Componentで外部APIからユーザー一覧を取得し、表示するページを書け
- [ ] Q5: ローディング中に「読み込み中...」を表示する `loading.tsx` を書け

---

### 3/27（金）動的ルーティング

**📝 知識問題**

- [ ] Q1: `[id]` フォルダの意味と、`params` からIDを取得する方法を説明せよ
- [ ] Q2: `generateStaticParams` は何のために使うか？
- [ ] Q3: `[...slug]` と `[[...slug]]` の違いは何か？

**💻 コード問題**

- [ ] Q4: ブログ記事の詳細ページ（`/posts/[id]`）を実装せよ。パラメータからIDを取得し、APIで記事を取得して表示
- [ ] Q5: `generateStaticParams` を使って、ビルド時に `/posts/1`, `/posts/2`, `/posts/3` を静的に生成するコードを書け

---

### 3/28（土）Supabase入門

**💻 コード問題**

- [ ] Q1: Supabaseクライアントを初期化するコードを書け
- [ ] Q2: Supabaseの `todos` テーブルから全件取得するコードを書け
- [ ] Q3: Supabaseに新しいレコードをINSERTするコードを書け

**🏋️ 実装チャレンジ**

- [ ] Q4: Next.js の Server Component から Supabase のデータを取得し、一覧表示するページを作れ

---

### 3/29（日）Prisma入門

**📝 知識問題**

- [ ] Q1: Prismaの `schema.prisma` ファイルの役割を説明せよ
- [ ] Q2: `npx prisma migrate dev` は何をするか？
- [ ] Q3: PrismaとSupabaseを直接使う場合の違い（ORMの有無の利点）を説明せよ

**💻 コード問題**

- [ ] Q4: Prismaスキーマで User モデル（id, name, email, createdAt）を定義せよ
- [ ] Q5: Prisma Client で全ユーザー取得、1件取得、新規作成、更新、削除のコードを書け

---

### ⭐ Week 6 総合チェック

| # | 問題 | 結果 |
|---|------|------|
| 1 | App Routerのファイル規約（page, layout, loading, error, not-found）を全て説明せよ | |
| 2 | Server Components で useState を使うとどうなるか？ | |
| 3 | 何も見ずに、`/products/[id]` の動的ルートページを書け | |
| 4 | `<Link href="/about">` と `<a href="/about">` の動作の違いを説明せよ | |
| 5 | SSR / SSG / ISR の違いを、それぞれ1行で説明せよ | |
| 6 | Prismaスキーマでリレーション（1対多: User has many Posts）を定義せよ | |

---

## Week 7（3/30〜4/5）：Next.js 応用

### 3/30（月）Server Actions + フォーム

**📝 知識問題**

- [ ] Q1: Server Actions とは何か？API Routes（Route Handlers）との違いは？
- [ ] Q2: `"use server"` ディレクティブはどこに書くか？
- [ ] Q3: `revalidatePath` は何をするか？なぜ必要か？

**💻 コード問題**

- [ ] Q4: フォーム送信でTODOを追加する Server Action を書け（zodバリデーション含む）
- [ ] Q5: Server Action実行後に `revalidatePath("/todos")` でデータを再取得するコードを書け

---

### 3/31（火）Route Handlers (API)

**📝 知識問題**

- [ ] Q1: Route Handlersのファイル名は何か？配置場所は？
- [ ] Q2: Server Actions と Route Handlers の使い分けの基準を説明せよ

**💻 コード問題**

- [ ] Q3: `app/api/users/route.ts` で GET（全件取得）と POST（新規作成）を実装せよ
- [ ] Q4: `app/api/users/[id]/route.ts` で GET（1件取得）、PUT（更新）、DELETE（削除）を実装せよ
- [ ] Q5: リクエストボディのバリデーション（zodで）とエラーレスポンス（400, 404, 500）のハンドリングを追加せよ

---

### 4/1（水）ミドルウェア + 認証基礎

**📝 知識問題**

- [ ] Q1: Next.jsのミドルウェア（`middleware.ts`）はリクエスト処理のどのタイミングで実行されるか？
- [ ] Q2: JWT（JSON Web Token）の構造（3つの部分）を説明せよ
- [ ] Q3: Cookie ベースのセッションと JWT ベースの認証の違いを説明せよ

**💻 コード問題**

- [ ] Q4: 特定のパス（`/dashboard/*`）にアクセスした際、認証トークンがなければ `/login` にリダイレクトする `middleware.ts` を書け
- [ ] Q5: `matcher` でミドルウェアを特定のパスにのみ適用する設定を書け

---

### 4/2（木）エラーハンドリング + メタデータ

**📝 知識問題**

- [ ] Q1: `error.tsx` と `not-found.tsx` の違いを説明せよ
- [ ] Q2: Next.jsの `Metadata` オブジェクトで設定できる主要な項目を5つ挙げよ
- [ ] Q3: `generateMetadata` 関数はどんな場面で使うか？（静的 `metadata` との使い分け）

**💻 コード問題**

- [ ] Q4: `error.tsx` を書け。エラーメッセージの表示と「再試行」ボタン（`reset` 関数を呼ぶ）を含むこと
- [ ] Q5: ブログ記事ページ（`/posts/[id]`）で、記事タイトルを動的にページタイトルに設定する `generateMetadata` を書け

---

### 4/3（金）Next.js総合演習

**💻 コード問題**

- [ ] Q1: ダッシュボードアプリのフォルダ構成を設計せよ（ルーティング、レイアウト、共通コンポーネント）
- [ ] Q2: サイドバーナビゲーション + メインコンテンツのレイアウトを書け
- [ ] Q3: ダッシュボードのトップページに、カード形式でKPI（数値データ）を表示するコンポーネントを書け

---

### 4/4（土）ポートフォリオ開発①

**🏋️ 実装チャレンジ**

- [ ] Q1: Prismaスキーマ → `prisma migrate dev` → シードデータ投入の一連の流れを何も見ずにできるか？
- [ ] Q2: Server Components でデータ取得 → 一覧表示 → 動的ルートで詳細表示の流れを実装できるか？
- [ ] Q3: Server Actions でフォーム送信 → データ作成 → `revalidatePath` の流れを実装できるか？

---

### 4/5（日）ポートフォリオ開発②

**🏋️ 実装チャレンジ**

- [ ] Q1: Tailwindでレスポンシブなレイアウト（モバイル/デスクトップ）を実装できるか？
- [ ] Q2: ローディング状態（`loading.tsx` or Suspense）とエラー状態（`error.tsx`）を適切に実装できるか？
- [ ] Q3: コンポーネントの責務が適切に分離されているか？Server/Client の使い分けは正しいか？

---

### ⭐ Week 7 総合チェック

| # | 問題 | 結果 |
|---|------|------|
| 1 | Server Actions で `"use server"` を忘れるとどうなるか？ | |
| 2 | Route Handler（`route.ts`）で `NextRequest` から JSON ボディを取得するコードを書け | |
| 3 | `middleware.ts` でリダイレクトを行うコードを何も見ずに書け | |
| 4 | `error.tsx` が "use client" を必要とする理由を説明せよ | |
| 5 | `revalidatePath` と `revalidateTag` の違いを説明せよ | |
| 6 | PrismaでCRUD操作（作成・読取・更新・削除）のコードを何も見ずに全て書け | |
| 7 | Server Actions vs Route Handlers — どちらをいつ使うか、判断基準を3つ挙げよ | |

---

## Week 8（4/6〜4/12）：総合演習 + 業務準備

### 4/6（月）総合アプリ構築（1）

**💻 コード問題**

- [ ] Q1: テーブル表示コンポーネントを書け。ヘッダー行とデータ行を Props で受け取る汎用的な設計にすること
- [ ] Q2: テーブルのヘッダーをクリックするとその列でソートする機能を実装せよ
- [ ] Q3: ページネーション（前へ/次へ + ページ番号表示）を実装せよ

---

### 4/7（火）総合アプリ構築（2）

**💻 コード問題**

- [ ] Q1: 編集画面で既存データをフォームに初期値として表示するコードを書け
- [ ] Q2: Server Action で更新処理を行い、成功後にリダイレクトするコードを書け
- [ ] Q3: 削除ボタンに確認ダイアログを表示してから削除する実装を書け

---

### 4/8（水）総合アプリ構築（3）

**💻 コード問題**

- [ ] Q1: 現在のアプリのコンポーネントツリーを図示し、Server / Client の区分を明記せよ
- [ ] Q2: 共通処理（API呼び出し、エラーハンドリング）を適切なユーティリティ関数やカスタムフックに切り出せ
- [ ] Q3: 不要な再レンダリングが発生している箇所を特定し、改善策を述べよ

---

### 4/9（木）コードレビュー + 弱点補強

**📝 知識問題（自己チェック）**

- [ ] Q1: 自分のコードの中で「なぜこう書いたか」を全てのファイルについて説明できるか？
- [ ] Q2: TypeScriptの型定義で `any` を使っている箇所はないか？あれば適切な型に修正
- [ ] Q3: コンポーネントの命名は一貫しているか？ファイル構成は論理的に整理されているか？

---

### 4/10（金）業務準備 + まとめ

**📝 知識問題（最終チェック）**

- [ ] Q1: 業務で使う技術スタックの各技術の役割を1行ずつで説明せよ
- [ ] Q2: 開発フロー（ブランチ作成→開発→PR→レビュー→マージ→デプロイ）の各ステップを説明せよ
- [ ] Q3: わからないことが出たとき、どのドキュメントをどの順番で調べるか、自分の調査フローを言語化せよ

---

### 4/11（土）ポートフォリオ仕上げ

**🏋️ 実装チャレンジ**

- [ ] Q1: Vercelにデプロイし、公開URLにアクセスして全機能が動作するか確認せよ
- [ ] Q2: 環境変数（DB接続文字列、APIキー等）が正しく設定されているか確認せよ
- [ ] Q3: OGP画像が設定され、SNSでシェアした際にプレビューが正しく表示されるか確認せよ
- [ ] Q4: Lighthouseでパフォーマンス・アクセシビリティのスコアを確認し、80点以上を目指せ

---

### 4/12（日）予備日 / 弱点補強

**📝 最終自己評価**

- [ ] Q1: 理解度チェックシートの全項目がLv3以上になっているか？Lv2以下の項目をリストアップせよ
- [ ] Q2: 8週間で作成した全成果物のGitHubリポジトリにREADMEが整備されているか？
- [ ] Q3: 業務初日に「何から始めればよいか」を自分の言葉で説明できるか？

---

## ⭐⭐ 8週間 最終総合チェック（4/12 に実施）

全て何も見ずに回答すること。7割以上正答できれば業務参加の準備は十分。

### TypeScript（目安: 5分）

| # | 問題 | 結果 |
|---|------|------|
| 1 | ジェネリック型 `ApiResponse<T>` を定義し、使用例を書け | |
| 2 | `Partial<User>` と `Pick<User, "name">` の違いを説明せよ | |
| 3 | Discriminated Union 型を使い、Shape（Circle / Rectangle）の面積計算関数を書け | |

### React（目安: 10分）

| # | 問題 | 結果 |
|---|------|------|
| 4 | useState + useEffect を使い、APIからデータ取得→表示するコンポーネントを書け | |
| 5 | カスタムフック `useDebounce` を実装せよ | |
| 6 | React Hook Form + zod で3フィールドのフォームを書け | |
| 7 | Zustandのstoreを定義し、コンポーネントから使用するコードを書け | |

### Tailwind CSS（目安: 5分）

| # | 問題 | 結果 |
|---|------|------|
| 8 | レスポンシブ対応（モバイル1列、デスクトップ3列）のカードグリッドのクラスを書け | |
| 9 | ダークモード対応のナビゲーションバーのクラスを書け | |

### Next.js（目安: 15分）

| # | 問題 | 結果 |
|---|------|------|
| 10 | App Router で `/posts/[id]` の動的ルートページを書け（データ取得含む） | |
| 11 | Server Action でフォーム送信→DB保存→revalidatePath の一連の処理を書け | |
| 12 | Route Handler（`route.ts`）で GET と POST を実装せよ | |
| 13 | `middleware.ts` で認証チェック + リダイレクトを書け | |
| 14 | Server Component と Client Component を適切に組み合わせた画面を設計せよ | |

### 総合（目安: 5分）

| # | 問題 | 結果 |
|---|------|------|
| 15 | Prismaスキーマで User と Post のリレーションを定義し、CRUD操作を書け | |
| 16 | 新機能を開発する際の Git ワークフロー（ブランチ戦略）を説明せよ | |
| 17 | 「なぜServer Componentsが必要か」をチームメンバーに説明するつもりで述べよ | |
| 18 | テストを書く際「何をテストすべきで何をテストしなくてよいか」の判断基準を述べよ | |

---

## 判定基準

| 正答率 | 判定 | アクション |
|--------|------|-----------|
| 90%以上 | 🟢 業務で自走できるレベル | 自信を持って業務参加 |
| 70〜89% | 🟡 業務参加OK、一部サポート必要 | 弱点を重点的に復習し業務参加 |
| 50〜69% | 🟠 基礎は理解、実践力が不足 | 弱点の項目をもう1週間集中学習 |
| 50%未満 | 🔴 基礎からの復習が必要 | 該当する週に戻って再学習 |
