# VB.NET 等幅整列出力（全角混在対応）

## 仕様

- 環境: VB.NET / .NET Framework 4.8
- 出力先: `Multiline` の TextBox（等幅フォント推奨: MS Gothic）
- 列構成
  - `#` … 連番。最大桁に合わせて**右揃え**
  - `項目A` … 文字列。左揃え
  - `項目B` … 文字列。左揃え
- 区切り: 半角スペース 1 つ
- 桁揃え: ヘッダー含めた各列の最大幅（Shift_JIS バイト数）に揃える

## コード

```vbnet
Imports System.Text

Public Class Item
    Public Property No As Integer
    Public Property FieldA As String
    Public Property FieldB As String
End Class

Private Function BuildTable(items As List(Of Item)) As String
    ' ヘッダー
    Dim headers = {"#", "項目A", "項目B"}

    ' 各列のデータを文字列化
    Dim col1 = items.Select(Function(i) i.No.ToString()).ToList()
    Dim col2 = items.Select(Function(i) If(i.FieldA, "")).ToList()
    Dim col3 = items.Select(Function(i) If(i.FieldB, "")).ToList()

    ' 各列の最大幅を計算（ヘッダーも含める）
    Dim w1 = Math.Max(GetWidth(headers(0)), col1.Max(Function(s) GetWidth(s)))
    Dim w2 = Math.Max(GetWidth(headers(1)), col2.Max(Function(s) GetWidth(s)))
    Dim w3 = Math.Max(GetWidth(headers(2)), col3.Max(Function(s) GetWidth(s)))

    Dim sb As New StringBuilder()

    ' ヘッダー行（# は右揃え、それ以外は左揃え）
    sb.Append(PadLeftByWidth(headers(0), w1)) : sb.Append(" ")
    sb.Append(PadRightByWidth(headers(1), w2)) : sb.Append(" ")
    sb.Append(PadRightByWidth(headers(2), w3))
    sb.AppendLine()

    ' データ行
    For i As Integer = 0 To items.Count - 1
        sb.Append(PadLeftByWidth(col1(i), w1)) : sb.Append(" ")
        sb.Append(PadRightByWidth(col2(i), w2)) : sb.Append(" ")
        sb.Append(PadRightByWidth(col3(i), w3))
        sb.AppendLine()
    Next

    Return sb.ToString()
End Function

' 全角=2 / 半角=1 / 半角カナ=1 を Shift_JIS バイト数で正確に判定
Private Function GetWidth(s As String) As Integer
    If String.IsNullOrEmpty(s) Then Return 0
    Return Encoding.GetEncoding("Shift_JIS").GetByteCount(s)
End Function

' 右側を半角スペースで埋める（左揃え）
Private Function PadRightByWidth(s As String, width As Integer) As String
    Return s & New String(" "c, Math.Max(0, width - GetWidth(s)))
End Function

' 左側を半角スペースで埋める（右揃え）
Private Function PadLeftByWidth(s As String, width As Integer) As String
    Return New String(" "c, Math.Max(0, width - GetWidth(s))) & s
End Function
```

## 呼び出し側

```vbnet
Dim items As New List(Of Item) From {
    New Item With {.No = 1,   .FieldA = "ABC-100",   .FieldB = "XYZ-0001"},
    New Item With {.No = 2,   .FieldA = "XYZ-12345", .FieldB = "XYZ-002"},
    New Item With {.No = 10,  .FieldA = "部品A",     .FieldB = "サンプル-100-A"},
    New Item With {.No = 100, .FieldA = "ﾊﾝｶｸｶﾅ",    .FieldB = "全角ﾐｯｸｽ"}
}

TextBox1.Text = BuildTable(items)
```

## 出力イメージ

```
  # 項目A       項目B
  1 ABC-100    XYZ-0001
  2 XYZ-12345  XYZ-002
 10 部品A      サンプル-100-A
100 ﾊﾝｶｸｶﾅ     全角ﾐｯｸｽ
```

## TextBox 側の設定

- `Multiline = True`
- `Font = "MS Gothic"`（全角と半角の幅比が綺麗に 2:1）
- `WordWrap = False`
- `ScrollBars = Both`

## 注意点

- `Encoding.GetEncoding("Shift_JIS")` を使うので、初回呼び出し前に `System.Text.Encoding.RegisterProvider(CodePagesEncodingProvider.Instance)` が必要なケースがあります（.NET Framework 4.8 なら通常不要、.NET Core/5+ で必要）。
- フォントが `MS Gothic` 以外（特にプロポーショナルフォント）だと桁が揃いません。
