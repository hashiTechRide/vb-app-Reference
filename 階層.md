階層ごとに別テーブルなら、`CONNECT BY`は使えないので、各階層で1段ずつJOINして遡る形になります。ただしトリガーごとに手書きするより、**共通のPL/SQL関数を1個作って、各トリガーはそれを呼ぶだけ**にすると保守が楽です。

## 1. ルートID解決用の共通関数

```sql
CREATE OR REPLACE FUNCTION get_root_id(
  p_level      IN NUMBER,   -- そのレコードが何階層目か(1=ルート, 6=末端)
  p_start_id   IN NUMBER    -- そのレコードの直接の親ID(parent_id)
) RETURN NUMBER
IS
  v_id NUMBER := p_start_id;
BEGIN
  -- ルート自身が変更された場合はそのIDがそのままroot_id
  IF p_level = 1 THEN
    RETURN p_start_id;
  END IF;

  -- level階層文だけ、1段ずつ親を辿る
  IF p_level >= 6 THEN
    SELECT parent_id INTO v_id FROM level5_table WHERE id = v_id;
  END IF;
  IF p_level >= 5 THEN
    SELECT parent_id INTO v_id FROM level4_table WHERE id = v_id;
  END IF;
  IF p_level >= 4 THEN
    SELECT parent_id INTO v_id FROM level3_table WHERE id = v_id;
  END IF;
  IF p_level >= 3 THEN
    SELECT parent_id INTO v_id FROM level2_table WHERE id = v_id;
  END IF;
  -- level2の親 = level1(ルート)のid
  RETURN v_id;
EXCEPTION
  WHEN NO_DATA_FOUND THEN
    RETURN NULL;  -- 親が見つからない場合(データ不整合など)
END get_root_id;
/
```

※実際のテーブル名・親子の対応関係(どのlevelがどのlevelの子か)に合わせて`IF`の中身を調整してください。上は「level6→level5→...→level1」の6階層想定のサンプルです。

## 2. 各階層のトリガーから呼び出す

例えば第4階層(level4_table)のトリガー:

```sql
CREATE OR REPLACE TRIGGER trg_level4_log
AFTER INSERT OR UPDATE OR DELETE ON level4_table
FOR EACH ROW
DECLARE
  v_pk        level4_table.id%TYPE;
  v_parent_id level4_table.parent_id%TYPE;
  v_root_id   NUMBER;
BEGIN
  v_pk        := COALESCE(:NEW.id, :OLD.id);
  v_parent_id := COALESCE(:NEW.parent_id, :OLD.parent_id);

  v_root_id := get_root_id(4, v_parent_id);  -- 第4階層 → 親IDから遡る

  INSERT INTO change_log (table_name, pk_value, root_id, changed_at)
  VALUES ('LEVEL4_TABLE', v_pk, v_root_id, SYSTIMESTAMP);
END;
/
```

第1階層(ルート自身)のトリガーだけは特別で、自分のidがそのままroot_idです:

```sql
CREATE OR REPLACE TRIGGER trg_level1_log
AFTER INSERT OR UPDATE OR DELETE ON level1_table
FOR EACH ROW
DECLARE
  v_pk level1_table.id%TYPE;
BEGIN
  v_pk := COALESCE(:NEW.id, :OLD.id);

  INSERT INTO change_log (table_name, pk_value, root_id, changed_at)
  VALUES ('LEVEL1_TABLE', v_pk, v_pk, SYSTIMESTAMP);
END;
/
```

## 3. 出力時のクエリ

```sql
SELECT DISTINCT root_id
FROM change_log
WHERE changed_at > :last_export_time
  AND root_id IS NOT NULL;
```

## 補足

- **NO_DATA_FOUND対策**: 親がすでに削除されている、あるいはトランザクション途中の不整合で親が見つからないケースがあるなら、`EXCEPTION`部分でログに警告を残す、あるいはNULLのまま許容するかは運用判断が必要です。
- **既存トリガーへの影響**: 今のログ用トリガーに`get_root_id(...)`の呼び出しとINSERT文への列追加だけ差し込めば済むので、変更範囲は各トリガーの数行だけです。
- **DELETE時の注意**: 親がすでに削除された後に子が削除される順序(カスケード削除など)だと、`get_root_id`実行時点で中間テーブルの行がすでに消えていてNO_DATA_FOUNDになる可能性があります。カスケード削除を使っているか確認しておくと安全です。

実際のテーブル名と各階層の親子関係(どの列がどのテーブルを指すか)を教えてもらえれば、そのまま使える形で書き直せます。
