二つとも「そのほうが素直」という方向ですが、理由が違うので分けて答えます。

## 1. 日付バリデーションが共通ならDateColumnを作るべきか

作るべきです。前回はTextColumnにValidatorを都度注入する形にしましたが、**全グリッドで日付の扱いが同じ（yyyy/mm/dd形式）なら、その共通ロジックをクラスとして固定する**ほうが理にかなっています。

理由は、Validatorを毎回渡す方式だと「日付列を作るたびに同じparse＆フォーマットのラムダを書く」ことになり、共通のはずのロジックが各レイアウトに散らばるからです。共通なら1箇所（DateColumn）に置いて、使う側は種類を指定するだけにします。

```vb
Public Class DateColumn
    Inherits ColumnDefinition

    Public Property Required As Boolean = False

    Public Overrides Sub ApplyToColumn(grid As C1FlexGrid, col As Column)
        col.DataType = GetType(String)     ' 文字列入力として扱う
        col.TextAlign = TextAlignEnum.CenterCenter
    End Sub

    ' 日付の検証は全グリッド共通なのでここに固定
    Public Overrides Function Validate(value As Object) As String
        Dim s = Convert.ToString(value)
        If String.IsNullOrWhiteSpace(s) Then
            Return If(Required, "日付は必須です", Nothing)
        End If
        Dim d As Date
        If Not Date.TryParseExact(s, "yyyy/MM/dd", Nothing,
                                  Globalization.DateTimeStyles.None, d) Then
            Return "日付は yyyy/MM/dd 形式で入力してください"
        End If
        Return Nothing
    End Function

    ' 表示整形も共通化できる（区切りを補う等）
    Public Overrides Function Format(value As Object) As String
        Dim d As Date
        If Date.TryParse(Convert.ToString(value), d) Then Return d.ToString("yyyy/MM/dd")
        Return Convert.ToString(value)
    End Function
End Class
```

使う側は一行。

```vb
New DateColumn With {.Name = "TARGET_DATE", .Caption = "対象日", .Required = True}
```

### 判断の物差し

「TextColumn＋Validator注入」と「専用クラス化」の選び分けは、**そのロジックが再利用される単位かどうか**です。

- 日付・金額・電話番号のように**型として繰り返し登場し、扱いが決まっている** → 専用クラス（DateColumn/MoneyColumn等）。名前が付いて意図が明快、重複が消える。
- その列**固有の一度きりの検証**（「この画面のこの列だけ、他列との相関チェック」等） → TextColumnにValidator注入。専用クラスにするほどではない。

日付は前者なので、DateColumnにするのが正解です。TextColumnは「汎用の文字列列」として残し、DateColumnはそれを特化した兄弟、という関係になります。

```
ColumnDefinition（基底）
   ├─ TextColumn      … 汎用文字列。固有検証はValidator注入
   ├─ DateColumn      … 日付特化。検証・整形を内蔵（共通）
   ├─ BooleanColumn   … チェックボックス
   ├─ MasterColumn    … DataMap
   └─ LookupColumn    … 別フォーム選択
```

将来「日付形式をyyyy/mm/dd以外も許す画面が出た」場合は、DateColumnにフォーマット指定プロパティを足すか、その画面だけTextColumn＋Validatorに落とせばよく、共通のDateColumnは壊れません。

## 2. LargeColumnLayoutをViewModelにマージしてよいか

これはマージしてよいです。むしろ**分けている必要性が薄い**。列レイアウトはそのタブ固有の情報で、ViewModelもそのタブ固有です。両者は1対1で結びついていて、他から再利用されないなら、別クラスにする意味は小さい。

前回IColumnLayoutを別に切ったのは「列構成をデータ取得やViewModelから独立させたい」意図でしたが、実際には列構成はViewModelの一部と考えるほうが自然な場面が多いです。ViewModelが「自分のタブはこういう列を持つ」と知っているのは責任として妥当です。

```vb
Public Class LargeEditorViewModel
    Implements ITableEditorViewModel

    Private ReadOnly _repository As IListRepository
    Private ReadOnly _factories As IList(Of MasterItem)

    Public Sub New(repository As IListRepository, factories As IList(Of MasterItem))
        _repository = repository
        _factories = factories
    End Sub

    ' 列構成をViewModelが直接持つ（旧LargeColumnLayoutをマージ）
    Public Function GetColumns() As IReadOnlyList(Of ColumnDefinition) _
            Implements ITableEditorViewModel.GetColumns
        Return New List(Of ColumnDefinition) From {
            New TextColumn With {.Name = "NAME", .Caption = "名称"},
            New DateColumn With {.Name = "TARGET_DATE", .Caption = "対象日", .Required = True},
            New BooleanColumn With {.Name = "ENABLED", .Caption = "有効"},
            New MasterColumn With {.Name = "FACTORY_ID", .Caption = "工場",
                                   .Map = ToMap(_factories)}
        }
    End Function
End Class
```

`ITableEditorViewModel` に `GetColumns()` を含めておけば、UserControlは「ViewModelから列構成をもらってグリッドに適用する」だけで済み、別のレイアウトクラスを知る必要がありません。

### ただしマージしないほうがよいケース

以下に当てはまるなら、分けたままのほうがよいです。

- **同じ列構成を複数のViewModelで共有する**。中・小タブが列レイアウトを一部共通化するなら、共通レイアウトを別クラスにして各ViewModelから参照するほうが重複が減る。
- **列構成が動的に大きく変わり、そのロジックが肥大する**。ユーザー権限や設定で列が増減し、その組み立てが数十行になるなら、ViewModelから切り出して見通しを保つ。
- **列構成だけを単体テストしたい**。レイアウトが複雑で、ViewModel全体を組まずに列定義だけ検証したい場合。

今回のように「各タブが自分の固定的な列構成を持つ」だけなら、これらに当てはまらないので、マージして問題ありません。分割は「再利用・肥大・独立テスト」のどれかの必要が出てから行えば十分です。

### 判断の物差し

クラスを分けるのは、**それ自体が独立した変更理由・再利用・テスト対象を持つとき**です。LargeColumnLayoutが「LargeEditorViewModelとだけ心中する」関係なら、独立した存在意義がないのでマージが素直。逆に他と共有したり単体で育つなら分ける。前半で話した「責任で分ける、名詞の一致では分けない」と同じ物差しです。

## まとめ

- **DateColumnは作るべき。** 日付の検証・整形が全グリッド共通なら、TextColumn＋Validator注入で毎回書くより、共通ロジックをDateColumnに固定するほうが重複が消えて意図が明快。「型として繰り返し登場し扱いが決まっている」ものは専用クラス、「一度きりの固有検証」はValidator注入、という使い分け。
- **LargeColumnLayoutはViewModelにマージしてよい。** 列構成がそのタブ固有でViewModelと1対1、他で再利用しないなら、別クラスの存在意義は薄い。`ITableEditorViewModel.GetColumns()` として持たせれば自然。
- ただし、複数ViewModelで列構成を共有する・組み立てが肥大する・列定義だけ単体テストしたい、のいずれかがあれば分けたままにする。
- 分割の物差しは一貫して「独立した変更理由・再利用・テスト対象を持つか」。持たないならマージ、持つなら分離。

日付は共通ロジックだからクラス化して重複を消す、レイアウトは固有だからViewModelに寄せて余計な分割を減らす——方向は逆ですが、どちらも「共通なら集約、固有なら近くに置く」という同じ判断から出ています。
