**ラウンドトリップ問題は「回避する」のではなく「検知する」方針に切り替える**必要があります。まずそのための道具を作ります。## 1. ラウンドトリップ検査（NPOI 採用の前提条件）

`TemplateIntegrityChecker` は、**原本を実際に NPOI で開いて書き出し、前後を突き合わせる**ものです。

- `System.IO.Packaging`（`WindowsBase.dll`、.NET Framework 標準）で xlsx の中身を OPC パート単位で列挙し、消えたパートを特定
- 「グラフ」「ピボットテーブル」「スライサー」「ActiveX」「マクロ」など、NPOI が扱えない要素を**日本語の名前で**警告
- シート数・名前定義数・結合セル数・図形数・画像数を処理前後で比較
- 書き出したファイルを**開き直せるか**まで確認（Excel が「修復が必要」と言い出すケースの一次検知）

追加 NuGet はゼロです。これを原本受け入れ時に必ず通してください。前回話した「検証ツール」の中核になります。

## 2. ShiftRows の重要な確認結果

`XSSFSheet.ShiftRows` の実装をソースで追ったところ、追随処理はこうなっていました。

```
✅ ShiftMergedRegions        結合セル
✅ UpdateNamedRanges         名前定義
✅ UpdateFormulas            数式
✅ UpdateConditionalFormatting  条件付き書式
✅ UpdateHyperlinks          ハイパーリンク
✅ ShiftCommentsAndRows      コメント・行の高さ

❌ 図形・画像（Drawing）— 対応する処理そのものが存在しない
```

つまり **行を挿入しても、アンカー済みの図形・画像は動きません。** セルだけが下にずれて、図形が本文に重なります。

図形を多用する方針とのことなので、ここは正面から効いてきます。`DetailBlockWriter` では2つのモードを用意しました。

- **`FixedRows`（推奨）** — 原本にあらかじめ N 行の明細行を用意しておき、その範囲で書く。**行を挿入しないので図形が絶対にずれません。** 件数上限が決まっている帳票はこちら
- **`ShiftAndCopy`** — 件数可変の一覧向け。実行前に**明細ブロックより下の図形を検出して例外を投げます**。「壊れた帳票が静かに出る」より落ちる方がマシ、という判断です

余った行は「空欄にして罫線を残す」「非表示にする」から選べます。金額欄のように原本側の数式を活かしたい列は `SourceColumn` を `Nothing` にすれば書き込みをスキップします。

## 3. あと2つ、運用前に決めておくこと

**原本の「入れてはいけないもの」を明文化してください。** 検査ツールで検出はできますが、ユーザーには事前に伝わっている方が親切です。少なくともグラフ・ピボットテーブル・テーブル書式（テーブルとして書式設定）・フォームコントロール・マクロは禁止リストに入れるべきです。条件付き書式は NPOI が ShiftRows で追随させてはいますが、データバーやアイコンセットは x14 拡張なので、検査で引っかかるようなら避けた方が無難です。

**数式は `sheet.ForceFormulaRecalculation = True` を忘れずに。** NPOI は数式を計算しないので、原本に仕込んだ `=D5*E5` は値が空のままファイルに残ります。このフラグを立てておけば Excel が開いた瞬間に再計算します。立て忘れると「金額が全部 0 の帳票」が出ます。
