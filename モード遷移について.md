要件次第ですが、Editを中心にする必要はありません。今の要件「削除・復元はEditからのみ遷移／保存・キャンセルでEditへ戻る」が、たまたまEdit中心のハブ&スポーク型になっているだけです。遷移ルールが変われば形も変わります。

## 今がEdit中心になっている理由

現状の要件を図にするとこうなります。

```
        ┌──────┐
   ┌───▶│ Edit │◀───┐
   │    └──────┘    │
   │      │  ▲       │
   ▼      ▼  │       ▼
┌────────┐  │  ┌─────────┐
│ Delete │──┘  │ Restore │
└────────┘     └─────────┘
   （DeleteとRestoreは直接行き来できない）
```

Delete↔Restoreの直接遷移が無く、必ずEditを経由する。だからEditがハブになります。これは「設計上Editを中心にした」のではなく、「要件がそうなっている」結果です。要件が変われば中心も変わります。

## 中心を持たない／別の中心にする例

たとえば「Delete中でもRestoreに直接切り替えたい」となれば、Edit中心は崩れます。

```
Edit ⇄ Delete ⇄ Restore ⇄ Edit  （相互に行き来）
```

この場合、`EnterDelete` が「Editからのみ」という制約を持つのは間違いになります。遷移ルールはあくまで要件を写したものなので、要件に合わせて遷移メソッドを書き換えます。

## 設計上のポイント：中心の有無に依存しない作りにする

重要なのは、**`EditorModeState` の構造が「Edit中心であること」に依存していない**点です。前回の実装は、各遷移メソッドが個別に「どこから来ていいか」をチェックする形でした。

```vb
Public Sub EnterDelete()
    If _mode <> EditorMode.Edit Then Throw ...   ' この制約は要件由来であって構造由来ではない
    Mode = EditorMode.Delete
End Sub
```

この `If _mode <> EditorMode.Edit` は「Editから来い」という**要件**を書いているだけで、`EditorModeState` という仕組みがEdit中心を強制しているわけではありません。だから要件が変われば、この条件を変えるだけで済みます。中心が無い相互遷移なら条件を外す、別の制約なら別の条件にする。

## 遷移ルールを宣言的にしておくと変更に強い

「どこからどこへ行けるか」が今後変わりそうなら、許可遷移を表で持つと、要件変更が一箇所で済みます。Edit中心かどうかをコードに埋め込まずに表現できます。

```vb
Public Class EditorModeState
    Implements INotifyPropertyChanged

    Private _mode As EditorMode = EditorMode.Edit

    ' 許可する遷移（From → 行けるToの集合）。ここだけ直せば遷移ルールが変わる
    Private Shared ReadOnly Allowed As New Dictionary(Of EditorMode, HashSet(Of EditorMode)) From {
        {EditorMode.Edit,    New HashSet(Of EditorMode) From {EditorMode.Delete, EditorMode.Restore}},
        {EditorMode.Delete,  New HashSet(Of EditorMode) From {EditorMode.Edit}},
        {EditorMode.Restore, New HashSet(Of EditorMode) From {EditorMode.Edit}}
    }

    Public Function CanTransit(toMode As EditorMode) As Boolean
        Return Allowed(_mode).Contains(toMode)
    End Function

    Public Sub TransitTo(toMode As EditorMode)
        If Not CanTransit(toMode) Then
            Throw New InvalidOperationException($"{_mode} から {toMode} へは遷移できません")
        End If
        Mode = toMode
    End Sub

    Private Property Mode As EditorMode
        Get
            Return _mode
        End Get
        Set(value As EditorMode)
            If _mode <> value Then
                _mode = value
                RaiseEvent PropertyChanged(Me, New PropertyChangedEventArgs(NameOf(Mode)))
            End If
        End Set
    End Property

    Public Event PropertyChanged As PropertyChangedEventHandler _
        Implements INotifyPropertyChanged.PropertyChanged
End Class
```

Edit中心にしたいなら今の表のまま。Delete↔Restoreを直接許すなら表に足すだけ。`CanTransit` をボタンの活性判定にそのまま使えるので、UI側の「Editのときだけ削除ボタン活性」も表から自動で導けます。

```vb
deleteModeButton.Enabled = _modeState.CanTransit(EditorMode.Delete) AndAlso Not locked
restoreModeButton.Enabled = _modeState.CanTransit(EditorMode.Restore) AndAlso Not locked
```

## 「保存・キャンセルでEditへ戻る」も同じ

保存後の戻り先も、今は「必ずEdit」ですが、これも要件です。「削除モードで保存したら一覧に戻る」等になれば、戻り先をモードごとに決める形にします。前回の `AfterAction` が `BackToEdit` 固定だった部分を、モードに応じた戻り先にすれば対応できます。

```vb
Private Function GetReturnMode(current As EditorMode) As EditorMode?
    Select Case current
        Case EditorMode.Edit Then Return Nothing       ' Nothing = 画面終了
        Case Else Return EditorMode.Edit               ' 今の要件。変えるならここ
    End Select
End Function
```

## まとめ

- Edit中心は**設計の決まりではなく、今の要件がそうなっているだけ**。中心にする必然性はない。
- `EditorModeState` の構造自体はEdit中心に依存していない。`If _mode <> Edit` は要件を写した条件にすぎず、要件が変われば書き換えればよい。
- 今後遷移ルールが変わりそうなら、**許可遷移を表（Dictionary）で宣言的に持つ**と、変更が一箇所で済み、ボタン活性判定にも流用できる。
- 戻り先（保存・キャンセル後）も同様に要件次第。Edit固定をやめてモードごとの戻り先にもできる。

今の要件のままならEdit中心で何も問題ありません。「将来DeleteとRestoreを直接行き来させるかも」といった変更可能性があるなら、宣言的な遷移表にしておくと、そのときコードのあちこちを触らずに済みます。どちらにするかは、遷移ルールがどれくらい変わりそうかで決めてください。
