列ごとの入力形式を扱うなら、「列の種類」を第一級の概念にするのが、今回の最適解だと思います。MVVMという枠より、**列定義（ColumnDefinition）を主役に据える**ほうが、見やすさ・切り替えやすさの両方に効きます。理由から説明します。

## 考え方：列の「種類」をオブジェクトにする

日付・真偽・コンボ・セルボタンといった入力形式は、要するに列ごとに違う「振る舞いの束」です。これを `If 列名 = "日付" Then ... ElseIf ...` で各所に散らすと、列が増えるたびに複数箇所を触ることになり、見づらくなります。

そこで**列1つ = 1つの定義オブジェクト**にまとめます。その列に関する知識（表示名、幅、編集の見せ方、入力検証、値の整形）を全部その中に閉じ込める。すると「この列はどう振る舞う?」の答えが1箇所に集まり、コードが追いやすくなります。

```
1列 = ColumnDefinition（この列の全知識を持つ）
   ├─ どう表示するか（キャプション・幅・書式）
   ├─ どう編集させるか（テキスト/チェック/コンボ/ボタン）
   ├─ 入力をどう検証するか（日付フォーマット等）
   └─ 値をどう整形するか
```

## 基底クラスと種類ごとの派生

列の種類ごとにクラスを分けます。共通の骨格を基底に、種類固有の振る舞いを派生に。

```vb
' 列定義の基底：全列種に共通する骨格
Public MustInherit Class ColumnDefinition
    Public Property Name As String          ' DataTableのカラム名
    Public Property Caption As String       ' 見出し
    Public Property Width As Integer = 100
    Public Property [ReadOnly] As Boolean

    ' グリッドの列にこの種類の編集方法を適用する
    Public MustOverride Sub ApplyToColumn(grid As C1FlexGrid, col As Column)

    ' 編集完了時の検証。エラー文字列を返す（Nothing=OK）
    Public Overridable Function Validate(value As Object) As String
        Return Nothing
    End Function

    ' 表示用の整形（必要な種類だけ上書き）
    Public Overridable Function Format(value As Object) As String
        Return Convert.ToString(value)
    End Function
End Class
```

### テキスト（日付など、文字列入力＋フォーマットチェック）

```vb
Public Class TextColumn
    Inherits ColumnDefinition

    Public Property MaxLength As Integer = 0
    ' 検証ロジックを差し込めるようにしておく（日付・数値などを都度定義せず済む）
    Public Property Validator As Func(Of String, String)

    Public Overrides Sub ApplyToColumn(grid As C1FlexGrid, col As Column)
        col.DataType = GetType(String)
        col.Editor = Nothing                 ' 既定のテキスト編集
    End Sub

    Public Overrides Function Validate(value As Object) As String
        If Validator IsNot Nothing Then Return Validator(Convert.ToString(value))
        Return Nothing
    End Function
End Class
```

日付列はこれを使い、Validatorに日付チェックを渡すだけで作れます。

```vb
Dim dateCol As New TextColumn With {
    .Name = "TARGET_DATE", .Caption = "対象日",
    .Validator = Function(s)
                     Dim d As Date
                     Return If(Date.TryParseExact(s, "yyyy/MM/dd",
                               Nothing, Globalization.DateTimeStyles.None, d),
                               Nothing, "日付は yyyy/MM/dd 形式で入力してください")
                 End Function
}
```

### 真偽（チェックボックス）

```vb
Public Class BooleanColumn
    Inherits ColumnDefinition

    Public Overrides Sub ApplyToColumn(grid As C1FlexGrid, col As Column)
        col.DataType = GetType(Boolean)
        ' C1FlexGridはBoolean列を自動でチェックボックス表示
        grid.Cols(col.Name).Style.WordWrap = False
    End Sub
End Class
```

### コンボ（マスタ管理の工場など、選択肢が少数）

選択肢（マスタ）を渡して構成。ComboBox形式にします。

```vb
Public Class ComboColumn
    Inherits ColumnDefinition

    Public Property Items As IList(Of MasterItem)   ' Id/Name のペア

    Public Overrides Sub ApplyToColumn(grid As C1FlexGrid, col As Column)
        col.DataType = GetType(String)
        Dim cb As New C1.Win.C1List.C1Combo()
        ' Items を C1Combo にバインド（表示=Name、値=Id）
        col.Editor = cb
        ' DataMap を使って「保存値(Id)↔表示(Name)」を対応させる方法もある
    End Sub

    Public Overrides Function Format(value As Object) As String
        Dim hit = Items.FirstOrDefault(Function(m) Equals(m.Id, value))
        Return If(hit?.Name, Convert.ToString(value))
    End Function
End Class
```

### セルボタン（取引先など、件数が多く別フォーム選択）

セルにボタンを出し、押すと選択フォームを開いて結果を書き戻す。列定義には「どのフォームを開くか」を差し込みます。

```vb
Public Class LookupColumn
    Inherits ColumnDefinition

    ' 現在値を渡すと、選択フォームを開いて選択結果を返す関数を注入
    Public Property PickValue As Func(Of Object, LookupResult)

    Public Overrides Sub ApplyToColumn(grid As C1FlexGrid, col As Column)
        col.DataType = GetType(String)
        ' C1FlexGridのセルボタン設定（ButtonPictureや ShowButtons）
        grid.Cols(col.Name).ButtonAppearance = ...   ' セルにボタン表示
        ' ボタンクリックのハンドリングはグリッド共通イベントで種類判別（後述）
    End Sub

    Public Overrides Function Format(value As Object) As String
        Return Convert.ToString(value)   ' 表示名解決が要るならここで
    End Function
End Class

Public Class LookupResult
    Public Property Selected As Boolean
    Public Property Id As Object
    Public Property DisplayName As String
End Class
```

`PickValue` にフォームを開く処理を注入するので、取引先フォームでも他のマスタ選択フォームでも、同じ `LookupColumn` を使い回せます。

## タブ（ViewModel）ごとの列構成

各タブは「どの列定義をどう並べるか」のリストを持つだけになります。カラムが違う問題は、このリストの中身が違うだけで吸収されます。**共通の強い型モデルは要らず、列定義のリストで各タブの個性を表現**します。

```vb
' 各タブが自分の列構成を返す
Public Interface IColumnLayout
    Function GetColumns() As IReadOnlyList(Of ColumnDefinition)
End Interface

Public Class LargeColumnLayout
    Implements IColumnLayout

    Private ReadOnly _factories As IList(Of MasterItem)
    Public Sub New(factories As IList(Of MasterItem))
        _factories = factories
    End Sub

    Public Function GetColumns() As IReadOnlyList(Of ColumnDefinition) _
            Implements IColumnLayout.GetColumns
        Return New List(Of ColumnDefinition) From {
            New TextColumn With {.Name = "NAME", .Caption = "名称"},
            New TextColumn With {.Name = "TARGET_DATE", .Caption = "対象日", .Validator = AddressOf ValidateDate},
            New BooleanColumn With {.Name = "ENABLED", .Caption = "有効"},
            New ComboColumn With {.Name = "FACTORY_ID", .Caption = "工場", .Items = _factories},
            New LookupColumn With {.Name = "CLIENT_ID", .Caption = "取引先", .PickValue = AddressOf PickClient}
        }
    End Function
End Class
```

この列レイアウトを見れば、そのタブがどんな列をどんな入力形式で持つか一目で分かります。改修が来ても「その列の定義を差し替える／足す」で済み、追う場所が明確です。

## グリッドへの適用（1箇所で回す）

列定義のリストをグリッドに適用する処理は共通化できます。種類ごとの分岐は各 `ApplyToColumn` に閉じているので、ここは種類を意識せずループするだけです。

```vb
Public Class GridConfigurator
    Public Shared Sub Configure(grid As C1FlexGrid,
                                table As DataTable,
                                columns As IReadOnlyList(Of ColumnDefinition))
        grid.DataSource = table
        For Each def In columns
            Dim col = grid.Cols(def.Name)
            col.Caption = def.Caption
            col.Width = def.Width
            col.AllowEditing = Not def.ReadOnly
            def.ApplyToColumn(grid, col)     ' 種類ごとの振る舞いは多態で解決
        Next
    End Sub
End Class
```

`If 種類 Then` が無いのがポイントです。種類が増えても `GridConfigurator` は変えず、新しい列定義クラスを1つ足すだけ。これが見やすさと切り替えやすさの核心です。

## 編集完了時の検証・ボタン処理も定義から引く

グリッドの共通イベントで、対象列の定義を引いて振る舞いを委譲します。列名→定義の辞書を用意しておきます。

```vb
Private _defs As Dictionary(Of String, ColumnDefinition)

' 検証：編集確定時
Private Sub grid_ValidateEdit(sender As Object, e As ValidateEditEventArgs) _
        Handles grid.ValidateEdit
    Dim def = _defs(grid.Cols(e.Col).Name)
    Dim err = def.Validate(grid.Editor?.Text)
    If err IsNot Nothing Then
        e.Cancel = True                      ' 確定させない
        errorProvider.SetError(grid, err)    ' or セルにエラー表示
    End If
End Sub

' セルボタン：クリック時
Private Sub grid_CellButtonClick(sender As Object, e As RowColEventArgs) _
        Handles grid.CellButtonClick
    Dim def = TryCast(_defs(grid.Cols(e.Col).Name), LookupColumn)
    If def IsNot Nothing Then
        Dim result = def.PickValue(grid(e.Row, e.Col))   ' 別フォームを開く
        If result.Selected Then
            grid(e.Row, e.Col) = result.Id               ' 値を書き戻す
            ' 表示名を別列に入れるならここで
        End If
    End If
End Sub
```

検証もボタン処理も、`If 列名...` の羅列ではなく「定義に聞く」形になり、列固有の知識が定義クラスに集約されます。

## MVVMとの距離について

MVVMにこだわらなくてよい、という判断は今回の要件に合っています。ただ全部をコードビハインドに戻すのではなく、**「列の種類」という軸で分ける**のが今回の最適解です。理由は、あなたの要件（列ごとに入力形式が違う・タブで共通仕様を切り替えたい）が、まさに「列の振る舞いの違い」だからです。分割の軸を要件に合わせると、コードが素直になります。

結果として残るのは、MVVMの厳密な三層ではなく、以下のゆるい構成です。

```
ColumnDefinition 派生群   … 列の種類ごとの振る舞い（主役）
IColumnLayout            … タブごとの列構成（どの定義を並べるか）
GridConfigurator         … 定義をグリッドに適用（共通・分岐なし）
DataTable + Repository   … データ（前回のまま）
UserControl              … グリッドと定義・データを繋ぐ橋渡し
```

「モード」「ロック」などタブ横断の状態は前回の共有オブジェクト（`EditorModeState`/`DbLockState`）のまま活かせます。列定義はそれらと独立した軸なので、共存できます。たとえば「モードがReadOnlyなら全列 `AllowEditing=False`」は `GridConfigurator` 側で一括制御すればよく、各列定義は触りません。

## なぜこれが「見やすい」か

- **1つの列の全知識が1クラスに集まる。** 「対象日列ってどう検証してたっけ」→ `TextColumn`＋そのValidatorを見れば終わり。あちこち探さない。
- **種類の追加が局所的。** 新しい入力形式（例：スピンボックス列）が来ても、`SpinColumn : ColumnDefinition` を1つ足すだけ。既存コードは無変更。
- **タブの構成が宣言的に読める。** `GetColumns()` を見れば、そのタブの列と入力形式が一覧できる。
- **分岐が消える。** `If 列名 Then` の連鎖が多態に置き換わり、条件分岐を追う負担が無くなる。

## まとめ

- 列ごとの入力形式は、**「列の種類」をオブジェクト化（`ColumnDefinition` 派生）** して扱うのが今回の最適解。
- 種類ごとに `TextColumn`（日付など・検証差し込み）／`BooleanColumn`（チェック）／`ComboColumn`（マスタ選択）／`LookupColumn`（別フォーム選択）を用意。共通の骨格は基底に、固有の振る舞いは派生に閉じる。
- 各タブは `IColumnLayout.GetColumns()` で**列定義のリストを宣言的に持つ**。カラム差異はこのリストの違いで吸収され、共通の強い型モデルは不要。
- グリッドへの適用・検証・ボタン処理は**列定義に委譲**し、`GridConfigurator` や共通イベントは種類を意識せず回す（`If 列名` を排除）。
- MVVMの厳密さより、**要件（列ごとの振る舞いの違い）に分割軸を合わせる**ことでコードが見やすくなる。モード・ロックの共有状態は独立軸として共存できる。

改修の想定が難しいとのことですが、この構成は「どんな改修か」を予測しなくても、**変更の単位（列の種類・タブの構成）とコードの単位が一致している**ので、来た改修に対して触る場所が自然に定まります。予測でなく、変更の局所化で対応する形です。列という具体に軸を置いたことが、結果的に改修耐性にもつながります。
