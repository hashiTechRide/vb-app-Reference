モードが状態機械になっているので、それを明示的にモデル化すると整理できます。今ボタンの意味がモードで変わって困っているのは、モードを各所で `If` 判定しているからだと思います。モードを1つの状態として持たせ、遷移とボタン挙動をモード側に閉じ込めます。

## まずモードを型にする

```vb
Public Enum EditorMode
    Edit       ' 初期。保存=画面終了、キャンセル=画面終了
    Delete     ' Editからのみ遷移。保存/キャンセル=Editへ戻る
    Restore    ' Editからのみ遷移。保存/キャンセル=Editへ戻る
End Enum
```

このモードは**全タブ共通の横断状態**です。各ViewModelが個別に持つと、タブ切り替えでモードがバラけます。前回のロック状態と同じ構図なので、共有状態にするのが筋です。

```vb
' 共有：現在のモード（単一の真実）
Public Class EditorModeState
    Implements INotifyPropertyChanged

    Private _mode As EditorMode = EditorMode.Edit  ' 最初は編集モード

    Public Property Mode As EditorMode
        Get
            Return _mode
        End Get
        Private Set(value As EditorMode)
            If _mode <> value Then
                _mode = value
                RaiseEvent PropertyChanged(Me, New PropertyChangedEventArgs(NameOf(Mode)))
            End If
        End Set
    End Property
```

## 遷移ルールを状態側に閉じ込める

「削除・復元は編集モードからしか遷移できない」「保存・キャンセルで編集へ戻る」というルールを、呼び出し側の `If` ではなく状態オブジェクトのメソッドにします。不正遷移をここで弾けます。

```vb
    ' Edit からのみ Delete/Restore へ
    Public Sub EnterDelete()
        If _mode <> EditorMode.Edit Then
            Throw New InvalidOperationException("削除モードは編集モードからのみ遷移できます")
        End If
        Mode = EditorMode.Delete
    End Sub

    Public Sub EnterRestore()
        If _mode <> EditorMode.Edit Then
            Throw New InvalidOperationException("復元モードは編集モードからのみ遷移できます")
        End If
        Mode = EditorMode.Restore
    End Sub

    Public Sub BackToEdit()
        Mode = EditorMode.Edit
    End Sub

    Public Event PropertyChanged As PropertyChangedEventHandler _
        Implements INotifyPropertyChanged.PropertyChanged
End Class
```

ルールが1箇所に集まるので、「どこからDeleteに行けるんだっけ」を探さずに済みます。

## 保存・キャンセルボタンの意味をモードに紐づける

ここが核心です。ボタンの `Click` ハンドラに `If Mode = ... Then ... ElseIf ...` を書くと、モードが増えるたびに全ボタンを触ることになります。**「保存したら次に何をするか」をモードの責務として持たせます。**

```vb
Public Class EditorViewModel
    Private ReadOnly _modeState As EditorModeState

    ' 保存ボタン押下時の振る舞い
    Public Sub OnSave()
        ' 1. まず実際の保存処理（モード共通でも分岐でもよい）
        PersistChanges()

        ' 2. 保存後の遷移はモードで決まる
        Select Case _modeState.Mode
            Case EditorMode.Edit
                RaiseEvent RequestClose(Me, EventArgs.Empty)   ' 画面終了
            Case EditorMode.Delete, EditorMode.Restore
                _modeState.BackToEdit()                        ' 編集モードへ
        End Select
    End Sub

    ' キャンセルボタン押下時の振る舞い
    Public Sub OnCancel()
        Select Case _modeState.Mode
            Case EditorMode.Edit
                RaiseEvent RequestClose(Me, EventArgs.Empty)   ' 画面終了
            Case EditorMode.Delete, EditorMode.Restore
                _modeState.BackToEdit()                        ' 編集モードへ
        End Select
    End Sub

    Public Event RequestClose As EventHandler
End Class
```

「画面を閉じる」のはViewの仕事なので、ViewModelは `RequestClose` イベントで依頼するだけにします（前回までの境界原則どおり、ViewModelが `Me.Close()` を呼ばない）。Formがそれを受けて閉じます。

```vb
' Form 側
AddHandler _activeViewModel.RequestClose, Sub(s, e) Me.Close()
```

`Select Case` が2箇所（保存・キャンセル）に出ていますが、保存とキャンセルで「保存処理をするか否か」だけが違い、遷移先は同じです。共通化するなら遷移部分を切り出せます。

```vb
Private Sub AfterAction()
    If _modeState.Mode = EditorMode.Edit Then
        RaiseEvent RequestClose(Me, EventArgs.Empty)
    Else
        _modeState.BackToEdit()
    End If
End Sub

Public Sub OnSave()
    PersistChanges()
    AfterAction()
End Sub

Public Sub OnCancel()
    AfterAction()
End Sub
```

## ボタンのラベルや活性もモードに従わせる

モードで「保存」ボタンの表示が変わるはず（削除モードなら「削除実行」など）。これもモード起点で1箇所にまとめ、モード変更通知で更新します。

```vb
' View：モード変更を購読してボタン表示・モード遷移ボタンの活性を更新
AddHandler _modeState.PropertyChanged,
    Sub(s, e)
        If e.PropertyName = NameOf(EditorModeState.Mode) Then
            If InvokeRequired Then BeginInvoke(AddressOf RefreshForMode) Else RefreshForMode()
        End If
    End Sub

Private Sub RefreshForMode()
    Select Case _modeState.Mode
        Case EditorMode.Edit
            saveButton.Text = "保存"
            cancelButton.Text = "キャンセル"
            deleteModeButton.Enabled = True    ' Editのときだけ遷移可能
            restoreModeButton.Enabled = True
        Case EditorMode.Delete
            saveButton.Text = "削除実行"
            cancelButton.Text = "戻る"
            deleteModeButton.Enabled = False
            restoreModeButton.Enabled = False
        Case EditorMode.Restore
            saveButton.Text = "復元実行"
            cancelButton.Text = "戻る"
            deleteModeButton.Enabled = False
            restoreModeButton.Enabled = False
    End Select
End Sub
```

「Editのときだけ削除・復元ボタンを活性」とすることで、遷移ルールがUI上でも表現され、不正遷移を物理的に防げます。状態側の例外は保険として残します。

## 前回までの構造との接続

モードは前回の `DbLockState` と同じ「共有横断状態」です。Form周りの共有状態が2つになります。

```
共有状態（起動時に1度だけ生成・購読）
   ├─ DbLockState       … ロック中はボタン全般を無効化
   └─ EditorModeState   … モードで保存/キャンセルの意味とボタン表示が変わる
各タブの ViewModel は両方を参照し、UserControl がそれらを購読して表示更新
```

ボタンの最終的な活性は両者のAND（モード的に押せる ∧ ロックされていない）になります。`RefreshForMode` と前回の `RefreshActionButtons` を1つの「ボタン状態再計算」にまとめると、二重管理になりません。

```vb
Private Sub RefreshButtons()
    Dim locked = _lockState.Info.IsLocked
    saveButton.Enabled = Not locked
    cancelButton.Enabled = True               ' キャンセルはロック中も許可するなら
    deleteModeButton.Enabled = (_modeState.Mode = EditorMode.Edit) AndAlso Not locked
    restoreModeButton.Enabled = (_modeState.Mode = EditorMode.Edit) AndAlso Not locked
    ' ラベル更新も同じ場所で
End Sub
```

## まとめ

- モードを `EditorMode` 列挙にし、**共有 `EditorModeState`** で1つの真実として持つ（最初はEdit）
- 遷移ルール（Delete/RestoreはEditからのみ、保存/キャンセルでEditへ）を**状態オブジェクトのメソッドに閉じ込め**、不正遷移を弾く
- 保存・キャンセルの「次に何をするか」を**モード起点の1メソッド**にまとめ、`If` の散在を防ぐ
- 画面終了はViewModelが `RequestClose` で依頼し、Formが閉じる（境界維持）
- ボタンの表示・活性はモード変更通知で再計算。**ロック状態とモードのANDで1箇所に集約**

ボタンの意味がモードで変わって困っていた根本は、モードが暗黙的にあちこちに散っていたことです。`EditorModeState` という1つの状態に遷移と「保存後の振る舞い」を持たせると、ボタン側は「押されたら今のモードに従う」だけになり、判定が消えます。
