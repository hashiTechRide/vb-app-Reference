# SVNからGitへの移行提案書

**想定ユーザー数: 10〜20名**

作成日: 2026年3月5日

---

## 目次

1. [Gitに移行したい理由 — SVNの課題とGitによる解決](#1-gitに移行したい理由)
2. [Gitクライアントの選定](#2-gitクライアントの選定)
3. [コードレビューの仕組み](#3-コードレビューの仕組み)
4. [レビュー指摘の管理・分析](#4-レビュー指摘の管理分析)
5. [リポジトリの立ち上げタイミングとブランチ戦略](#5-リポジトリの立ち上げタイミングとブランチ戦略)
6. [GitLab CE + Sourcetreeでの通知](#6-gitlab-ce--sourcetreeでの通知)
7. [SVNとGitのリポジトリ管理方法の違い](#7-svnとgitのリポジトリ管理方法の違い)
8. [Git移行の効果測定方法](#8-git移行の効果測定方法)

---

## 1. Gitに移行したい理由

### 1.1 SVNで発生している課題とGitでの解決

| # | SVNで発生する課題 | Gitではどう解決されるか |
|---|-----------------|----------------------|
| 1 | **サーバー障害時に全作業が停止する** — SVNは集中型のため、中央サーバーがダウンすると、コミットはもちろん、ログ閲覧やdiff確認すらできなくなる | Gitは分散型のため、各開発者のローカルにリポジトリの完全なコピーが存在する。サーバー停止中もコミット、ブランチ作成、履歴閲覧、diff確認がすべてローカルで可能。サーバー復旧後にpushすればよい |
| 2 | **ブランチの作成・マージが重い** — SVNのブランチはディレクトリコピーで実装されているため、作成に時間がかかり、マージもコンフリクトが頻発しやすい。結果としてブランチの利用が敬遠される | Gitのブランチはポインタ（参照）として実装されており、作成は一瞬（数ミリ秒）。マージも3-wayマージアルゴリズムで自動解決能力が高い。気軽にブランチを切れるため、フィーチャーブランチ戦略が実践しやすい |
| 3 | **オフラインで作業できない** — 出張先やネットワーク障害時に、コミットも履歴確認もできず、作業の区切りをつけられない | すべての操作がローカルで完結する。新幹線の中でもコミット・ブランチ切り替え・履歴確認が可能。ネットワーク復帰後にpushするだけ |
| 4 | **コードレビューの仕組みがない** — SVN単体にはプルリクエストやレビュー承認の機能がない。レビューはメールや口頭で行われ、記録が残りにくい | GitLab/GitHub等のホスティングサービスにはマージリクエスト（プルリクエスト）機能が標準搭載。インラインコメント、承認フロー、差分表示がWeb UIで完結し、すべてのレビュー記録が自動的に残る |
| 5 | **ログ確認・diff表示が遅い** — SVNではログやdiffの取得にサーバーとの通信が必要で、特にリビジョン数が多いリポジトリでは操作が遅くなる | Gitは全履歴がローカルにあるため、`git log` や `git diff` は瞬時に完了する |
| 6 | **ファイルロックによる待ち時間** — SVNでは同じファイルを複数人が同時に編集するとコンフリクトが発生しやすく、ロックで排他制御する場合は他の人が待たされる | Gitではブランチごとに独立した作業領域を持つため、同じファイルを複数人が同時に編集してもブランチ上では問題ない。マージ時にコンフリクトがあっても、3-wayマージで解決が容易 |
| 7 | **CI/CDとの連携が限定的** — SVN対応のCI/CDツールは減少傾向であり、最新のDevOpsツールチェーンとの統合が困難 | GitはほぼすべてのCI/CDツール（Jenkins、GitLab CI、GitHub Actions等）と統合可能。Webhookやトリガーによる自動ビルド・自動テストが容易に構築できる |
| 8 | **人材採用・新人教育** — 新しい開発者はGitの経験しかないケースが増えており、SVNの教育コストが年々増加している | 現在のソフトウェア開発業界ではGitが事実上の標準。新規採用者はGitの基本操作を既に習得しているケースが多く、オンボーディングが効率的 |

### 1.2 移行しない場合のリスク

SVNを使い続けた場合、以下のリスクが今後増大していきます。

**ツールサポートの縮小**: 主要なIDE、CI/CDツール、クラウドサービスでSVN対応が縮小・廃止される傾向にあります。最新のDevOps環境との統合がますます困難になります。

**人材確保の困難化**: Git経験のみの開発者が増えており、SVN環境を前提とした採用は候補者の母集団を狭めます。

**セキュリティ・保守性の低下**: VisualSVN Server等のSVNサーバーソフトウェアは、Gitホスティングと比較してセキュリティアップデートや機能追加の頻度が低下しています。

---

## 2. Gitクライアントの選定

### 2.1 選定の前提

> **TortoiseSVNですら使いこなせているとは言えない**

この前提を踏まえ、Gitクライアントの選定では以下の点を重視します。

- **直感的なGUI**: コマンドラインは必須としない
- **視覚的なブランチ表示**: ブランチの分岐・マージが一目でわかるグラフ表示
- **日本語対応**: メニューやエラーメッセージの日本語化
- **TortoiseSVNからの移行のしやすさ**: 似た操作感があるとベスト
- **コスト**: 無料または低コスト
- **安定性**: 頻繁にクラッシュしない、安定したツール

### 2.2 主要Gitクライアント比較

| クライアント | 対応OS | 料金 | GitLab連携 | 学習コスト | 特徴 |
|-------------|--------|------|-----------|-----------|------|
| **Sourcetree** | Windows, Mac | 無料 | ✅ | 低〜中 | Atlassian製。ブランチグラフが見やすい。無料で機能豊富 |
| **TortoiseGit** | Windowsのみ | 無料（OSS） | △（直接連携なし） | 低 | Windows Explorer統合。TortoiseSVN経験者に最も馴染みやすい |
| **GitKraken** | Windows, Mac, Linux | 無料（公開リポのみ）/Pro $4.95/月 | ✅ | 低 | 最も視覚的で直感的。初心者に好評。プライベートリポにはPro版が必要 |
| **Fork** | Windows, Mac | $49.99（買い切り） | △ | 低〜中 | 高速・軽量。買い切りモデル。UIがシンプルで見やすい |
| **GitHub Desktop** | Windows, Mac | 無料（OSS） | △ | 最低 | GitHub公式。非常にシンプルだがGitLab連携は限定的 |
| **VS Code（Git拡張）** | Windows, Mac, Linux | 無料 | ✅（拡張機能で対応） | 中 | エディタ内蔵。GitLens拡張と組み合わせると強力 |
| **SmartGit** | Windows, Mac, Linux | 非商用無料/$37/年（商用） | ✅ | 中 | 多機能。Git-Flow対応。商用は有料 |

### 2.3 推奨クライアント

#### 推奨1: Sourcetree（メインクライアント）

**選定理由**:

- **無料**で利用でき、プライベートリポジトリでも制限なし
- GitLab、GitHub、Bitbucketいずれとも連携可能
- ブランチのグラフ表示が視覚的で、マージの流れが理解しやすい
- コミット、プッシュ、プル、マージ、コンフリクト解決等の基本操作がGUIで完結
- TortoiseSVN経験者にとって、操作の概念（コミット→プッシュ、プル→アップデート）が対応しやすい
- 日本語表示対応

**注意点**: Sourcetreeからは直接マージリクエスト（プルリクエスト）の作成はできますが、**レビュー自体（コメント、承認）はGitLab Web UI上で行います**（後述）。Sourcetreeのカスタムアクション機能でGitLab APIと連携するスクリプトを組むことも可能です。

#### 推奨2: TortoiseGit（補助クライアント）

**選定理由**:

- TortoiseSVNユーザーに最も馴染みやすいUI（右クリックメニューから操作）
- Windows Explorer統合で、ファイルのステータスがアイコンオーバーレイで表示される
- 完全無料（OSS）
- ただし、**ブランチのグラフ表示はSourcetreeの方が優れている**ため、メインではなく補助的な使用を推奨

#### 導入パターン（推奨）

```
メイン操作: Sourcetree
  ↓ コミット、プッシュ、プル、ブランチ管理、マージ、グラフ確認
  
コードレビュー: GitLab Web UI（ブラウザ）
  ↓ マージリクエストの作成、レビューコメント、承認
  
補助的に: TortoiseGit（Explorerから素早くdiff確認、ログ確認したい場合）
```

### 2.4 TortoiseSVNとの操作対応表

TortoiseSVNに慣れているメンバー向けに、操作の対応を示します。

| TortoiseSVNの操作 | Gitでの対応操作 | Sourcetreeでの操作 |
|------------------|---------------|-------------------|
| SVN Update | git pull | ツールバーの「プル」ボタン |
| SVN Commit | git add → git commit → git push | ステージング → コミット → プッシュ |
| Show Log | git log | 左サイドバーの「History」 |
| Create Branch | svn copy | ツールバーの「ブランチ」ボタン |
| Switch | git checkout / git switch | ブランチ名をダブルクリック |
| Merge | git merge | ブランチを右クリック →「マージ」 |
| Diff | git diff | ファイルをダブルクリック |
| Revert | git checkout -- file | ファイルを右クリック →「破棄」 |
| Resolve Conflict | git mergetool | コンフリクトファイルを右クリック → 外部マージツールを起動 |

**注意すべき相違点**:

SVNでは「コミット = サーバーへの送信」でしたが、Gitでは「コミット = ローカルへの記録」です。サーバーに反映するには、コミットの後に「プッシュ」が必要です。この2段階の概念が最も重要な違いであり、教育時に重点的に説明する必要があります。

---

## 3. コードレビューの仕組み

### 3.1 レビュー方法の全体像

Gitベースの開発環境では、レビューの方法は主に以下の3つがあります。

| レビュー方式 | 概要 | リモートへのプッシュ | 記録の残り方 |
|-------------|------|-------------------|-------------|
| **マージリクエスト（MR）/プルリクエスト（PR）** | リモートブランチにプッシュした変更をMR/PRとして作成し、Web UI上でレビュー | **必要** | GitLab上にすべて記録される |
| **コミット単位のレビュー（Web UI）** | プッシュ済みの特定コミットに対してWeb UI上でコメント | **必要** | GitLab上にコメントが記録される |
| **ローカルでのペアレビュー/対面レビュー** | 画面共有や同じPCでローカルブランチのdiffを確認 | **不要** | 自動記録されない（別途記録が必要） |

### 3.2 マージリクエスト（MR）によるレビュー — メインの方式

GitLab CE環境での推奨フローです。

```
① 開発者: featureブランチで作業・コミット
    ↓
② 開発者: featureブランチをリモートにプッシュ（git push origin feature/xxx）
    ↓
③ 開発者: GitLab Web UI上でマージリクエスト（MR）を作成
    ↓
④ レビュアー: GitLab Web UI上で差分を確認し、インラインコメントを記入
    ↓
⑤ 開発者: コメントに基づき修正し、同じブランチにプッシュ（MRに自動反映）
    ↓
⑥ レビュアー: 修正を確認し、承認（Approve）
    ↓
⑦ 開発者またはレビュアー: MRをマージ
```

**レビューはどこで行うのか？**

レビュー（コメント追記、差分の確認、承認）は**GitLab Web UI（ブラウザ）上で行います**。Sourcetreeなどのクライアントからはレビューできません。これはGitHub/GitLab/Bitbucketいずれのサービスでも同様です。

Sourcetreeの役割は、プッシュまでの作業（コミット、ブランチ作成、プッシュ）と、マージ後のプル操作です。

### 3.3 プルリクエスト以外でのレビュー方法

#### 方法A: コミット単位のコメント（リモートプッシュが必要）

GitLab Web UIでは、プッシュされた個々のコミットに対してもコメントを付けることが可能です。ただし、MRと異なり承認フローは組み込まれていないため、非公式なフィードバック用途に適しています。

#### 方法B: ローカルブランチのまま（プッシュなし）でレビューする方法

> **質問: 作業者がローカルBranchの内容をリモートにプッシュせずにコードレビューを受けることは可能か？**

**技術的には可能ですが、記録が残らないため非推奨です。** 具体的な方法は以下の通りです。

| 方法 | やり方 | メリット | デメリット |
|------|--------|---------|-----------|
| 画面共有レビュー | Sourcetree上で差分を表示し、Teams/Zoom等で画面共有 | プッシュ不要で即座にレビュー可能 | レビューの記録が残らない。指摘の追跡ができない |
| パッチファイル送付 | `git diff > review.patch` でパッチを作成し、メール等で共有 | プッシュ不要 | パッチの管理が煩雑。記録が分散する |
| 同じPC上でのレビュー | レビュアーが開発者の横でSourcetreeのdiff画面を確認 | 最もシンプル | 同じ場所にいる必要がある。記録が残らない |

**推奨**: 「まだ完成前だけど見てほしい」場合は、**Draft（下書き）MR**を使用してください。GitLab CEではMRのタイトルに `Draft:` を付けることで下書き状態にでき、マージボタンが無効化されます。これにより、未完成のコードをリモートにプッシュしつつ、レビューを受けることが可能です。プッシュしたからといって他のブランチに影響はありません。

### 3.4 SVNでのコードレビューとの違い

| 項目 | SVN環境でのレビュー | Git + GitLab環境でのレビュー |
|------|-------------------|--------------------------|
| レビューのトリガー | 口頭依頼、メール、または第三者ツール | マージリクエスト（MR）の作成で自動的にレビュー依頼 |
| 差分の確認方法 | TortoiseSVNのログからdiff、または第三者ツール | GitLab Web UIで差分がハイライト表示（行単位） |
| コメントの記録 | メール、Excel、口頭（散逸しやすい） | MR上のインラインコメントとして自動保存 |
| 指摘の対応確認 | メールやExcelで手動追跡 | コメントの「解決」ボタンで追跡。未解決コメントがあるとマージ不可にもできる |
| 承認フロー | なし（SVN標準機能にはない） | MRの承認機能。必須承認者の設定も可能（GitLab CEでもApproveボタンは利用可能） |
| 履歴の一元管理 | 散逸（メール、チャット、Excel等に分散） | GitLab上にすべて一元管理。プロジェクトを超えて検索可能 |
| レビュー前の状態管理 | trunkに直接コミットされることが多く、レビュー前のコードが本線に入るリスクがある | ブランチ保護ルールにより、MRが承認されるまでマージ不可に設定できる |

---

## 4. レビュー指摘の管理・分析

### 4.1 GitLab CEで利用できるレビュー分析機能

> **質問: 指摘数や分類を管理できるか？プロジェクト毎の指摘数やカテゴライズ、推移を確認できるか？**

GitLab CEには基本的な分析機能が含まれていますが、高度なレビューメトリクスは有償版（Premium/Ultimate）の機能です。

| 分析機能 | GitLab CE（無償） | GitLab Premium | GitLab Ultimate |
|---------|------------------|----------------|-----------------|
| マージリクエスト一覧・検索 | ✅ | ✅ | ✅ |
| コメント数の表示（MRごと） | ✅ | ✅ | ✅ |
| ラベルによる分類 | ✅ | ✅ | ✅ |
| マージリクエスト分析（月別スループット、平均マージ時間） | ❌ | ✅ | ✅ |
| コードレビュー分析（レビュー時間、コメント数、承認者数の統計） | ❌ | ✅ | ✅ |
| 生産性分析（マージまでの日数分布、コミット数/行数の統計） | ❌ | ❌ | ✅ |
| Value Stream Analytics（開発ライフサイクル全体の分析） | △（基本のみ） | ✅ | ✅ |

### 4.2 GitLab CEでの運用による指摘管理の実現方法

GitLab CEの標準機能を活用して、レビュー指摘の分類・管理を行う方法です。

#### ラベルによる指摘のカテゴライズ

GitLabのラベル機能を使い、レビュー指摘を分類できます。以下のようなラベルを事前に定義しておきます。

```
レビューラベルの例:
  review::bug         — バグ指摘
  review::design      — 設計改善
  review::style       — コーディング規約違反
  review::performance — パフォーマンス
  review::security    — セキュリティ
  review::nit         — 軽微な指摘（修正任意）
  review::question    — 質問・確認事項
```

MRにこれらのラベルを付けることで、ラベルごとの集計やフィルタリングが可能になります。

#### GitLab APIを活用した集計

GitLab CEでもAPIが利用可能です。以下のようなスクリプトを定期実行することで、指摘数の推移を集計できます。

```
GitLab API で取得できるデータ:
  - プロジェクト別のMR数（作成数、マージ数、クローズ数）
  - MRごとのコメント数（ディスカッション数）
  - ラベル別のMR数
  - ユーザー別のMR作成数・レビュー参加数
  - MR作成からマージまでの所要時間
```

APIで取得したデータをExcelやGoogleスプレッドシートに出力すれば、指摘数の推移グラフやプロジェクト間比較が可能です。

#### 外部ツールとの連携による高度な分析

有償版にアップグレードせずに高度な分析を行いたい場合は、以下の方法もあります。

- **Redmine / Jira連携**: レビュー指摘をイシューとして起票し、Redmine/Jira側で統計管理
- **Grafana + GitLab APIデータ**: APIデータをGrafanaでダッシュボード化
- **自作の集計スクリプト**: GitLab APIからデータを取得し、CSV/Excel出力する簡易ツールを作成

---

## 5. リポジトリの立ち上げタイミングとブランチ戦略

### 5.1 SVNの場合（現状の確認）

SVNでは一般的に以下の流れでリポジトリを立ち上げます。

```
1. VisualSVN Server上でリポジトリを新規作成
2. trunk / branches / tags のディレクトリ構造を作成（初期コミット）
3. trunkに初期コードをコミット
4. 開発メンバーにアクセス権を付与
5. 開発開始
```

最初に立てるのは **trunk** で、ここが開発の本線です。branches、tagsは必要になった時点で使用します。

### 5.2 Gitの場合

#### リポジトリを立ち上げるタイミング

**結論: コードがない段階でリポジトリを作成して問題ありません。**

GitLab上でプロジェクトを作成すると、自動的にリポジトリが生成されます。この時点ではコードがなくても構いません。README.mdや.gitignoreファイルだけの初期コミットで十分です。

```
推奨手順:
1. GitLab上で新規プロジェクト（リポジトリ）を作成
   → READMEを生成して初期化（GitLabの画面上で選択可能）
2. この時点で「main」ブランチが自動作成される
3. 必要に応じて「develop」ブランチを作成
4. 開発メンバーをプロジェクトに追加
5. 開発開始
```

#### 最初にどのブランチを立ち上げるか

SVNの `trunk` / `branches` / `tags` に対応するGitの概念は以下の通りです。

| SVN | Git | 説明 |
|-----|-----|------|
| trunk | main（またはmaster） | 本線ブランチ。リリース可能な状態を維持 |
| branches/develop | develop | 開発統合ブランチ（Git Flowの場合） |
| branches/feature-xxx | feature/xxx | 機能開発ブランチ |
| tags/v1.0 | v1.0（タグ） | タグ。Gitではブランチではなくタグ機能を使う |

**ブランチ戦略の選択肢（10〜20名規模向け）**:

#### パターンA: Git Flow（推奨 — SVNのtrunk/branches構造に近い）

```
main ──────────────────────────────────────── リリース用
  │
  └── develop ─────────────────────────────── 開発統合用
        │        │        │
        ├── feature/login  ├── feature/search  ├── feature/report
        │   (機能開発)      │   (機能開発)      │   (機能開発)
        
タグ: v1.0, v1.1, v2.0 ... （リリース時に打つ）
```

最初に `main` と `develop` を作成します。各機能はdevelopから派生するfeatureブランチで開発し、完了後にdevelopへマージ（MR経由）。リリース時にdevelopからmainへマージしてタグを打ちます。

#### パターンB: GitHub Flow（シンプル — 小規模・頻繁リリース向け）

```
main ──────────────────────────────────────── 本線（常にリリース可能）
  │        │        │
  ├── feature/login  ├── feature/search  ├── fix/bug-123
  
タグ: v1.0, v1.1 ... （任意）
```

`main` のみを常設ブランチとし、featureブランチをmainから直接派生させます。完了後にMR経由でmainにマージ。developブランチが不要でシンプルですが、リリースタイミングの管理は別途必要です。

**推奨**: SVNのtrunk/branches構造に慣れているチームには**Git Flow（パターンA）**をお勧めします。概念的な対応関係が理解しやすいためです。

---

## 6. GitLab CE + Sourcetreeでの通知

### 6.1 通知の仕組み

> **質問: GitLab CE（オンプレ版）とSourcetreeの環境で、MRやレビュー完了は通知されるのか？**

**はい、通知されます。** ただし、通知の経路を理解しておく必要があります。

| 通知の経路 | 対応状況 | 通知されるイベント例 |
|-----------|---------|-------------------|
| **GitLab → メール通知** | ✅（GitLab CEで対応） | MR作成、コメント追加、MR承認、MRマージ、パイプライン結果 |
| **GitLab Web UI → ToDo通知** | ✅（GitLab CEで対応） | 自分がアサインされた、メンションされた、レビュー依頼された |
| **Sourcetree → 通知** | ❌（Sourcetreeにはプッシュ通知機能がない） | — |

### 6.2 メール通知の設定

GitLab CEではSMTPサーバーを設定することで、以下のイベントごとにメール通知が送られます。

**通知されるイベント（主なもの）**:

- マージリクエストが作成された
- マージリクエストにコメントが追加された
- マージリクエストが承認（Approve）された
- マージリクエストがマージされた
- マージリクエストがクローズされた
- 自分が@メンションされた
- 自分がレビュアーとしてアサインされた
- パイプラインの成功/失敗（CI/CD利用時）

**注意点**: GitLab CEのオンプレ版では、メール通知を有効にするためにGitLabのSMTP設定（`/etc/gitlab/gitlab.rb`）を事前に行う必要があります。社内メールサーバーとの連携設定が必要です。

**通知レベルの設定**: 各ユーザーが自分のプロフィール設定から通知レベルを選択できます（Global / Watch / Participating / On mention / Disabled / Custom）。

### 6.3 運用のポイント

Sourcetreeから直接通知を受けることはできませんが、以下の運用で問題なくレビューフローが回ります。

```
① 開発者: Sourcetreeでコミット → プッシュ
② 開発者: GitLab Web UIでMRを作成（レビュアーを指定）
③ レビュアー: メール通知を受信 → GitLab Web UIでレビュー
④ 開発者: メール通知（コメント追加）を受信 → GitLab Web UIで確認・対応
⑤ 開発者: Sourcetreeで修正コミット → プッシュ（MRに自動反映）
⑥ レビュアー: メール通知（更新）を受信 → 再レビュー → 承認
⑦ 開発者: メール通知（承認完了）を受信 → マージ
```

---

## 7. SVNとGitのリポジトリ管理方法の違い

### 7.1 SVN（VisualSVN Server）でのリポジトリ管理

```
管理構造:
  VisualSVN Server（1台のサーバー）
    ├── リポジトリA（サーバー管理者が作成）
    ├── リポジトリB（サーバー管理者が作成）
    └── リポジトリC（サーバー管理者が作成）
    
アクセス権:
  Windows ACLまたはVisualSVN Server Manager上で設定
  → 開発グループ（Active Directoryグループ等）に対して
    リポジトリ単位でRead/Write権限を付与
    
リポジトリの所有者:
  サーバー管理者（インフラ担当者）が一元管理
  開発者はリポジトリの作成・削除権限を持たない（通常）
```

### 7.2 GitLab CE（オンプレ版）でのリポジトリ管理

GitLabでは「グループ」と「プロジェクト」という概念で管理します。

```
管理構造:
  GitLab CE（1台のサーバー）
    ├── グループA（例: 開発1課）
    │     ├── プロジェクト（リポジトリ）A-1
    │     ├── プロジェクト（リポジトリ）A-2
    │     └── サブグループA-i
    │           └── プロジェクトA-i-1
    ├── グループB（例: 開発2課）
    │     └── プロジェクトB-1
    └── グループC（例: 共通基盤）
          └── プロジェクトC-1
```

#### 権限体系

GitLabでは、VisualSVN Serverとは異なり、**ロールベースのアクセス制御**を使用します。

| GitLabロール | 権限の概要 | SVNでの対応 |
|-------------|-----------|------------|
| Guest | イシューの閲覧、コメント | 読み取りのみ |
| Reporter | コードの閲覧、CI/CD結果の確認 | 読み取りのみ |
| Developer | コードのプッシュ、MR作成、ブランチ作成 | 読み書き（trunk以外） |
| Maintainer | ブランチ保護設定、MRマージ、プロジェクト設定変更 | 読み書き + 管理 |
| Owner | グループ/プロジェクトの作成・削除、メンバー管理 | サーバー管理者 |

#### VisualSVN Serverからの移行パターン

| VisualSVN Serverの設定 | GitLab CEでの対応 |
|-----------------------|-----------------|
| リポジトリの作成 | GitLab上で「新規プロジェクト」を作成（Maintainer以上の権限で可能） |
| 開発グループへのアクセス権付与 | GitLabのグループにメンバーを追加し、ロールを設定 |
| Active Directory連携 | GitLab CEのLDAP連携機能でAD認証を統合可能 |
| リポジトリ単位の権限設定 | プロジェクト単位でメンバーとロールを設定 |
| パスベースの権限（ディレクトリ単位） | GitLabでは非対応。リポジトリ（プロジェクト）単位での管理が基本。必要に応じてリポジトリを分割する |

**重要な違い**: SVNではサーバー管理者がすべてのリポジトリを一元管理するのが一般的ですが、GitLabでは**各グループのOwnerやMaintainerがプロジェクト（リポジトリ）を管理できます**。これにより、インフラ担当者の負荷を軽減しつつ、各チームが自律的にリポジトリを管理できるようになります。ただし、ガバナンスのためにGitLab管理者（Admin）がグループ構成やルールの方針を定める必要があります。

---

## 8. Git移行の効果測定方法

### 8.1 測定指標

Git移行後の効果を定量的に測定するための指標です。

#### 開発効率の指標

| 指標 | 測定方法 | 期待される改善 |
|------|---------|--------------|
| **コミット頻度** | Git log / GitLab統計 | SVN時代より頻繁な小さいコミットが増える（品質向上の兆候） |
| **ブランチ作成数** | GitLab統計 | SVN時代は少なかった（重いため）→ 増加 |
| **マージリクエスト数** | GitLab統計 | レビュー文化の定着を示す指標 |
| **レビュー所要時間** | MR作成〜マージまでの時間 | 測定開始後、段階的に短縮を目指す |
| **コンフリクト発生頻度** | 手動集計またはMRの状態確認 | SVN時代と比較して減少が期待される |

#### 品質の指標

| 指標 | 測定方法 | 期待される改善 |
|------|---------|--------------|
| **レビュー指摘密度** | 指摘数 ÷ コード行数 | 初期は増加（レビューが機能している証拠）、その後減少 |
| **リリース後バグ数** | バグ管理ツール | レビュー文化の定着とともに減少 |
| **本線ブランチの安定性** | mainブランチのビルド成功率 | ブランチ保護とMRレビューにより向上 |

#### 運用の指標

| 指標 | 測定方法 | 期待される改善 |
|------|---------|--------------|
| **サーバー障害時の影響時間** | 障害発生〜復旧の時間×影響人数 | ローカルで作業継続可能なため、影響が大幅に軽減 |
| **新メンバーのオンボーディング時間** | 環境構築〜初コミットまでの日数 | SVNよりGitの方が教材・情報が豊富 |
| **開発者の満足度** | アンケート調査 | 最新ツールの利用による満足度向上 |

### 8.2 測定のタイムライン

```
移行前（ベースライン計測）:
  ・SVN環境での各指標を1ヶ月間計測（比較基準とする）
  ・特に「サーバー障害の頻度・影響」「コンフリクト発生頻度」「レビュー手段」を記録

移行直後（1〜3ヶ月）:
  ・学習期間として、効率の一時的な低下は想定内
  ・コミット頻度、ブランチ利用数の推移を確認
  ・操作に関する問い合わせ件数を記録（教育効果の測定）

安定期（3〜6ヶ月）:
  ・MR数、レビュー所要時間、レビュー指摘密度の推移を確認
  ・SVN時代のベースラインと比較
  ・開発者へのアンケート実施

定着期（6ヶ月〜）:
  ・四半期ごとに指標を集計・報告
  ・レビュー文化の定着度を確認
  ・必要に応じてブランチ戦略やレビュールールを見直し
```

### 8.3 GitLab CEで取得できるデータ

GitLab CEのWeb UIおよびAPIから、以下のデータを取得できます。

| データ | 取得方法 |
|--------|---------|
| プロジェクト別のMR数 | Web UI: プロジェクト → マージリクエスト / API: `/api/v4/projects/:id/merge_requests` |
| MRごとのコメント数 | API: `/api/v4/projects/:id/merge_requests/:mr_iid/notes` |
| ユーザー別のコミット数 | Web UI: プロジェクト → Contributors / API: `/api/v4/projects/:id/repository/contributors` |
| ブランチ一覧 | Web UI: プロジェクト → Repository → Branches / API: `/api/v4/projects/:id/repository/branches` |
| MR作成日〜マージ日 | API: MRオブジェクトの `created_at` と `merged_at` から計算 |

これらのデータをスクリプトで定期取得し、スプレッドシートやダッシュボードに出力することで、継続的な効果測定が可能です。

---

## 付録: 用語対応表

| SVN用語 | Git用語 | 説明 |
|---------|---------|------|
| リビジョン（Revision） | コミット（Commit） | コードの変更単位 |
| チェックアウト（Checkout） | クローン（Clone） | リポジトリの複製を取得 |
| アップデート（Update） | プル（Pull） | リモートの最新を取得・統合 |
| コミット（Commit） | コミット + プッシュ（Commit + Push） | SVNのコミットはGitではコミット+プッシュの2段階 |
| trunk | main（またはmaster） | 開発の本線 |
| branches | ブランチ（branch） | 並行開発用の分岐 |
| tags | タグ（tag） | リリースポイントの記録 |
| マージ（Merge） | マージ（Merge）/ マージリクエスト（MR） | ブランチの統合 |
| ロック（Lock） | なし（Git LFSのロック機能は別途あり） | Gitでは基本的にファイルロックは使わない |
| リポジトリ | プロジェクト（GitLab上） | GitLabではリポジトリをプロジェクトと呼ぶ |

---

## 改訂履歴

| 日付 | 版 | 内容 |
|------|---|------|
| 2026/3/5 | 1.0 | 初版作成 |
