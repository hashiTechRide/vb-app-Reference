ComponentOneのC1FlexGridとC1Comboは私の知識が古い可能性があるので、まず現行の正確な情報を確認します。

DataMapとColumn.Editorの詳細も確認しておきます。

十分な情報が集まりました。まず用語の整理から入ります。あなたが挙げた「C1Combo」「ComboList」は別物なので、そこを解きほぐしてから、グリッドの編集設定を全部まとめます。

## 用語の整理（重要）

混同しやすいのですが、C1FlexGridでセル編集の見た目を決める手段は大きく3つあり、名前が似ていて紛らわしいです。

- **ComboList**（グリッドのプロパティ・文字列で指定）… グリッド組み込みの簡易エディタ。文字列を渡すだけでドロップダウンやボタンになる。手軽だが**文字列しか扱えない**。
- **DataMap**（列のプロパティ・辞書で指定）… 「保存値（ID）↔表示値（名称）」を対応させる辞書。工場IDを保存して工場名を表示、が実現できる。マスタ選択の本命。
- **Column.Editor**（列に別コントロールを埋め込む）… `C1Combo`（＝ComponentOneの単体コンボボックスコントロール）などを列のエディタとして差し込む。最も自由度が高い。

あなたが言う「ComboListでやる」は1つ目、「C1Combo（C1ComboBox）でやる」は3つ目に当たります。ComboListはAllowEditingをtrueにしてBeforeEditイベントで文字列を設定して使うもので、 [Mescius](https://developer.mescius.com/componentone/docs/win/online-flexgrid/C1.Win.C1FlexGrid.4.5.2~C1.Win.C1FlexGrid.C1FlexGridBase~ComboList.html) テキストボックス・ドロップダウンリスト・ドロップダウンコンボ・編集ボタンのいずれかを選べます。 [Mescius](https://developer.mescius.com/componentone/docs/win/online-flexgrid/C1.Win.FlexGrid.6~C1.Win.FlexGrid.C1FlexGrid~ComboList.html)

以下、グリッドの編集設定を全体としてまとめ、その中でComboList方式とC1Combo方式を詳述します。

---

## 1. まず土台：C1FlexGridの編集の基本

セルを編集可能にする前提。

```vb
grid.AllowEditing = True          ' これがないと一切編集不可
grid.Cols("NAME").AllowEditing = True   ' 列単位でも制御可
```

編集に関わる主要イベントの流れ（発生順）。

```
セルで編集開始しようとする
   → BeforeEdit      … 編集を許すか、この行のComboListを何にするか決める
   → SetupEditor     … 埋め込みエディタ(C1Combo等)を使う場合、ここで最終調整
   （ユーザーが入力）
   → ValidateEdit    … 入力値を検証。e.Cancel=Trueで確定を止める
   → AfterEdit       … 確定後。値はもうセルに入っている
```

前回まで作ってきた `ColumnDefinition` は、この各イベントで「対象列の定義に処理を委譲する」形で噛ませます。

---

## 2. 方式A：ComboList（組み込み簡易エディタ）

### 何ができるか

文字列を1つ設定するだけで編集UIが決まります。セルに定義済みの選択肢がある場合、パイプ文字で区切った文字列を作ってComboListに割り当てると、ドロップダウンが表示されます。 [Componentone](http://helpcentral.componentone.com/nethelp/c1flexgrid/listsandcombos.html)

設定パターン（文字列の形で種類が変わる）:

| ComboListに設定する文字列 | 見た目・挙動 |
|---|---|
| `""`（空） | 普通のテキストボックス編集 |
| `"男\|女\|不明"` | ドロップダウン**リスト**（選択のみ、手入力不可） |
| `"\|男\|女\|不明"`（先頭にパイプ） | ドロップダウン**コンボ**（選択＋手入力可） |
| `"..."` | セルに**省略ボタン**（…）表示、押すと `CellButtonClick` 発火、直接編集不可 |
| `"\|..."` | テキスト編集＋省略ボタンの両方 |

先頭にパイプ文字を付けると（例 "|True|False"）、テキストボックスとドロップダウンリストを組み合わせたコンボになり、ユーザーが一覧にない値も入力できます。 [Componentone](http://helpcentral.componentone.com/nethelp/c1flexgrid/listsandcombos.html) 省略記号("...")を設定すると、セルの右側にボタンが表示され、クリック時にCellButtonClickイベントが発火します。これが前回の「取引先を別フォームで選択」に使えます。 [Mescius](https://developer.mescius.com/componentone/docs/win/online-flexgrid/C1.Win.C1FlexGrid.4.5.2~C1.Win.C1FlexGrid.C1FlexGridBase~ComboList.html)

### 列全体に同じComboListを使う（推奨される基本形）

列内の全行が同じ種類のデータなら、ColumnのComboListプロパティを使えば、グリッドが編集中の列に応じて自動でリストを選ぶのでイベント処理は不要です。 [Mescius](https://developer.mescius.com/componentone/docs/win/online-flexgrid/C1.Win.C1FlexGrid.4.5.2~C1.Win.C1FlexGrid.C1FlexGridBase~ComboList.html)

```vb
' 列に直接設定。BeforeEditを書かなくてよい
grid.Cols("GENDER").ComboList = "男|女|不明"           ' 選択のみ
grid.Cols("STATUS").ComboList = "|進行中|完了|保留"     ' 選択＋手入力
grid.Cols("CLIENT").ComboList = "..."                  ' 別フォーム選択ボタン
```

### 行ごとに種類を変える（PropertyGridのような場合）

同じ列でも行によって異なる種類のデータを扱う場合、BeforeEditイベントで行に応じてComboListを設定できます。 [Mescius](https://developer.mescius.com/componentone/docs/win/online-flexgrid/C1.Win.FlexGrid.6~C1.Win.FlexGrid.C1FlexGrid~ComboList.html)

```vb
Private Sub grid_BeforeEdit(sender As Object, e As RowColEventArgs) Handles grid.BeforeEdit
    ' 行や隣接セルの値に応じて動的に切替
    If grid(e.Row, "TYPE") = "区分A" Then
        grid.ComboList = "X|Y|Z"
    Else
        grid.ComboList = ""
    End If
End Sub
```

### ComboListの限界

ComboListは文字列のリストしか指定できません。セルはこれらの文字列をそのまま保存・表示します。データ（例：顧客ID）を保存して関連する表示値（例：顧客名）を表示したい場合は、DataMapプロパティを使います。 [Mescius](https://developer.mescius.com/componentone/docs/win/online-flexgrid/C1.Win.C1FlexGrid.4.5.2~C1.Win.C1FlexGrid.C1FlexGridBase~ComboList.html)

つまり工場を「IDで保存・名前で表示」したいなら、ComboListだけでは不足で、次のDataMapが要ります。

---

## 3. 方式B：DataMap（ID保存・名称表示）＝マスタ選択の本命

### 何ができるか

セルには**キー（工場ID）を保存**し、画面には**対応する表示値（工場名）を出す**。ドロップダウンには名称一覧が出て、選ぶと裏でIDが入ります。マスタ管理の工場・区分などに最適です。

C1ComboBoxをエディタに使う場合も、DataTableと同じキー・説明のペアを持つDictionaryを作り、それをFlexGridの列のDataMapプロパティに割り当てる必要があります。 [Mescius](https://developer.mescius.com/forums/winforms-edition/c1-combobox-as-editor-on-flixgrid-column)

### 単純な辞書での設定

```vb
' 工場マスタ: ID → 名称
Dim factoryMap As New Dictionary(Of Integer, String) From {
    {1, "静岡工場"}, {2, "浜松工場"}, {3, "沼津工場"}
}
grid.Cols("FACTORY_ID").DataMap = factoryMap
```

これで `FACTORY_ID` 列は、セルに1・2・3を保持しつつ、画面には「静岡工場」等を表示し、編集時はドロップダウンで名称から選べます。DataMapを設定すると自動でドロップダウン編集になります。

### DataTableから作る（マスタをDBから引く場合）

`MultiColumnDictionary` を使うと、DataTableから直接マップを作れます。MultiColumnDictionaryクラスは、単純な文字列ベースのデータマップや、データソースから取得するバインドされたデータマップを実装できます。 [Componentone](http://helpcentral.componentone.com/nethelp/c1flexgrid/C1.Win.C1FlexGrid.4~C1.Win.C1FlexGrid.MultiColumnDictionary.html)

```vb
' employeesテーブルから、キー=EmployeeID、表示に複数列を使うマップ
Dim dt As DataTable = GetFactoryMaster()
Dim map As New MultiColumnDictionary(dt, "FACTORY_ID", New String() {"FACTORY_NAME"}, 0)
grid.Cols("FACTORY_ID").DataMap = map
```

データソースが変わるとリストも自動的に更新されます。複数列を表示に使えるので「コード＋名称」を並べたドロップダウンも作れます。 [Componentone](http://helpcentral.componentone.com/nethelp/c1flexgrid/C1.Win.C1FlexGrid.4~C1.Win.C1FlexGrid.MultiColumnDictionary.html)

### 隣の列の値でマップを変える（連動ドロップダウン）

「大分類を選ぶと小分類の選択肢が変わる」ような連動は、`StartEdit`（または`BeforeEdit`）でその都度DataMapを差し替えます。ただし注意点があります。列にDataMapを設定すると、その列の全セル値をDataMapのキーと突き合わせるため、キーに無い他行のセルが空白になります。 [Mescius](https://developer.mescius.com/forums/winforms-edition/c1flexgrid-with-datamap-that-changes-based-on-value-in-previous-column)

対策として、列全体ではなく**編集中セルだけ**にマップを当てる方法が案内されています。列にDataMapを設定する代わりに、CellStyleのDataMapを使い、SetCellStyleで編集中のセルにだけ適用します。 [Mescius](https://developer.mescius.com/forums/winforms-edition/c1flexgrid-with-datamap-that-changes-based-on-value-in-previous-column)

```vb
Private Sub grid_StartEdit(sender As Object, e As RowColEventArgs) Handles grid.StartEdit
    If grid.Cols(e.Col).Name = "SUB_CATEGORY" Then
        Dim dtSub = LoadSubCategories(grid(e.Row, "MAIN_CATEGORY"))
        Dim cs As CellStyle = grid.Styles.Add("")
        cs.DataMap = New MultiColumnDictionary(dtSub, "SUB_ID", New String() {"SUB_NAME"}, 0)
        grid.SetCellStyle(e.Row, e.Col, cs)   ' このセルだけに適用
    End If
End Sub
```

---

## 4. 方式C：Column.Editor に C1Combo（C1ComboBox）を埋め込む

### C1Comboとは何か

C1Combo（現行では `C1ComboBox`）は、ComponentOneの**単体のコンボボックスコントロール**です。標準ComboBoxより高機能で、複数列表示・オートコンプリート・DataTableバインドなどができます。これを**グリッドの列のエディタとして差し込む**のが方式Cです。ComboListが「グリッド組み込みの簡易版」なのに対し、こちらは「本格的なコンボを持ち込む」イメージです。

### いつ使うか

ComboListやDataMapで足りるならそちらが簡単です。C1Comboを埋め込むのは、次のような場合。

- オートコンプリートや複数列表示など、リッチなコンボ挙動が欲しい
- 既にフォームで使っているC1Comboと編集体験を揃えたい
- ドロップダウンの見た目・挙動を細かく制御したい

### 基本の埋め込み方

```vb
' C1ComboBoxを生成してマスタをバインド
Dim factoryCombo As New C1.Win.C1List.C1Combo()
factoryCombo.DataSource = GetFactoryMaster()   ' DataTable
factoryCombo.DisplayMember = "FACTORY_NAME"    ' 表示する列
factoryCombo.ValueMember = "FACTORY_ID"        ' 実際に扱う値
factoryCombo.DropdownWidth = 200

' 列のエディタとして設定
grid.Cols("FACTORY_ID").Editor = factoryCombo
```

列のエディタは Cols(n).Editor に設定します。埋め込みエディタは [Mescius](https://developer.mescius.com/forums/winforms-edition/c1flexgrid-multi-select-in-a-combolist) `SetupEditor` イベントで編集開始直前に微調整できます。

```vb
Private Sub grid_SetupEditor(sender As Object, e As RowColEventArgs) Handles grid.SetupEditor
    If TypeOf grid.Editor Is C1.Win.C1List.C1Combo Then
        Dim cmb = DirectCast(grid.Editor, C1.Win.C1List.C1Combo)
        ' 例：ドロップダウンを即開く、現在値を選択状態にする等
    End If
End Sub
```

### 重要な注意：Editorを使ってもDataMapが要る

C1Comboをエディタにしても、それは「編集中の見た目」を担うだけです。**編集していない通常表示のセルは、DataMapが無いとID（値）のまま表示されます。**だから表示のためにDataMapも併用します。前掲フォーラムの指摘どおり、C1ComboBoxに割り当てたDataTableと同じキー・説明のペアを持つDictionaryを作り、列のDataMapに割り当てる必要があります。 [Mescius](https://developer.mescius.com/forums/winforms-edition/c1-combobox-as-editor-on-flixgrid-column)

```vb
' エディタ（編集時の見た目）
grid.Cols("FACTORY_ID").Editor = factoryCombo
' DataMap（非編集時の表示：ID→名称）
grid.Cols("FACTORY_ID").DataMap = factoryMapDictionary
```

これを忘れると「選択中は名前が出るが、確定したらIDの数字に戻る」という典型的な不具合になります。

### さらに自由度が要るなら IC1EmbeddedEditor

任意のコントロールをエディタにするには、そのコントロールが `IC1EmbeddedEditor` を実装する必要があります。サードパーティのエディタをC1FlexGridで使いたい場合は、派生クラスを作ってこのインターフェースを実装します。IC1EmbeddedEditorのメソッドはリフレクションで呼ばれるため、コントロールはインターフェース全体を実装する必要はなく、シグネチャが一致するpublicメソッドがあれば呼び出されます。C1Comboは対応済みなので通常そのまま使えます。 [GitHub](https://github.com/vincentpaca/diia/blob/master/Main/C1.Win.C1FlexGrid.2.xml)

---

## 5. 三方式の使い分け（あなたの要件に対応）

| 要件 | 最適な方式 | 理由 |
|---|---|---|
| 真偽（チェックボックス） | 列を`Boolean`型にする（後述） | 自動でチェックボックス化 |
| 選択肢が固定・少数・文字列でよい | **ComboList** `"A\|B\|C"` | 一行で済む・最も簡単 |
| 工場など**IDで保存・名称表示**のマスタ | **DataMap**（＋必要ならComboList/Editor） | ID↔名称の対応が本来の用途 |
| 取引先など**別フォームで選択** | **ComboList `"..."`** ＋ `CellButtonClick` | ボタン表示と別フォーム起動 |
| リッチなコンボ（補完・複数列） | **C1Combo を Editor** ＋ DataMap | 高機能な編集体験 |

真偽のチェックボックスは、列のデータ型をBooleanにするのが素直です（DataTableのカラムがBoolean型なら自動）。

```vb
' DataTableのカラムがGetType(Boolean)なら自動でチェックボックス表示
' DataMapやFormatが無い限り、Boolean列はチェックボックスになる
grid.Cols("ENABLED").DataType = GetType(Boolean)
```

---

## 6. 前回のColumnDefinition構成への組み込み

各方式を、前回作った列定義クラスの `ApplyToColumn` に閉じ込めます。これで「どの列がどの編集方式か」が定義クラス1箇所に集約されます。

```vb
' 選択肢固定（ComboList方式）
Public Class ChoiceColumn
    Inherits ColumnDefinition
    Public Property Choices As String        ' "男|女|不明"
    Public Property AllowFreeText As Boolean  ' Trueなら先頭にパイプ
    Public Overrides Sub ApplyToColumn(grid As C1FlexGrid, col As Column)
        col.ComboList = If(AllowFreeText, "|", "") & Choices
    End Sub
End Class

' マスタ選択（DataMap方式）
Public Class MasterColumn
    Inherits ColumnDefinition
    Public Property Map As IDictionary        ' ID→名称
    Public Overrides Sub ApplyToColumn(grid As C1FlexGrid, col As Column)
        col.DataMap = Map
    End Sub
End Class

' 別フォーム選択（ComboList "..." 方式）
Public Class LookupColumn
    Inherits ColumnDefinition
    Public Property PickValue As Func(Of Object, LookupResult)
    Public Overrides Sub ApplyToColumn(grid As C1FlexGrid, col As Column)
        col.ComboList = "..."   ' セルボタン。クリックは grid.CellButtonClick で処理
    End Sub
End Class

' リッチコンボ（C1Combo Editor方式）
Public Class ComboEditorColumn
    Inherits ColumnDefinition
    Public Property Editor As Control
    Public Property Map As IDictionary
    Public Overrides Sub ApplyToColumn(grid As C1FlexGrid, col As Column)
        col.Editor = Editor      ' 編集時の見た目
        col.DataMap = Map        ' 非編集時のID→名称表示（必須）
    End Sub
End Class

' 真偽（チェックボックス）
Public Class BooleanColumn
    Inherits ColumnDefinition
    Public Overrides Sub ApplyToColumn(grid As C1FlexGrid, col As Column)
        col.DataType = GetType(Boolean)
    End Sub
End Class
```

セルボタンのクリックは、前回どおりグリッド共通イベントで定義に委譲します。

```vb
Private Sub grid_CellButtonClick(sender As Object, e As RowColEventArgs) Handles grid.CellButtonClick
    Dim def = TryCast(_defs(grid.Cols(e.Col).Name), LookupColumn)
    If def IsNot Nothing Then
        Dim r = def.PickValue(grid(e.Row, e.Col))
        If r.Selected Then grid(e.Row, e.Col) = r.Id
    End If
End Sub
```

---

## 7. グリッド設定の全体まとめ（初期化の型）

編集まわり以外も含めた、グリッド初期化の基本セットです。

```vb
Private Sub SetupGrid(columns As IReadOnlyList(Of ColumnDefinition))
    With grid
        ' 基本
        .DataSource = _table
        .AllowEditing = True
        .SelectionMode = SelectionModeEnum.Cell   ' エクセルライクなセル単位選択
        .AllowSorting = AllowSortingEnum.None       ' 業務編集画面ではソート切ることが多い
        .KeyActionTab = KeyActionEnum.MoveAcross    ' Tabで右へ移動
        .KeyActionEnter = KeyActionEnum.MoveDown    ' Enterで下へ（エクセルライク）

        ' 見た目
        .Styles.Fixed.BackColor = Color.FromArgb(240, 240, 240)
        .ExtendLastCol = True
        .DrawMode = DrawModeEnum.OwnerDraw          ' 背景色を自前で塗るなら

        ' 列ごとの編集方式を適用
        For Each def In columns
            Dim col = .Cols(def.Name)
            col.Caption = def.Caption
            col.Width = def.Width
            col.AllowEditing = Not def.ReadOnly
            def.ApplyToColumn(grid, col)             ' ← 方式ごとの設定は多態で
        Next
    End With
End Sub
```

検証は前回どおり `ValidateEdit` で定義に委譲します。

```vb
Private Sub grid_ValidateEdit(sender As Object, e As ValidateEditEventArgs) Handles grid.ValidateEdit
    Dim def = _defs(grid.Cols(e.Col).Name)
    Dim err = def.Validate(grid.Editor?.Text)
    If err IsNot Nothing Then e.Cancel = True   ' 確定させずセルに留まる
End Sub
```

---

## まとめ

- あなたの言う「C1Combo」と「ComboList」は別物。編集の見た目を決める手段は3系統ある。**ComboList**（文字列で簡易指定）、**DataMap**（ID保存・名称表示の辞書）、**Column.Editor**（C1Comboなど別コントロール埋め込み）。
- **ComboList** … AllowEditingをtrueにして、テキスト／ドロップダウンリスト／ドロップダウンコンボ／編集ボタンを文字列で選ぶ。列全体なら [Mescius](https://developer.mescius.com/componentone/docs/win/online-flexgrid/C1.Win.FlexGrid.6~C1.Win.FlexGrid.C1FlexGrid~ComboList.html) `Cols(n).ComboList`、行ごとに変えるなら `BeforeEdit`。文字列しか扱えず、ID保存・名称表示はできない。 [Mescius](https://developer.mescius.com/componentone/docs/win/online-flexgrid/C1.Win.C1FlexGrid.4.5.2~C1.Win.C1FlexGrid.C1FlexGridBase~ComboList.html)
- **DataMap** … 工場のようなマスタ選択の本命。IDを保存し名称を表示。`Dictionary` か、DataTableから作る `MultiColumnDictionary` を列に割り当てる。連動ドロップダウンは列全体に設定すると他行が空白になるため、CellStyleのDataMapを編集セルだけに当てる。 [Mescius](https://developer.mescius.com/forums/winforms-edition/c1flexgrid-with-datamap-that-changes-based-on-value-in-previous-column)
- **C1Combo を Editor** … リッチなコンボが要るとき列のEditorに差し込む。ただし非編集時の名称表示のためにDataMapの併用が必要。忘れるとID表示に戻る。 [Mescius](https://developer.mescius.com/forums/winforms-edition/c1-combobox-as-editor-on-flixgrid-column)
- 真偽はDataTableのカラムをBoolean型にすれば自動でチェックボックス。
- 取引先の別フォーム選択は `ComboList = "..."` ＋ `CellButtonClick`。
- 全部、前回の `ColumnDefinition.ApplyToColumn` に閉じ込めれば、グリッド初期化は種類を意識せずループするだけで済む。

要件に素直に当てると、工場＝**DataMap**、真偽＝**Boolean列**、取引先＝**ComboList "..."**、補完が欲しい選択＝**C1Combo Editor＋DataMap**、という振り分けになります。まずDataMapとComboListで組み、リッチさが要る列だけC1Combo Editorに上げるのが、複雑さを最小に保つ順序です。
